---
uid: mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
title: "#4 di iterazione – Rendere l'applicazione ad accoppiamento libero (VB) Documenti Microsoft"
author: rick-anderson
description: In questa quarta iterazione, sfruttiamo diversi modelli di progettazione software per semplificare la manutenzione e la modifica dell'applicazione Contact Manager. Per...
ms.author: riande
ms.date: 02/20/2009
ms.assetid: 92c70297-4430-4e4e-919a-9c2333a8d09a
msc.legacyurl: /mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
msc.type: authoredcontent
ms.openlocfilehash: 1026e307b7e498a8b1392f2907c08cdcd05a3199
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542768"
---
# <a name="iteration-4--make-the-application-loosely-coupled-vb"></a><span data-ttu-id="cb8ec-104">Iterazione 4 - Rendere l'applicazione a regime di controllo libero (VB)</span><span class="sxs-lookup"><span data-stu-id="cb8ec-104">Iteration #4 – Make the application loosely coupled (VB)</span></span>

<span data-ttu-id="cb8ec-105">da parte [di Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="cb8ec-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="cb8ec-106">Scarica il codice</span><span class="sxs-lookup"><span data-stu-id="cb8ec-106">Download Code</span></span>](iteration-4-make-the-application-loosely-coupled-vb/_static/contactmanager_4_vb1.zip)

> <span data-ttu-id="cb8ec-107">In questa quarta iterazione, sfruttiamo diversi modelli di progettazione software per semplificare la manutenzione e la modifica dell'applicazione Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-107">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="cb8ec-108">Ad esempio, viene eseguito il refactoring dell'applicazione per usare il modello Repository e il modello di inserimento delle dipendenze.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-108">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

## <a name="building-a-contact-management-aspnet-mvc-application-vb"></a><span data-ttu-id="cb8ec-109">Creazione di una gestione dei contatti ASP.NET applicazione MVC (VB)Building a Contact Management ASP.NET MVC Application (VB)</span><span class="sxs-lookup"><span data-stu-id="cb8ec-109">Building a Contact Management ASP.NET MVC Application (VB)</span></span>

<span data-ttu-id="cb8ec-110">In questa serie di esercitazioni viene compilata un'intera applicazione di gestione dei contatti dall'inizio alla fine.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-110">In this series of tutorials, we build an entire Contact Management application from start to finish.</span></span> <span data-ttu-id="cb8ec-111">L'applicazione Contact Manager consente di memorizzare le informazioni di contatto - nomi, numeri di telefono e indirizzi e-mail - per un elenco di persone.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-111">The Contact Manager application enables you to store contact information - names, phone numbers and email addresses - for a list of people.</span></span>

<span data-ttu-id="cb8ec-112">Costruiamo l'applicazione su più iterazioni.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-112">We build the application over multiple iterations.</span></span> <span data-ttu-id="cb8ec-113">Ad ogni iterazione, miglioriamo gradualmente l'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-113">With each iteration, we gradually improve the application.</span></span> <span data-ttu-id="cb8ec-114">L'obiettivo di questo approccio a più iterazioni è consentire di comprendere il motivo di ogni modifica.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-114">The goal of this multiple iteration approach is to enable you to understand the reason for each change.</span></span>

- <span data-ttu-id="cb8ec-115">Iterazione #1- Crea l'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-115">Iteration #1 - Create the application.</span></span> <span data-ttu-id="cb8ec-116">Nella prima iterazione, creiamo il Contact Manager nel modo più semplice possibile.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-116">In the first iteration, we create the Contact Manager in the simplest way possible.</span></span> <span data-ttu-id="cb8ec-117">Aggiungiamo il supporto per le operazioni di base del database: crea, leggi, aggiorna ed elimina (CRUD).</span><span class="sxs-lookup"><span data-stu-id="cb8ec-117">We add support for basic database operations: Create, Read, Update, and Delete (CRUD).</span></span>

- <span data-ttu-id="cb8ec-118">Iterazione #2 - Rendere l'applicazione un aspetto gradel.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-118">Iteration #2 - Make the application look nice.</span></span> <span data-ttu-id="cb8ec-119">In questa iterazione viene migliorato l'aspetto dell'applicazione modificando la pagina master della visualizzazione MVC ASP.NET predefinita e il foglio di stile CSS.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-119">In this iteration, we improve the appearance of the application by modifying the default ASP.NET MVC view master page and cascading style sheet.</span></span>

- <span data-ttu-id="cb8ec-120">#3 iterazione - Aggiungere la convalida del form.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-120">Iteration #3 - Add form validation.</span></span> <span data-ttu-id="cb8ec-121">Nella terza iterazione, aggiungiamo la convalida di base del form.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-121">In the third iteration, we add basic form validation.</span></span> <span data-ttu-id="cb8ec-122">Impediamo alle persone di inviare un modulo senza compilare i campi modulo obbligatori.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-122">We prevent people from submitting a form without completing required form fields.</span></span> <span data-ttu-id="cb8ec-123">Convalidiamo anche gli indirizzi e-mail e i numeri di telefono.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-123">We also validate email addresses and phone numbers.</span></span>

- <span data-ttu-id="cb8ec-124">Iterazione #4 - Rendere l'applicazione liberamente accoppiato.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-124">Iteration #4 - Make the application loosely coupled.</span></span> <span data-ttu-id="cb8ec-125">In questa quarta iterazione, sfruttiamo diversi modelli di progettazione software per semplificare la manutenzione e la modifica dell'applicazione Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-125">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="cb8ec-126">Ad esempio, viene eseguito il refactoring dell'applicazione per usare il modello Repository e il modello di inserimento delle dipendenze.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-126">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

- <span data-ttu-id="cb8ec-127">#5 di iterazione - Creare unit test.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-127">Iteration #5 - Create unit tests.</span></span> <span data-ttu-id="cb8ec-128">Nella quinta iterazione, seminiamo la nostra applicazione più facile da gestire e modificare aggiungendo unit test.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-128">In the fifth iteration, we make our application easier to maintain and modify by adding unit tests.</span></span> <span data-ttu-id="cb8ec-129">Ci prendiamo gioco delle classi del modello di dati e creiamo unit test per i controller e la logica di convalida.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-129">We mock our data model classes and build unit tests for our controllers and validation logic.</span></span>

- <span data-ttu-id="cb8ec-130">#6 di iterazione - Utilizzare lo sviluppo basato su test.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-130">Iteration #6 - Use test-driven development.</span></span> <span data-ttu-id="cb8ec-131">In questa sesta iterazione, aggiungiamo nuove funzionalità all'applicazione scrivendo prima gli unit test e scrivendo il codice sugli unit test.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-131">In this sixth iteration, we add new functionality to our application by writing unit tests first and writing code against the unit tests.</span></span> <span data-ttu-id="cb8ec-132">In questa iterazione vengono aggiunti gruppi di contatti.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-132">In this iteration, we add contact groups.</span></span>

- <span data-ttu-id="cb8ec-133">Iterazione #7- Aggiungere funzionalità Ajax.Iteration #7 - Add Ajax functionality.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-133">Iteration #7 - Add Ajax functionality.</span></span> <span data-ttu-id="cb8ec-134">Nella settima iterazione, miglioriamo la velocità di risposta e le prestazioni dell'applicazione aggiungendo il supporto per Ajax.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-134">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>

## <a name="this-iteration"></a><span data-ttu-id="cb8ec-135">Questa iterazione</span><span class="sxs-lookup"><span data-stu-id="cb8ec-135">This Iteration</span></span>

<span data-ttu-id="cb8ec-136">In questa quarta iterazione dell'applicazione Contact Manager, viene eseguito il refactoring dell'applicazione per rendere l'applicazione più liberamente accoppiata.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-136">In this fourth iteration of the Contact Manager application, we refactor the application to make the application more loosely coupled.</span></span> <span data-ttu-id="cb8ec-137">Quando un'applicazione è ad accoppiamento ridotto, è possibile modificare il codice in una parte dell'applicazione senza dover modificare il codice in altre parti dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-137">When an application is loosely coupled, you can modify the code in one part of the application without needing to modify the code in other parts of the application.</span></span> <span data-ttu-id="cb8ec-138">Le applicazioni ad accoppiamento ridotto sono più resilienti al cambiamento.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-138">Loosely coupled applications are more resilient to change.</span></span>

<span data-ttu-id="cb8ec-139">Attualmente, tutta la logica di convalida e accesso ai dati utilizzata dall'applicazione Contact Manager è contenuta nelle classi controller.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-139">Currently, all of the data access and validation logic used by the Contact Manager application is contained in the controller classes.</span></span> <span data-ttu-id="cb8ec-140">È una pessima idea.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-140">This is a bad idea.</span></span> <span data-ttu-id="cb8ec-141">Ogni volta che è necessario modificare una parte dell'applicazione, si rischia di introdurre bug in un'altra parte dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-141">Whenever you need to modify one part of your application, you risk introducing bugs into another part of your application.</span></span> <span data-ttu-id="cb8ec-142">Ad esempio, se si modifica la logica di convalida, si rischia di introdurre nuovi bug nell'accesso ai dati o nella logica del controller.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-142">For example, if you modify your validation logic, you risk introducing new bugs into your data access or controller logic.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="cb8ec-143">(SRP), una classe non dovrebbe mai avere più di un motivo per cambiare.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-143">(SRP), a class should never have more than one reason to change.</span></span> <span data-ttu-id="cb8ec-144">La combinazione di controller, convalida e logica di database è una violazione massiccia del principio di responsabilità singola.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-144">Mixing controller, validation, and database logic is a massive violation of the Single Responsibility Principle.</span></span>

<span data-ttu-id="cb8ec-145">Esistono diversi motivi per cui potrebbe essere necessario modificare l'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-145">There are several reasons that you might need to modify your application.</span></span> <span data-ttu-id="cb8ec-146">Potrebbe essere necessario aggiungere una nuova funzionalità all'applicazione, potrebbe essere necessario correggere un bug nell'applicazione o potrebbe essere necessario modificare la modalità di implementazione di una funzionalità dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-146">You might need to add a new feature to your application, you might need to fix a bug in your application, or you might need to modify how a feature of your application is implemented.</span></span> <span data-ttu-id="cb8ec-147">Le applicazioni sono raramente statiche.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-147">Applications are rarely static.</span></span> <span data-ttu-id="cb8ec-148">Tendono a crescere e mutare nel tempo.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-148">They tend to grow and mutate over time.</span></span>

<span data-ttu-id="cb8ec-149">Si supponga, ad esempio, di decidere di modificare la modalità di implementazione del livello di accesso ai dati.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-149">Imagine, for example, that you decide to change how you implement your data access layer.</span></span> <span data-ttu-id="cb8ec-150">A questo punto, l'applicazione Contact Manager utilizza Microsoft Entity Framework per accedere al database.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-150">Right now, the Contact Manager application uses the Microsoft Entity Framework to access the database.</span></span> <span data-ttu-id="cb8ec-151">Tuttavia, è possibile decidere di eseguire la migrazione a una tecnologia di accesso ai dati nuova o alternativa, ad esempio ADO.NET Data Services o NHibernate.However, you might decide to migrate to a new or alternative data access technology such as ADO.NET Data Services or NHibernate.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-151">However, you might decide to migrate to a new or alternative data access technology such as ADO.NET Data Services or NHibernate.</span></span> <span data-ttu-id="cb8ec-152">Tuttavia, poiché il codice di accesso ai dati non è isolato dal codice di convalida e dal controller, non è possibile modificare il codice di accesso ai dati nell'applicazione senza modificare altro codice non direttamente correlato all'accesso ai dati.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-152">However, because the data access code is not isolated from the validation and controller code, there is no way to modify the data access code in your application without modifying other code that is not directly related to data access.</span></span>

<span data-ttu-id="cb8ec-153">Quando un'applicazione è ad accoppiamento libero, d'altra parte, è possibile apportare modifiche a una parte di un'applicazione senza toccare altre parti di un'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-153">When an application is loosely coupled, on the other hand, you can make changes to one part of an application without touching other parts of an application.</span></span> <span data-ttu-id="cb8ec-154">Ad esempio, è possibile cambiare le tecnologie di accesso ai dati senza modificare la logica di convalida o controller.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-154">For example, you can switch data access technologies without modifying your validation or controller logic.</span></span>

<span data-ttu-id="cb8ec-155">In questa iterazione, sfruttiamo diversi modelli di progettazione software che consentono di eseguire il refactoring dell'applicazione Contact Manager in un'applicazione più liberamente accoppiata.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-155">In this iteration, we take advantage of several software design patterns that enable us to refactor our Contact Manager application into a more loosely coupled application.</span></span> <span data-ttu-id="cb8ec-156">Quando abbiamo finito, il Contact Manager non farà nulla che non ha fatto prima.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-156">When we are done, the Contact Manager won t do anything that it didn't do before.</span></span> <span data-ttu-id="cb8ec-157">Tuttavia, saremo in grado di modificare l'applicazione più facilmente in futuro.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-157">However, we'll be able to change the application more easily in the future.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="cb8ec-158">Il refactoring è il processo di riscrittura di un'applicazione in modo che non perda alcuna funzionalità esistente.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-158">Refactoring is the process of rewriting an application in such a way that it does not lose any existing functionality.</span></span>

## <a name="using-the-repository-software-design-pattern"></a><span data-ttu-id="cb8ec-159">Utilizzo del modello di progettazione del software del repository</span><span class="sxs-lookup"><span data-stu-id="cb8ec-159">Using the Repository Software Design Pattern</span></span>

<span data-ttu-id="cb8ec-160">La nostra prima modifica consiste nell'utilizzare un modello di progettazione software chiamato modello Repository.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-160">Our first change is to take advantage of a software design pattern called the Repository pattern.</span></span> <span data-ttu-id="cb8ec-161">Utilizzeremo il modello Repository per isolare il codice di accesso ai dati dal resto dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-161">We'll use the Repository pattern to isolate our data access code from the rest of our application.</span></span>

<span data-ttu-id="cb8ec-162">L'implementazione del modello Repository richiede di completare i due passaggi seguenti:Implementing the Repository pattern requires us to complete the following two steps:</span><span class="sxs-lookup"><span data-stu-id="cb8ec-162">Implementing the Repository pattern requires us to complete the following two steps:</span></span>

1. <span data-ttu-id="cb8ec-163">Creare un'interfaccia</span><span class="sxs-lookup"><span data-stu-id="cb8ec-163">Create an interface</span></span>
2. <span data-ttu-id="cb8ec-164">Creare una classe concreta che implementa l'interfacciaCreate a concrete class that implements the interface</span><span class="sxs-lookup"><span data-stu-id="cb8ec-164">Create a concrete class that implements the interface</span></span>

<span data-ttu-id="cb8ec-165">In primo luogo, è necessario creare un'interfaccia che descrive tutti i metodi di accesso ai dati che è necessario eseguire.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-165">First, we need to create an interface that describes all of the data access methods that we need to perform.</span></span> <span data-ttu-id="cb8ec-166">Il IContactManagerRepository interfaccia è contenuta nel listato 1.The IContactManagerRepository interface is contained in Listing 1.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-166">The IContactManagerRepository interface is contained in Listing 1.</span></span> <span data-ttu-id="cb8ec-167">Questa interfaccia descrive cinque metodi: CreateContact(), DeleteContact(), EditContact(), GetContact e ListContacts().</span><span class="sxs-lookup"><span data-stu-id="cb8ec-167">This interface describes five methods: CreateContact(), DeleteContact(), EditContact(), GetContact, and ListContacts().</span></span>

<span data-ttu-id="cb8ec-168">**Listato 1 - Modelli: IContactManagerRepository.vb**</span><span class="sxs-lookup"><span data-stu-id="cb8ec-168">**Listing 1 - Models\IContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample1.vb)]

<span data-ttu-id="cb8ec-169">Successivamente, è necessario creare una classe concreta che implementa il IContactManagerRepository interfaccia.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-169">Next, we need to create a concrete class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="cb8ec-170">Poiché si utilizza Microsoft Entity Framework per accedere al database, verrà creata una nuova classe denominata EntityContactManagerRepository.Because we are using the Microsoft Entity Framework to access the database, we'll create a new class named EntityContactManagerRepository.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-170">Because we are using the Microsoft Entity Framework to access the database, we'll create a new class named EntityContactManagerRepository.</span></span> <span data-ttu-id="cb8ec-171">Questa classe è contenuta nel listato 2.This class is contained in Listing 2.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-171">This class is contained in Listing 2.</span></span>

<span data-ttu-id="cb8ec-172">**Listato 2 - Modelli, EntityContactManagerRepository.vb**</span><span class="sxs-lookup"><span data-stu-id="cb8ec-172">**Listing 2 - Models\EntityContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample2.vb)]

<span data-ttu-id="cb8ec-173">Si noti che la classe EntityContactManagerRepository implementa l'interfaccia IContactManagerRepository.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-173">Notice that the EntityContactManagerRepository class implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="cb8ec-174">La classe implementa tutti e cinque i metodi descritti da tale interfaccia.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-174">The class implements all five of the methods described by that interface.</span></span>

<span data-ttu-id="cb8ec-175">Ci si potrebbe chiedere perché abbiamo bisogno di preoccuparsi con un'interfaccia.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-175">You might wonder why we need to bother with an interface.</span></span> <span data-ttu-id="cb8ec-176">Perché è necessario creare sia un'interfaccia che una classe che la implementa?</span><span class="sxs-lookup"><span data-stu-id="cb8ec-176">Why do we need to create both an interface and a class that implements it?</span></span>

<span data-ttu-id="cb8ec-177">Con un'eccezione, il resto dell'applicazione interagirà con l'interfaccia e non con la classe concreta.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-177">With one exception, the remainder of our application will interact with the interface and not the concrete class.</span></span> <span data-ttu-id="cb8ec-178">Invece di chiamare i metodi esposti dalla classe EntityContactManagerRepository, chiameremo i metodi esposti dall'interfaccia IContactManagerRepository.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-178">Instead of calling the methods exposed by the EntityContactManagerRepository class, we'll call the methods exposed by the IContactManagerRepository interface.</span></span>

<span data-ttu-id="cb8ec-179">In questo modo, è possibile implementare l'interfaccia con una nuova classe senza dover modificare il resto dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-179">That way, we can implement the interface with a new class without needing to modify the remainder of our application.</span></span> <span data-ttu-id="cb8ec-180">Ad esempio, in una data futura, è possibile implementare un DataServicesContactManagerRepository classe che implementa il IContactManagerRepository interfaccia.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-180">For example, at some future date, we might want to implement an DataServicesContactManagerRepository class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="cb8ec-181">La classe DataServicesContactManagerRepository potrebbe utilizzare ADO.NET Data Services per accedere a un database anziché a Microsoft Entity Framework.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-181">The DataServicesContactManagerRepository class might use ADO.NET Data Services to access a database instead of the Microsoft Entity Framework.</span></span>

<span data-ttu-id="cb8ec-182">Se il codice dell'applicazione viene programmato in base all'interfaccia IContactManagerRepository anziché alla classe EntityContactManagerRepository concreta, è possibile passare da classi concrete senza modificare il resto del codice.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-182">If our application code is programmed against the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class then we can switch concrete classes without modifying any of the rest of our code.</span></span> <span data-ttu-id="cb8ec-183">Ad esempio, è possibile passare dalla classe EntityContactManagerRepository alla classe DataServicesContactManagerRepository senza modificare la logica di accesso ai dati o di convalida.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-183">For example, we can switch from the EntityContactManagerRepository class to the DataServicesContactManagerRepository class without modifying our data access or validation logic.</span></span>

<span data-ttu-id="cb8ec-184">La programmazione in base alle interfacce (astrazione) anziché alle classi concrete rende l'applicazione più resiliente alle modifiche.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-184">Programming against interfaces (abstractions) instead of concrete classes makes our application more resilient to change.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="cb8ec-185">È possibile creare rapidamente un'interfaccia da una classe concreta all'interno di Visual Studio selezionando l'opzione di menu Effettua refactoring, Estrai interfaccia.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-185">You can quickly create an interface from a concrete class within Visual Studio by selecting the menu option Refactor, Extract Interface.</span></span> <span data-ttu-id="cb8ec-186">Ad esempio, è possibile creare prima la classe EntityContactManagerRepository e quindi utilizzare Estrai interfaccia per generare automaticamente l'interfaccia IContactManagerRepository.For example, you can create the EntityContactManagerRepository class first and then use Extract Interface to generate the IContactManagerRepository interface automatically.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-186">For example, you can create the EntityContactManagerRepository class first and then use Extract Interface to generate the IContactManagerRepository interface automatically.</span></span>

## <a name="using-the-dependency-injection-software-design-pattern"></a><span data-ttu-id="cb8ec-187">Utilizzo del modello di progettazione software di inserimento delle dipendenzeUsing the Dependency Injection Software Design Pattern</span><span class="sxs-lookup"><span data-stu-id="cb8ec-187">Using the Dependency Injection Software Design Pattern</span></span>

<span data-ttu-id="cb8ec-188">Ora che è stata eseguita la migrazione del codice di accesso ai dati in una classe Repository separata, è necessario modificare il controller di contatto per utilizzare questa classe.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-188">Now that we have migrated our data access code to a separate Repository class, we need to modify our Contact controller to use this class.</span></span> <span data-ttu-id="cb8ec-189">Si sfruttuorà di un modello di progettazione software chiamato Dependency Injection per utilizzare la classe Repository nel controller.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-189">We will take advantage of a software design pattern called Dependency Injection to use the Repository class in our controller.</span></span>

<span data-ttu-id="cb8ec-190">Il controller di contatto modificato è contenuto nel listato 3.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-190">The modified Contact controller is contained in Listing 3.</span></span>

<span data-ttu-id="cb8ec-191">**Listato 3 - Controllers-ContactController.vb**</span><span class="sxs-lookup"><span data-stu-id="cb8ec-191">**Listing 3 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample3.vb)]

<span data-ttu-id="cb8ec-192">Si noti che il controller di contatto nel listato 3 ha due costruttori.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-192">Notice that the Contact controller in Listing 3 has two constructors.</span></span> <span data-ttu-id="cb8ec-193">Il primo costruttore passa un'istanza concreta dell'interfaccia IContactManagerRepository al secondo costruttore.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-193">The first constructor passes a concrete instance of the IContactManagerRepository interface to the second constructor.</span></span> <span data-ttu-id="cb8ec-194">La classe del controller di contatto utilizza *l'inserimento di dipendenze del costruttore*.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-194">The Contact controller class uses *Constructor Dependency Injection*.</span></span>

<span data-ttu-id="cb8ec-195">L'unica e unica posizione in cui viene utilizzata la classe EntityContactManagerRepository è nel primo costruttore.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-195">The one and only place that the EntityContactManagerRepository class is used is in the first constructor.</span></span> <span data-ttu-id="cb8ec-196">Il resto della classe utilizza l'interfaccia IContactManagerRepository anziché la classe EntityContactManagerRepository concreta.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-196">The remainder of the class uses the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class.</span></span>

<span data-ttu-id="cb8ec-197">In questo modo è più semplice cambiare implementazioni della classe IContactManagerRepository in futuro.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-197">This makes it easy to switch implementations of the IContactManagerRepository class in the future.</span></span> <span data-ttu-id="cb8ec-198">Se si desidera utilizzare la classe DataServicesContactRepository anziché la classe EntityContactManagerRepository, è sufficiente modificare il primo costruttore.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-198">If you want to use the DataServicesContactRepository class instead of the EntityContactManagerRepository class, just modify the first constructor.</span></span>

<span data-ttu-id="cb8ec-199">L'inserimento di dipendenze del costruttore rende inoltre la classe controller Contact molto tepossibile.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-199">Constructor Dependency injection also makes the Contact controller class very testable.</span></span> <span data-ttu-id="cb8ec-200">Negli unit test è possibile creare un'istanza del controller di contatto passando un'implementazione fittizia della classe IContactManagerRepository.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-200">In your unit tests, you can instantiate the Contact controller by passing a mock implementation of the IContactManagerRepository class.</span></span> <span data-ttu-id="cb8ec-201">Questa funzionalità di Dependency Injection sarà molto importante per noi nella prossima iterazione quando si compila unit test per l'applicazione Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-201">This feature of Dependency Injection will be very important to us in the next iteration when we build unit tests for the Contact Manager application.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="cb8ec-202">Se si desidera separare completamente la classe controller di contatto da una particolare implementazione dell'interfaccia IContactManagerRepository, è possibile sfruttare un framework che supporta l'inserimento delle dipendenze, ad esempio StructureMap o Microsoft Entity Framework (MEF).</span><span class="sxs-lookup"><span data-stu-id="cb8ec-202">If you want to completely decouple the Contact controller class from a particular implementation of the IContactManagerRepository interface then you can take advantage of a framework that supports Dependency Injection such as StructureMap or the Microsoft Entity Framework (MEF).</span></span> <span data-ttu-id="cb8ec-203">Sfruttando un framework di inserimento delle dipendenze, non è mai necessario fare riferimento a una classe concreta nel codice.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-203">By taking advantage of a Dependency Injection framework, you never need to refer to a concrete class in your code.</span></span>

## <a name="creating-a-service-layer"></a><span data-ttu-id="cb8ec-204">Creazione di un livello di servizioCreating a Service Layer</span><span class="sxs-lookup"><span data-stu-id="cb8ec-204">Creating a Service Layer</span></span>

<span data-ttu-id="cb8ec-205">Potresti aver notato che la logica di convalida è ancora mescolata con la logica del controller nella classe controller modificata nel listato 3.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-205">You might have noticed that our validation logic is still mixed up with our controller logic in the modified controller class in Listing 3.</span></span> <span data-ttu-id="cb8ec-206">Per lo stesso motivo per cui è consigliabile isolare la logica di accesso ai dati, è consigliabile isolare la logica di convalida.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-206">For the same reason that it is a good idea to isolate our data access logic, it is a good idea to isolate our validation logic.</span></span>

<span data-ttu-id="cb8ec-207">Per risolvere questo problema, è possibile creare un livello di [servizio](http://martinfowler.com/eaaCatalog/serviceLayer.html)separato .</span><span class="sxs-lookup"><span data-stu-id="cb8ec-207">To fix this problem, we can create a separate [service layer](http://martinfowler.com/eaaCatalog/serviceLayer.html).</span></span> <span data-ttu-id="cb8ec-208">Il livello di servizio è un livello separato che è possibile inserire tra le classi di controller e repository.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-208">The service layer is a separate layer that we can insert between our controller and repository classes.</span></span> <span data-ttu-id="cb8ec-209">Il livello di servizio contiene la logica di business, inclusa tutta la logica di convalida.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-209">The service layer contains our business logic including all of our validation logic.</span></span>

<span data-ttu-id="cb8ec-210">ContactManagerService è contenuto nel listato 4.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-210">The ContactManagerService is contained in Listing 4.</span></span> <span data-ttu-id="cb8ec-211">Contiene la logica di convalida dalla classe del controller di contatto.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-211">It contains the validation logic from the Contact controller class.</span></span>

<span data-ttu-id="cb8ec-212">**Listato 4 - Modelli, ContactManagerService.vb**</span><span class="sxs-lookup"><span data-stu-id="cb8ec-212">**Listing 4 - Models\ContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample4.vb)]

<span data-ttu-id="cb8ec-213">Si noti che il costruttore per il ContactManagerService richiede un ValidationDictionary.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-213">Notice that the constructor for the ContactManagerService requires a ValidationDictionary.</span></span> <span data-ttu-id="cb8ec-214">The service layer communicates with the controller layer through this ValidationDictionary.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-214">The service layer communicates with the controller layer through this ValidationDictionary.</span></span> <span data-ttu-id="cb8ec-215">Discutereil ValidationDictionary in dettaglio nella sezione seguente quando si discute il modello Decorator.We discuss the ValidationDictionary in detail in the following section when we discuss the Decorator pattern.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-215">We discuss the ValidationDictionary in detail in the following section when we discuss the Decorator pattern.</span></span>

<span data-ttu-id="cb8ec-216">Si noti, inoltre, che il ContactManagerService implementa il IContactManagerService interfaccia.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-216">Notice, furthermore, that the ContactManagerService implements the IContactManagerService interface.</span></span> <span data-ttu-id="cb8ec-217">Si dovrebbe sempre cercare di programmare contro le interfacce invece di classi concrete.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-217">You should always strive to program against interfaces instead of concrete classes.</span></span> <span data-ttu-id="cb8ec-218">Altre classi nell'applicazione Contact Manager non interagiscono direttamente con la classe ContactManagerService.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-218">Other classes in the Contact Manager application do not interact with the ContactManagerService class directly.</span></span> <span data-ttu-id="cb8ec-219">Al contrario, con un'eccezione, il resto dell'applicazione Contact Manager viene programmato in base all'interfaccia IContactManagerService.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-219">Instead, with one exception, the remainder of the Contact Manager application is programmed against the IContactManagerService interface.</span></span>

<span data-ttu-id="cb8ec-220">L'interfaccia IContactManagerService è contenuta nel listato 5.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-220">The IContactManagerService interface is contained in Listing 5.</span></span>

<span data-ttu-id="cb8ec-221">**Listato 5 - Modelli: IContactManagerService.vb**</span><span class="sxs-lookup"><span data-stu-id="cb8ec-221">**Listing 5 - Models\IContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample5.vb)]

<span data-ttu-id="cb8ec-222">La classe controller Contact modificata è contenuta nel listato 6.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-222">The modified Contact controller class is contained in Listing 6.</span></span> <span data-ttu-id="cb8ec-223">Si noti che il controller di contatto non interagisce più con il repository ContactManager.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-223">Notice that the Contact controller no longer interacts with the ContactManager repository.</span></span> <span data-ttu-id="cb8ec-224">Al contrario, il controller di contatto interagisce con il servizio ContactManager.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-224">Instead, the Contact controller interacts with the ContactManager service.</span></span> <span data-ttu-id="cb8ec-225">Ogni livello è isolato il più possibile dagli altri livelli.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-225">Each layer is isolated as much as possible from other layers.</span></span>

<span data-ttu-id="cb8ec-226">**Listato 6 - Controllers-ContactController.vb**</span><span class="sxs-lookup"><span data-stu-id="cb8ec-226">**Listing 6 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample6.vb)]

<span data-ttu-id="cb8ec-227">La nostra applicazione non è più in contrasto con il Principio di Responsabilità Singola (SRP).</span><span class="sxs-lookup"><span data-stu-id="cb8ec-227">Our application no longer runs afoul of the Single Responsibility Principle (SRP).</span></span> <span data-ttu-id="cb8ec-228">Il controller di contatto nel listato 6 è stato rimosso da ogni responsabilità diversa dal controllo del flusso di esecuzione dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-228">The Contact controller in Listing 6 has been stripped of every responsibility other than controlling the flow of application execution.</span></span> <span data-ttu-id="cb8ec-229">Tutta la logica di convalida è stata rimossa dal controller di contatto e inserita nel livello di servizio.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-229">All the validation logic has been removed from the Contact controller and pushed into the service layer.</span></span> <span data-ttu-id="cb8ec-230">Tutta la logica del database è stata inserita nel livello del repository.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-230">All of the database logic has been pushed into the repository layer.</span></span>

## <a name="using-the-decorator-pattern"></a><span data-ttu-id="cb8ec-231">Utilizzo del modello DecoratorUsing the Decorator Pattern</span><span class="sxs-lookup"><span data-stu-id="cb8ec-231">Using the Decorator Pattern</span></span>

<span data-ttu-id="cb8ec-232">Vogliamo essere in grado di disaccoppiare completamente il nostro livello di servizio dal nostro livello di controller.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-232">We want to be able to completely decouple our service layer from our controller layer.</span></span> <span data-ttu-id="cb8ec-233">In linea di principio, dovremmo essere in grado di compilare il livello di servizio in un assembly separato dal livello del controller senza dover aggiungere un riferimento all'applicazione MVC.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-233">In principle, we should be able to compile our service layer in a separate assembly from our controller layer without needing to add a reference to our MVC application.</span></span>

<span data-ttu-id="cb8ec-234">Tuttavia, il livello di servizio deve essere in grado di passare i messaggi di errore di convalida al livello del controller.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-234">However, our service layer needs to be able to pass validation error messages back to the controller layer.</span></span> <span data-ttu-id="cb8ec-235">Come possiamo consentire al livello di servizio di comunicare messaggi di errore di convalida senza accantare il controller e il livello di servizio?</span><span class="sxs-lookup"><span data-stu-id="cb8ec-235">How can we enable the service layer to communicate validation error messages without coupling the controller and service layer?</span></span> <span data-ttu-id="cb8ec-236">È possibile sfruttare un modello di progettazione software denominato [modello Decorator](http://en.wikipedia.org/wiki/Decorator_pattern).</span><span class="sxs-lookup"><span data-stu-id="cb8ec-236">We can take advantage of a software design pattern named the [Decorator pattern](http://en.wikipedia.org/wiki/Decorator_pattern).</span></span>

<span data-ttu-id="cb8ec-237">Un controller utilizza un ModelStateDictionary denominato ModelState per rappresentare gli errori di convalida.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-237">A controller uses a ModelStateDictionary named ModelState to represent validation errors.</span></span> <span data-ttu-id="cb8ec-238">Pertanto, si potrebbe essere tentati di passare ModelState dal livello di controller al livello di servizio.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-238">Therefore, you might be tempted to pass ModelState from the controller layer to the service layer.</span></span> <span data-ttu-id="cb8ec-239">Tuttavia, l'utilizzo di ModelState nel livello di servizio renderebbe il livello di servizio dipendente da una funzionalità del framework MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-239">However, using ModelState in the service layer would make your service layer dependent on a feature of the ASP.NET MVC framework.</span></span> <span data-ttu-id="cb8ec-240">Questo sarebbe male perché, un giorno, si potrebbe desiderare di utilizzare il livello di servizio con un'applicazione WPF anziché un'applicazione MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-240">This would be bad because, someday, you might want to use the service layer with a WPF application instead of an ASP.NET MVC application.</span></span> <span data-ttu-id="cb8ec-241">In tal caso, non si desidera fare riferimento al framework MVC ASP.NET per utilizzare la classe ModelStateDictionary.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-241">In that case, you wouldn't want to reference the ASP.NET MVC framework to use the ModelStateDictionary class.</span></span>

<span data-ttu-id="cb8ec-242">Il modello Decorator consente di eseguire il wrapping di una classe esistente in una nuova classe per implementare un'interfaccia.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-242">The Decorator pattern enables you to wrap an existing class in a new class in order to implement an interface.</span></span> <span data-ttu-id="cb8ec-243">Il progetto Contact Manager include la classe ModelStateWrapper contenuta nel listato 7.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-243">Our Contact Manager project includes the ModelStateWrapper class contained in Listing 7.</span></span> <span data-ttu-id="cb8ec-244">La classe ModelStateWrapper implementa l'interfaccia nel listato 8.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-244">The ModelStateWrapper class implements the interface in Listing 8.</span></span>

<span data-ttu-id="cb8ec-245">**Listato 7 - Modelli, Convalida, ModelStateWrapper.vb**</span><span class="sxs-lookup"><span data-stu-id="cb8ec-245">**Listing 7 - Models\Validation\ModelStateWrapper.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample7.vb)]

<span data-ttu-id="cb8ec-246">**Listato 8 - Modelli, Convalida, IValidationDictionary.vb**</span><span class="sxs-lookup"><span data-stu-id="cb8ec-246">**Listing 8 - Models\Validation\IValidationDictionary.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample8.vb)]

<span data-ttu-id="cb8ec-247">Se si esamina da vicino il listato 5, si noterà che il livello di servizio ContactManager utilizza esclusivamente l'interfaccia IValidationDictionary.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-247">If you take a close look at Listing 5 then you'll see that the ContactManager service layer uses the IValidationDictionary interface exclusively.</span></span> <span data-ttu-id="cb8ec-248">Il servizio ContactManager non dipende dalla classe ModelStateDictionary.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-248">The ContactManager service is not dependent on the ModelStateDictionary class.</span></span> <span data-ttu-id="cb8ec-249">Quando il controller di contatto crea il servizio ContactManager, il controller esegue il wrapping di ModelState in questo modo:When the Contact controller creates the ContactManager service, the controller wraps its ModelState like this:</span><span class="sxs-lookup"><span data-stu-id="cb8ec-249">When the Contact controller creates the ContactManager service, the controller wraps its ModelState like this:</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample9.vb)]

## <a name="summary"></a><span data-ttu-id="cb8ec-250">Riepilogo</span><span class="sxs-lookup"><span data-stu-id="cb8ec-250">Summary</span></span>

<span data-ttu-id="cb8ec-251">In questa iterazione, non sono stati aggiunte nuove funzionalità all'applicazione Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-251">In this iteration, we did not add any new functionality to the Contact Manager application.</span></span> <span data-ttu-id="cb8ec-252">L'obiettivo di questa iterazione era di eseguire il refactoring dell'applicazione Contact Manager in modo che sia più facile da gestire e modificare.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-252">The goal of this iteration was to refactor the Contact Manager application so that is easier to maintain and modify.</span></span>

<span data-ttu-id="cb8ec-253">In primo luogo, abbiamo implementato il modello di progettazione del software Repository.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-253">First, we implemented the Repository software design pattern.</span></span> <span data-ttu-id="cb8ec-254">È stata eseguita la migrazione di tutto il codice di accesso ai dati in una classe di repository ContactManager separata.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-254">We migrated all of the data access code to a separate ContactManager repository class.</span></span>

<span data-ttu-id="cb8ec-255">Abbiamo anche isolato la logica di convalida dalla logica del controller.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-255">We also isolated our validation logic from our controller logic.</span></span> <span data-ttu-id="cb8ec-256">È stato creato un livello di servizio separato che contiene tutto il codice di convalida.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-256">We created a separate service layer that contains all of our validation code.</span></span> <span data-ttu-id="cb8ec-257">Il livello controller interagisce con il livello di servizio e il livello di servizio interagisce con il livello del repository.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-257">The controller layer interacts with the service layer, and the service layer interacts with the repository layer.</span></span>

<span data-ttu-id="cb8ec-258">Quando abbiamo creato il livello di servizio, abbiamo sfruttato il modello Decorator per isolare ModelState dal livello di servizio.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-258">When we created the service layer, we took advantage of the Decorator pattern to isolate ModelState from our service layer.</span></span> <span data-ttu-id="cb8ec-259">Nel nostro livello di servizio, abbiamo programmato in base all'interfaccia IValidationDictionary anziché ModelState.In our service layer, we programd against the IValidationDictionary interface instead of ModelState.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-259">In our service layer, we programmed against the IValidationDictionary interface instead of ModelState.</span></span>

<span data-ttu-id="cb8ec-260">Infine, abbiamo sfruttato un modello di progettazione software denominato modello di inserimento delle dipendenze.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-260">Finally, we took advantage of a software design pattern named the Dependency Injection pattern.</span></span> <span data-ttu-id="cb8ec-261">Questo modello consente di eseguire programmi in base alle interfacce (astrazione) anziché alle classi concrete.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-261">This pattern enables us to program against interfaces (abstractions) instead of concrete classes.</span></span> <span data-ttu-id="cb8ec-262">L'implementazione del modello di progettazione Dependency Injection rende inoltre il codice più tepotebile.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-262">Implementing the Dependency Injection design pattern also makes our code more testable.</span></span> <span data-ttu-id="cb8ec-263">Nell'iterazione successiva, aggiungiamo unit test al progetto.</span><span class="sxs-lookup"><span data-stu-id="cb8ec-263">In the next iteration, we add unit tests to our project.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="cb8ec-264">[Successivo](iteration-3-add-form-validation-vb.md)
> [precedente](iteration-5-create-unit-tests-vb.md)</span><span class="sxs-lookup"><span data-stu-id="cb8ec-264">[Previous](iteration-3-add-form-validation-vb.md)
[Next](iteration-5-create-unit-tests-vb.md)</span></span>
