---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
title: Informazioni sui filtri delle azioni (VB) Documenti Microsoft
author: rick-anderson
description: L'obiettivo di questa esercitazione è spiegare i filtri azione. Un filtro azione è un attributo che è possibile applicare a un'azione del controller, o a un intero controller...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: e83812f2-c53e-4a43-a7c1-d64c59ecf694
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
msc.type: authoredcontent
ms.openlocfilehash: 70ab564bc1217f67874090d2ae6899d9bcd743a1
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542105"
---
# <a name="understanding-action-filters-vb"></a><span data-ttu-id="a7abf-104">Informazioni sui filtri per azioni (VB)</span><span class="sxs-lookup"><span data-stu-id="a7abf-104">Understanding Action Filters (VB)</span></span>

<span data-ttu-id="a7abf-105">da parte [di Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="a7abf-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="a7abf-106">Scarica il PDF</span><span class="sxs-lookup"><span data-stu-id="a7abf-106">Download PDF</span></span>](https://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_VB.pdf)

> <span data-ttu-id="a7abf-107">L'obiettivo di questa esercitazione è spiegare i filtri azione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="a7abf-108">Un filtro azione è un attributo che è possibile applicare a un'azione del controller, o a un intero controller, che modifica il modo in cui viene eseguita l'azione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="a7abf-109">Informazioni sui filtri azione</span><span class="sxs-lookup"><span data-stu-id="a7abf-109">Understanding Action Filters</span></span>

<span data-ttu-id="a7abf-110">L'obiettivo di questa esercitazione è spiegare i filtri azione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="a7abf-111">Un filtro azione è un attributo che è possibile applicare a un'azione del controller, o a un intero controller, che modifica il modo in cui viene eseguita l'azione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="a7abf-112">Il framework di MVC ASP.NET include diversi filtri azione:</span><span class="sxs-lookup"><span data-stu-id="a7abf-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="a7abf-113">OutputCache: questo filtro azione memorizza nella cache l'output di un'azione del controller per un periodo di tempo specificato.</span><span class="sxs-lookup"><span data-stu-id="a7abf-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="a7abf-114">HandleError: questo filtro azione gestisce gli errori generati quando viene eseguita un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="a7abf-115">Autorizza: questo filtro azioni consente di limitare l'accesso a un determinato utente o ruolo.</span><span class="sxs-lookup"><span data-stu-id="a7abf-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="a7abf-116">È inoltre possibile creare filtri azione personalizzati.</span><span class="sxs-lookup"><span data-stu-id="a7abf-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="a7abf-117">Ad esempio, è possibile creare un filtro azione personalizzato per implementare un sistema di autenticazione personalizzato.</span><span class="sxs-lookup"><span data-stu-id="a7abf-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="a7abf-118">In alternativa, è possibile creare un filtro azione che modifica i dati di visualizzazione restituiti da un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="a7abf-119">In questa esercitazione imparerai a creare un filtro d'azione da zero.</span><span class="sxs-lookup"><span data-stu-id="a7abf-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="a7abf-120">Viene creato un filtro azione log che registra diverse fasi dell'elaborazione di un'azione nella finestra Output di Visual Studio.We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span><span class="sxs-lookup"><span data-stu-id="a7abf-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="a7abf-121">Utilizzo di un filtro azioni</span><span class="sxs-lookup"><span data-stu-id="a7abf-121">Using an Action Filter</span></span>

<span data-ttu-id="a7abf-122">Un filtro azione è un attributo.</span><span class="sxs-lookup"><span data-stu-id="a7abf-122">An action filter is an attribute.</span></span> <span data-ttu-id="a7abf-123">È possibile applicare la maggior parte dei filtri azione a una singola azione del controller o a un intero controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="a7abf-124">Ad esempio, il controller di dati nel `Index()` listato 1 espone un'azione denominata che restituisce l'ora corrente.</span><span class="sxs-lookup"><span data-stu-id="a7abf-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="a7abf-125">Questa azione è `OutputCache` decorata con il filtro azione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="a7abf-126">Questo filtro fa sì che il valore restituito dall'azione venga memorizzato nella cache per 10 secondi.</span><span class="sxs-lookup"><span data-stu-id="a7abf-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="a7abf-127">**Lista torto 1 –`Controllers\DataController.vb`**</span><span class="sxs-lookup"><span data-stu-id="a7abf-127">**Listing 1 – `Controllers\DataController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample1.vb)]

<span data-ttu-id="a7abf-128">Se si richiama `Index()` ripetutamente l'azione immettendo l'URL /Data/Index nella barra degli indirizzi del browser e premendo il pulsante Aggiorna più volte, verrà visualizzato lo stesso tempo per 10 secondi.</span><span class="sxs-lookup"><span data-stu-id="a7abf-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="a7abf-129">L'output `Index()` dell'azione viene memorizzato nella cache per 10 secondi (vedere Figura 1).</span><span class="sxs-lookup"><span data-stu-id="a7abf-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="a7abf-130">[![Tempo memorizzato nella cache](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="a7abf-130">[![Cached time](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span></span>

<span data-ttu-id="a7abf-131">**Figura 01**: Tempo memorizzato nella cache ([Fare clic per visualizzare l'immagine a dimensioni intere](understanding-action-filters-vb/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="a7abf-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-vb/_static/image3.png))</span></span>

<span data-ttu-id="a7abf-132">Nel listato 1, al `OutputCache` `Index()` metodo viene applicato un singolo filtro azione, ovvero il filtro azione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="a7abf-133">Se necessario, è possibile applicare più filtri azione alla stessa azione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="a7abf-134">Ad esempio, è possibile applicare `OutputCache` `HandleError` entrambi i filtri di azione e alla stessa azione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="a7abf-135">Nel listato `OutputCache` 1, il filtro `Index()` azione viene applicato all'azione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="a7abf-136">È anche possibile applicare `DataController` questo attributo alla classe stessa.</span><span class="sxs-lookup"><span data-stu-id="a7abf-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="a7abf-137">In tal caso, il risultato restituito da qualsiasi azione esposta dal controller verrà memorizzato nella cache per 10 secondi.</span><span class="sxs-lookup"><span data-stu-id="a7abf-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="a7abf-138">I diversi tipi di filtri</span><span class="sxs-lookup"><span data-stu-id="a7abf-138">The Different Types of Filters</span></span>

<span data-ttu-id="a7abf-139">Il framework di ASP.NET MVC supporta quattro diversi tipi di filtri:</span><span class="sxs-lookup"><span data-stu-id="a7abf-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="a7abf-140">Filtri di autorizzazione: implementa l'attributo. `IAuthorizationFilter`</span><span class="sxs-lookup"><span data-stu-id="a7abf-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="a7abf-141">Filtri azione: `IActionFilter` implementa l'attributo.</span><span class="sxs-lookup"><span data-stu-id="a7abf-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="a7abf-142">Filtri dei risultati `IResultFilter` : Implementa l'attributo.</span><span class="sxs-lookup"><span data-stu-id="a7abf-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="a7abf-143">Filtri eccezioni: `IExceptionFilter` implementa l'attributo .</span><span class="sxs-lookup"><span data-stu-id="a7abf-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="a7abf-144">I filtri vengono eseguiti nell'ordine sopra elencato.</span><span class="sxs-lookup"><span data-stu-id="a7abf-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="a7abf-145">Ad esempio, i filtri di autorizzazione vengono sempre eseguiti prima dei filtri azione e i filtri eccezioni vengono sempre eseguiti dopo ogni altro tipo di filtro.</span><span class="sxs-lookup"><span data-stu-id="a7abf-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="a7abf-146">I filtri di autorizzazione vengono utilizzati per implementare l'autenticazione e l'autorizzazione per le azioni del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="a7abf-147">Ad esempio, il filtro Autorizza è un esempio di filtro Autorizzazione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="a7abf-148">I filtri azione contengono la logica che viene eseguita prima e dopo l'esecuzione di un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="a7abf-149">È possibile utilizzare un filtro azioni, ad esempio, per modificare i dati di visualizzazione restituiti da un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="a7abf-150">I filtri dei risultati contengono la logica che viene eseguita prima e dopo l'esecuzione di un risultato della visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="a7abf-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="a7abf-151">Ad esempio, è possibile modificare il risultato di una vista subito prima che venga eseguito il rendering della vista nel browser.</span><span class="sxs-lookup"><span data-stu-id="a7abf-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="a7abf-152">I filtri eccezioni sono l'ultimo tipo di filtro da eseguire.</span><span class="sxs-lookup"><span data-stu-id="a7abf-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="a7abf-153">È possibile usare un filtro eccezioni per gestire gli errori generati dalle azioni del controller o dai risultati dell'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="a7abf-154">È inoltre possibile utilizzare i filtri eccezioni per registrare gli errori.</span><span class="sxs-lookup"><span data-stu-id="a7abf-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="a7abf-155">Ogni tipo diverso di filtro viene eseguito in un ordine particolare.</span><span class="sxs-lookup"><span data-stu-id="a7abf-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="a7abf-156">Se si desidera controllare l'ordine di esecuzione dei filtri dello stesso tipo, è possibile impostare la proprietà Order di un filtro.</span><span class="sxs-lookup"><span data-stu-id="a7abf-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="a7abf-157">La classe base per tutti `System.Web.Mvc.FilterAttribute` i filtri azione è la classe.</span><span class="sxs-lookup"><span data-stu-id="a7abf-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="a7abf-158">Se si desidera implementare un particolare tipo di filtro, è necessario creare una classe che eredita dalla classe Filter di base e implementa una o più delle interfacce IAuthorizationFilter, IActionFilter, IResultFilter o ExceptionFilter.</span><span class="sxs-lookup"><span data-stu-id="a7abf-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the IAuthorizationFilter, IActionFilter, IResultFilter, or ExceptionFilter interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="a7abf-159">La classe Base ActionFilterAttribute</span><span class="sxs-lookup"><span data-stu-id="a7abf-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="a7abf-160">Per semplificare l'implementazione di un filtro azione personalizzato, il `ActionFilterAttribute` framework MVC ASP.NET include una classe base.</span><span class="sxs-lookup"><span data-stu-id="a7abf-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="a7abf-161">Questa classe implementa `IActionFilter` `IResultFilter` le interfacce e `Filter` e dalla classe .</span><span class="sxs-lookup"><span data-stu-id="a7abf-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="a7abf-162">La terminologia qui non è del tutto coerente.</span><span class="sxs-lookup"><span data-stu-id="a7abf-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="a7abf-163">Tecnicamente, una classe che eredita dalla classe ActionFilterAttribute è sia un filtro azione che un filtro dei risultati.</span><span class="sxs-lookup"><span data-stu-id="a7abf-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="a7abf-164">Tuttavia, in senso sciolto, il filtro azione parola viene utilizzato per fare riferimento a qualsiasi tipo di filtro nel ASP.NET framework MVC.</span><span class="sxs-lookup"><span data-stu-id="a7abf-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="a7abf-165">La classe ActionFilterAttribute di base dispone dei seguenti metodi di cui è possibile eseguire l'override:</span><span class="sxs-lookup"><span data-stu-id="a7abf-165">The base ActionFilterAttribute class has the following methods that you can override:</span></span>

- <span data-ttu-id="a7abf-166">OnActionExecuting : questo metodo viene chiamato prima dell'esecuzione di un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="a7abf-167">OnActionExecuted : questo metodo viene chiamato dopo l'esecuzione di un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="a7abf-168">OnResultExecuting: questo metodo viene chiamato prima dell'esecuzione di un risultato dell'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="a7abf-169">OnResultExecuted : questo metodo viene chiamato dopo l'esecuzione di un risultato dell'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="a7abf-170">Nella sezione successiva, vedremo come è possibile implementare ciascuno di questi diversi metodi.</span><span class="sxs-lookup"><span data-stu-id="a7abf-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="a7abf-171">Creazione di un filtro azioni logCreating a Log Action Filter</span><span class="sxs-lookup"><span data-stu-id="a7abf-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="a7abf-172">Per illustrare come è possibile compilare un filtro azione personalizzato, verrà creato un filtro azione personalizzato che registra le fasi dell'elaborazione di un'azione del controller nella finestra Output di Visual Studio.In to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span><span class="sxs-lookup"><span data-stu-id="a7abf-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="a7abf-173">Il `LogActionFilter` nostro è contenuto nella lista 2.</span><span class="sxs-lookup"><span data-stu-id="a7abf-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="a7abf-174">**Listato 2 –`ActionFilters\LogActionFilter.vb`**</span><span class="sxs-lookup"><span data-stu-id="a7abf-174">**Listing 2 – `ActionFilters\LogActionFilter.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample2.vb)]

<span data-ttu-id="a7abf-175">Nel listato `OnActionExecuting()`2, `OnResultExecuting()`i `OnResultExecuted()` metodi , `Log()` `OnActionExecuted()`, e chiamano tutti il metodo .</span><span class="sxs-lookup"><span data-stu-id="a7abf-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="a7abf-176">Il nome del metodo e i dati `Log()` della route corrente vengono passati al metodo.</span><span class="sxs-lookup"><span data-stu-id="a7abf-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="a7abf-177">Il `Log()` metodo scrive un messaggio nella finestra Output di Visual Studio (vedere Figura 2).</span><span class="sxs-lookup"><span data-stu-id="a7abf-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="a7abf-178">[![Scrittura nella finestra Output di Visual StudioWriting to the Visual Studio Output window](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="a7abf-178">[![Writing to the Visual Studio Output window](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span></span>

<span data-ttu-id="a7abf-179">**Figura 02**: Scrittura nella finestra Output di Visual Studio ([Fare clic per visualizzare l'immagine](understanding-action-filters-vb/_static/image6.png)a dimensioni intere )</span><span class="sxs-lookup"><span data-stu-id="a7abf-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-vb/_static/image6.png))</span></span>

<span data-ttu-id="a7abf-180">Il controller Home nel listato 3 illustra come applicare il filtro azione Log a un'intera classe controller.</span><span class="sxs-lookup"><span data-stu-id="a7abf-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="a7abf-181">Ogni volta che viene richiamata una delle azioni `Index()` esposte `About()` dal controller Home, ovvero il metodo o il metodo, le fasi dell'elaborazione dell'azione vengono registrate nella finestra Output di Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a7abf-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="a7abf-182">**Lista 3 –`Controllers\HomeController.vb`**</span><span class="sxs-lookup"><span data-stu-id="a7abf-182">**Listing 3 – `Controllers\HomeController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample3.vb)]

### <a name="summary"></a><span data-ttu-id="a7abf-183">Riepilogo</span><span class="sxs-lookup"><span data-stu-id="a7abf-183">Summary</span></span>

<span data-ttu-id="a7abf-184">In questa esercitazione sono stati introdotti a ASP.NETi filtri di azione MVC.</span><span class="sxs-lookup"><span data-stu-id="a7abf-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="a7abf-185">Sono stati acquisiti i quattro diversi tipi di filtri: filtri di autorizzazione, filtri azione, filtri risultati e filtri eccezioni.</span><span class="sxs-lookup"><span data-stu-id="a7abf-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="a7abf-186">Hai anche imparato `ActionFilterAttribute` a conoscere la classe base.</span><span class="sxs-lookup"><span data-stu-id="a7abf-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="a7abf-187">Infine, si è appreso come implementare un filtro azione semplice.</span><span class="sxs-lookup"><span data-stu-id="a7abf-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="a7abf-188">È stato creato un filtro azione di log che registra le fasi dell'elaborazione di un'azione del controller nella finestra Output di Visual Studio.We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span><span class="sxs-lookup"><span data-stu-id="a7abf-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="a7abf-189">[Successivo](asp-net-mvc-routing-overview-vb.md)
> [precedente](improving-performance-with-output-caching-vb.md)</span><span class="sxs-lookup"><span data-stu-id="a7abf-189">[Previous](asp-net-mvc-routing-overview-vb.md)
[Next](improving-performance-with-output-caching-vb.md)</span></span>
