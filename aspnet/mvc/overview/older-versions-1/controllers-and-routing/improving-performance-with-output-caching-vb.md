---
uid: mvc/overview/older-versions-1/controllers-and-routing/improving-performance-with-output-caching-vb
title: Miglioramento delle prestazioni con la memorizzazione nella cache di output (VB) Documenti Microsoft
author: rick-anderson
description: In questa esercitazione si apprenderà come migliorare notevolmente le prestazioni delle applicazioni Web MVC ASP.NET sfruttando la memorizzazione nella cache di output. tu...
ms.author: riande
ms.date: 01/27/2009
ms.assetid: 0e7b4d85-2c46-4eaf-b6a8-6cd566a67334
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/improving-performance-with-output-caching-vb
msc.type: authoredcontent
ms.openlocfilehash: e18d4c5132d4dccc97f1465e7885c9c47a0edab1
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542690"
---
# <a name="improving-performance-with-output-caching-vb"></a><span data-ttu-id="add3c-104">Miglioramento delle prestazioni con la cache di output (VB)</span><span class="sxs-lookup"><span data-stu-id="add3c-104">Improving Performance with Output Caching (VB)</span></span>

<span data-ttu-id="add3c-105">da parte [di Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="add3c-105">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="add3c-106">In questa esercitazione si apprenderà come migliorare notevolmente le prestazioni delle applicazioni Web MVC ASP.NET sfruttando la memorizzazione nella cache di output.</span><span class="sxs-lookup"><span data-stu-id="add3c-106">In this tutorial, you learn how you can dramatically improve the performance of your ASP.NET MVC web applications by taking advantage of output caching.</span></span> <span data-ttu-id="add3c-107">Si apprenderà come memorizzare nella cache il risultato restituito da un'azione del controller in modo che non sia necessario creare lo stesso contenuto ogni volta che un nuovo utente richiama l'azione.</span><span class="sxs-lookup"><span data-stu-id="add3c-107">You learn how to cache the result returned from a controller action so that the same content does not need to be created each and every time a new user invokes the action.</span></span>

<span data-ttu-id="add3c-108">L'obiettivo di questa esercitazione è spiegare come è possibile migliorare notevolmente le prestazioni di un'applicazione MVC ASP.NET sfruttando la cache di output.</span><span class="sxs-lookup"><span data-stu-id="add3c-108">The goal of this tutorial is to explain how you can dramatically improve the performance of an ASP.NET MVC application by taking advantage of the output cache.</span></span> <span data-ttu-id="add3c-109">La cache di output consente di memorizzare nella cache il contenuto restituito da un'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="add3c-109">The output cache enables you to cache the content returned by a controller action.</span></span> <span data-ttu-id="add3c-110">In questo modo, non è necessario generare lo stesso contenuto ogni volta che viene richiamata la stessa azione del controller.</span><span class="sxs-lookup"><span data-stu-id="add3c-110">That way, the same content does not need to be generated each and every time the same controller action is invoked.</span></span>

<span data-ttu-id="add3c-111">Si supponga, ad esempio, che l'applicazione ASP.NET MVC visualizza un elenco di record di database in una visualizzazione denominata Indice.</span><span class="sxs-lookup"><span data-stu-id="add3c-111">Imagine, for example, that your ASP.NET MVC application displays a list of database records in a view named Index.</span></span> <span data-ttu-id="add3c-112">In genere, ogni volta che un utente richiama l'azione del controller che restituisce la visualizzazione dell'indice, il set di record del database deve essere recuperato dal database eseguendo una query di database.</span><span class="sxs-lookup"><span data-stu-id="add3c-112">Normally, each and every time that a user invokes the controller action that returns the Index view, the set of database records must be retrieved from the database by executing a database query.</span></span>

<span data-ttu-id="add3c-113">Se, d'altra parte, si sfrutta la cache di output, è possibile evitare di eseguire una query di database ogni volta che un utente richiama la stessa azione del controller.</span><span class="sxs-lookup"><span data-stu-id="add3c-113">If, on the other hand, you take advantage of the output cache then you can avoid executing a database query every time any user invokes the same controller action.</span></span> <span data-ttu-id="add3c-114">La visualizzazione può essere recuperata dalla cache anziché essere rigenerata dall'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="add3c-114">The view can be retrieved from the cache instead of being regenerated from the controller action.</span></span> <span data-ttu-id="add3c-115">La memorizzazione nella cache consente di evitare di eseguire operazioni ridondanti sul server.</span><span class="sxs-lookup"><span data-stu-id="add3c-115">Caching enables you to avoid performing redundant work on the server.</span></span>

#### <a name="enabling-output-caching"></a><span data-ttu-id="add3c-116">Abilitazione della memorizzazione nella cache di outputEnabling Output Caching</span><span class="sxs-lookup"><span data-stu-id="add3c-116">Enabling Output Caching</span></span>

<span data-ttu-id="add3c-117">Per abilitare la memorizzazione nella cache di output, aggiungere un &lt;attributo OutputCache&gt; a una singola azione del controller o a un'intera classe controller.</span><span class="sxs-lookup"><span data-stu-id="add3c-117">You enable output caching by adding an &lt;OutputCache&gt; attribute to either an individual controller action or an entire controller class.</span></span> <span data-ttu-id="add3c-118">Ad esempio, il controller nel listato 1 espone un'azione denominata Index().</span><span class="sxs-lookup"><span data-stu-id="add3c-118">For example, the controller in Listing 1 exposes an action named Index().</span></span> <span data-ttu-id="add3c-119">L'output dell'azione Index() viene memorizzato nella cache per 10 secondi.</span><span class="sxs-lookup"><span data-stu-id="add3c-119">The output of the Index() action is cached for 10 seconds.</span></span>

<span data-ttu-id="add3c-120">**Listato 1 – Controller-HomeController.vb**</span><span class="sxs-lookup"><span data-stu-id="add3c-120">**Listing 1 – Controllers\HomeController.vb**</span></span>

[!code-vb[Main](improving-performance-with-output-caching-vb/samples/sample1.vb)]

<span data-ttu-id="add3c-121">Nelle versioni Beta di ASP.NET MVC, la memorizzazione [http://www.MySite.com/](http://www.mysite.com/)nella cache di output non funziona per un URL come .</span><span class="sxs-lookup"><span data-stu-id="add3c-121">In the Beta versions of ASP.NET MVC, output caching does not work for a URL like [http://www.MySite.com/](http://www.mysite.com/).</span></span> <span data-ttu-id="add3c-122">È invece necessario immettere un URL come [http://www.MySite.com/Home/Index](http://www.mysite.com/Home/Index).</span><span class="sxs-lookup"><span data-stu-id="add3c-122">Instead, you must enter a URL like [http://www.MySite.com/Home/Index](http://www.mysite.com/Home/Index).</span></span>

<span data-ttu-id="add3c-123">Nel listato 1, l'output dell'azione Index() viene memorizzato nella cache per 10 secondi.</span><span class="sxs-lookup"><span data-stu-id="add3c-123">In Listing 1, the output of the Index() action is cached for 10 seconds.</span></span> <span data-ttu-id="add3c-124">Se si preferisce, è possibile specificare una durata della cache molto più lunga.</span><span class="sxs-lookup"><span data-stu-id="add3c-124">If you prefer, you can specify a much longer cache duration.</span></span> <span data-ttu-id="add3c-125">Ad esempio, se si desidera memorizzare nella cache l'output di un'azione del controller per un \* giorno, \* è possibile specificare una durata della cache di 86400 secondi (60 secondi 60 minuti 24 ore).</span><span class="sxs-lookup"><span data-stu-id="add3c-125">For example, if you want to cache the output of a controller action for one day then you can specify a cache duration of 86400 seconds (60 seconds \* 60 minutes \* 24 hours).</span></span>

<span data-ttu-id="add3c-126">Non vi è alcuna garanzia che il contenuto verrà memorizzato nella cache per il periodo di tempo specificato.</span><span class="sxs-lookup"><span data-stu-id="add3c-126">There is no guarantee that content will be cached for the amount of time that you specify.</span></span> <span data-ttu-id="add3c-127">Quando le risorse di memoria diventano inesaurite, la cache inizia a rimuovere automaticamente il contenuto.</span><span class="sxs-lookup"><span data-stu-id="add3c-127">When memory resources become low, the cache starts evicting content automatically.</span></span>

<span data-ttu-id="add3c-128">Il controller Home nel listato 1 restituisce la visualizzazione Dell'indice nel listato 2.</span><span class="sxs-lookup"><span data-stu-id="add3c-128">The Home controller in Listing 1 returns the Index view in Listing 2.</span></span> <span data-ttu-id="add3c-129">Non c'è niente di speciale in questo punto di vista.</span><span class="sxs-lookup"><span data-stu-id="add3c-129">There is nothing special about this view.</span></span> <span data-ttu-id="add3c-130">La vista Indice visualizza semplicemente l'ora corrente (vedere Figura 1).</span><span class="sxs-lookup"><span data-stu-id="add3c-130">The Index view simply displays the current time (see Figure 1).</span></span>

<span data-ttu-id="add3c-131">**Listato 2 – Visualizzazioni, Home, Indice.aspx**</span><span class="sxs-lookup"><span data-stu-id="add3c-131">**Listing 2 – Views\Home\Index.aspx**</span></span>

[!code-aspx[Main](improving-performance-with-output-caching-vb/samples/sample2.aspx)]

<span data-ttu-id="add3c-132">**Figura 1 – Visualizzazione dell'indice memorizzato nella cache**</span><span class="sxs-lookup"><span data-stu-id="add3c-132">**Figure 1 – Cached Index view**</span></span>

![clip_image002](improving-performance-with-output-caching-vb/_static/image1.jpg)

<span data-ttu-id="add3c-134">Se invocate l'azione Indice( più volte immettendo l'URL /Home/Index nella barra degli indirizzi del browser e premendo ripetutamente il pulsante Aggiorna/Ricarica nel browser), il tempo visualizzato dalla vista Indice non cambierà per 10 secondi.</span><span class="sxs-lookup"><span data-stu-id="add3c-134">If you invoke the Index() action multiple times by entering the URL /Home/Index in the address bar of your browser and hitting the Refresh/Reload button in your browser repeatedly, then the time displayed by the Index view won't change for 10 seconds.</span></span> <span data-ttu-id="add3c-135">Viene visualizzata la stessa ora perché la visualizzazione viene memorizzata nella cache.</span><span class="sxs-lookup"><span data-stu-id="add3c-135">The same time is displayed because the view is cached.</span></span>

<span data-ttu-id="add3c-136">È importante comprendere che la stessa visualizzazione viene memorizzata nella cache per tutti gli utenti che visitano l'applicazione.</span><span class="sxs-lookup"><span data-stu-id="add3c-136">It is important to understand that the same view is cached for everyone who visits your application.</span></span> <span data-ttu-id="add3c-137">Chiunque richiami l'azione Index() otterrà la stessa versione memorizzata nella cache della vista Indice.</span><span class="sxs-lookup"><span data-stu-id="add3c-137">Anyone who invokes the Index() action will get the same cached version of the Index view.</span></span> <span data-ttu-id="add3c-138">Ciò significa che la quantità di lavoro che il server web deve eseguire per servire la visualizzazione indice è drasticamente ridotta.</span><span class="sxs-lookup"><span data-stu-id="add3c-138">This means that the amount of work that the web server must perform to serve the Index view is dramatically reduced.</span></span>

<span data-ttu-id="add3c-139">La vista nella lista 2 sembra fare qualcosa di veramente semplice.</span><span class="sxs-lookup"><span data-stu-id="add3c-139">The view in Listing 2 happens to be doing something really simple.</span></span> <span data-ttu-id="add3c-140">La vista mostra solo l'ora corrente.</span><span class="sxs-lookup"><span data-stu-id="add3c-140">The view just displays the current time.</span></span> <span data-ttu-id="add3c-141">Tuttavia, è possibile memorizzare nella cache una visualizzazione nella cache con cui viene visualizzato un set di record del database.</span><span class="sxs-lookup"><span data-stu-id="add3c-141">However, you could just as easily cache a view that displays a set of database records.</span></span> <span data-ttu-id="add3c-142">In tal caso, non è necessario recuperare il set di record del database dal database ogni volta che viene richiamata l'azione del controller che restituisce la visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="add3c-142">In that case, the set of database records would not need to be retrieved from the database each and every time the controller action that returns the view is invoked.</span></span> <span data-ttu-id="add3c-143">La memorizzazione nella cache può ridurre la quantità di lavoro che il server Web e il server di database devono eseguire.</span><span class="sxs-lookup"><span data-stu-id="add3c-143">Caching can reduce the amount of work that both your web server and database server must perform.</span></span>

<span data-ttu-id="add3c-144">Non utilizzare la &lt;pagina % ,&gt; OutputCache % direttiva in una visualizzazione MVC.</span><span class="sxs-lookup"><span data-stu-id="add3c-144">Don't use the page &lt;%@ OutputCache %&gt; directive in an MVC view.</span></span> <span data-ttu-id="add3c-145">Questa direttiva non viene eseguito dal mondo Web Form e non deve essere utilizzata in un'applicazione MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="add3c-145">This directive is bleeding over from the Web Forms world and should not be used in an ASP.NET MVC application.</span></span> 

#### <a name="where-content-is-cached"></a><span data-ttu-id="add3c-146">Dove il contenuto viene memorizzato nella cacheWhere Content is Cached</span><span class="sxs-lookup"><span data-stu-id="add3c-146">Where Content is Cached</span></span>

<span data-ttu-id="add3c-147">Per impostazione predefinita, &lt;quando&gt; si utilizza l'attributo OutputCache, il contenuto viene memorizzato nella cache in tre posizioni: il server Web, tutti i server proxy e il browser Web.</span><span class="sxs-lookup"><span data-stu-id="add3c-147">By default, when you use the &lt;OutputCache&gt; attribute, content is cached in three locations: the web server, any proxy servers, and the web browser.</span></span> <span data-ttu-id="add3c-148">È possibile controllare esattamente dove il contenuto viene &lt;memorizzato nella cache modificando il Location proprietà del OutputCache&gt; attributo.</span><span class="sxs-lookup"><span data-stu-id="add3c-148">You can control exactly where content is cached by modifying the Location property of the &lt;OutputCache&gt; attribute.</span></span>

<span data-ttu-id="add3c-149">È possibile impostare la proprietà Location su uno dei seguenti valori:</span><span class="sxs-lookup"><span data-stu-id="add3c-149">You can set the Location property to any one of the following values:</span></span>

> <span data-ttu-id="add3c-150">· Qualsiasi</span><span class="sxs-lookup"><span data-stu-id="add3c-150">· Any</span></span>
> 
> <span data-ttu-id="add3c-151">· Client</span><span class="sxs-lookup"><span data-stu-id="add3c-151">· Client</span></span>
> 
> <span data-ttu-id="add3c-152">· Downstream</span><span class="sxs-lookup"><span data-stu-id="add3c-152">· Downstream</span></span>
> 
> <span data-ttu-id="add3c-153">· Server</span><span class="sxs-lookup"><span data-stu-id="add3c-153">· Server</span></span>
> 
> <span data-ttu-id="add3c-154">· Nessuno</span><span class="sxs-lookup"><span data-stu-id="add3c-154">· None</span></span>
> 
> <span data-ttu-id="add3c-155">· ServerAndClient</span><span class="sxs-lookup"><span data-stu-id="add3c-155">· ServerAndClient</span></span>

<span data-ttu-id="add3c-156">Per impostazione predefinita, il Location proprietà ha il valore Any.By default, the Location property has the value Any.</span><span class="sxs-lookup"><span data-stu-id="add3c-156">By default, the Location property has the value Any.</span></span> <span data-ttu-id="add3c-157">Tuttavia, esistono situazioni in cui è possibile memorizzare nella cache solo nel browser o solo sul server.</span><span class="sxs-lookup"><span data-stu-id="add3c-157">However, there are situations in which you might want to cache only on the browser or only on the server.</span></span> <span data-ttu-id="add3c-158">Ad esempio, se si memorizzano nella cache informazioni personalizzate per ogni utente, è consigliabile non memorizzare nella cache le informazioni sul server.</span><span class="sxs-lookup"><span data-stu-id="add3c-158">For example, if you are caching information that is personalized for each user then you should not cache the information on the server.</span></span> <span data-ttu-id="add3c-159">Se si visualizzano informazioni diverse a utenti diversi, è necessario memorizzare nella cache le informazioni solo sul client.</span><span class="sxs-lookup"><span data-stu-id="add3c-159">If you are displaying different information to different users then you should cache the information only on the client.</span></span>

<span data-ttu-id="add3c-160">Ad esempio, il controller nel listato 3 espone un'azione denominata GetName() che restituisce il nome utente corrente.</span><span class="sxs-lookup"><span data-stu-id="add3c-160">For example, the controller in Listing 3 exposes an action named GetName() that returns the current user name.</span></span> <span data-ttu-id="add3c-161">Se Jack accede al sito Web e richiama l'azione GetName(), l'azione restituisce la stringa "Hi Jack".</span><span class="sxs-lookup"><span data-stu-id="add3c-161">If Jack logs into the website and invokes the GetName() action then the action returns the string "Hi Jack".</span></span> <span data-ttu-id="add3c-162">Se, successivamente, Jill accede al sito Web e richiama l'azione GetName(), otterrà anche la stringa "Hi Jack".</span><span class="sxs-lookup"><span data-stu-id="add3c-162">If, subsequently, Jill logs into the website and invokes the GetName() action then she also will get the string "Hi Jack".</span></span> <span data-ttu-id="add3c-163">La stringa viene memorizzata nella cache del server Web per tutti gli utenti dopo che Jack richiama inizialmente l'azione del controller.</span><span class="sxs-lookup"><span data-stu-id="add3c-163">The string is cached on the web server for all users after Jack initially invokes the controller action.</span></span>

<span data-ttu-id="add3c-164">**Listato 3 – Controller- BadUserController.vb**</span><span class="sxs-lookup"><span data-stu-id="add3c-164">**Listing 3 – Controllers\BadUserController.vb**</span></span>

[!code-vb[Main](improving-performance-with-output-caching-vb/samples/sample3.vb)]

<span data-ttu-id="add3c-165">Molto probabilmente, il controller nel listato 3 non funziona nel modo desiderato.</span><span class="sxs-lookup"><span data-stu-id="add3c-165">Most likely, the controller in Listing 3 does not work the way that you want.</span></span> <span data-ttu-id="add3c-166">Non si desidera visualizzare il messaggio "Ciao Jack" a Jill.</span><span class="sxs-lookup"><span data-stu-id="add3c-166">You don't want to display the message "Hi Jack" to Jill.</span></span>

<span data-ttu-id="add3c-167">Non memorizzare mai nella cache il contenuto personalizzato nella cache del server.</span><span class="sxs-lookup"><span data-stu-id="add3c-167">You should never cache personalized content in the server cache.</span></span> <span data-ttu-id="add3c-168">Tuttavia, è possibile memorizzare nella cache il contenuto personalizzato nella cache del browser per migliorare le prestazioni.</span><span class="sxs-lookup"><span data-stu-id="add3c-168">However, you might want to cache the personalized content in the browser cache to improve performance.</span></span> <span data-ttu-id="add3c-169">Se si memorizza il contenuto nella cache nel browser e un utente richiama più volte la stessa azione del controller, il contenuto può essere recuperato dalla cache del browser anziché dal server.</span><span class="sxs-lookup"><span data-stu-id="add3c-169">If you cache content in the browser, and a user invokes the same controller action multiple times, then the content can be retrieved from the browser cache instead of the server.</span></span>

<span data-ttu-id="add3c-170">Il controller modificato nel listato 4 memorizza nella cache l'output dell'azione GetName().</span><span class="sxs-lookup"><span data-stu-id="add3c-170">The modified controller in Listing 4 caches the output of the GetName() action.</span></span> <span data-ttu-id="add3c-171">Tuttavia, il contenuto viene memorizzato nella cache solo nel browser e non nel server.</span><span class="sxs-lookup"><span data-stu-id="add3c-171">However, the content is cached only on the browser and not on the server.</span></span> <span data-ttu-id="add3c-172">In questo modo, quando più utenti richiamano il metodo GetName(), ogni persona ottiene il proprio nome utente e non il nome utente di un'altra persona.</span><span class="sxs-lookup"><span data-stu-id="add3c-172">That way, when multiple users invoke the GetName() method, each person gets their own user name and not another person's user name.</span></span>

<span data-ttu-id="add3c-173">**Listato 4 – Controller/Controlloutente.vb**</span><span class="sxs-lookup"><span data-stu-id="add3c-173">**Listing 4 – Controllers\UserController.vb**</span></span>

[!code-vb[Main](improving-performance-with-output-caching-vb/samples/sample4.vb)]

<span data-ttu-id="add3c-174">Si noti che l'attributo &lt;OutputCache&gt; nel listato 4 include una proprietà Location impostata sul valore OutputCacheLocation.Client.</span><span class="sxs-lookup"><span data-stu-id="add3c-174">Notice that the &lt;OutputCache&gt; attribute in Listing 4 includes a Location property set to the value OutputCacheLocation.Client.</span></span> <span data-ttu-id="add3c-175">L'attributo &lt;OutputCache include anche una proprietà NoStore.The OutputCache&gt; attribute also includes a NoStore property.</span><span class="sxs-lookup"><span data-stu-id="add3c-175">The &lt;OutputCache&gt; attribute also includes a NoStore property.</span></span> <span data-ttu-id="add3c-176">Il NoStore proprietà viene utilizzata per informare i server proxy e browser che non devono archiviare una copia permanente del contenuto memorizzato nella cache.</span><span class="sxs-lookup"><span data-stu-id="add3c-176">The NoStore property is used to inform proxy servers and browsers that they should not store a permanent copy of the cached content.</span></span>

#### <a name="varying-the-output-cache"></a><span data-ttu-id="add3c-177">Variazione della cache di outputVarying the Output Cache</span><span class="sxs-lookup"><span data-stu-id="add3c-177">Varying the Output Cache</span></span>

<span data-ttu-id="add3c-178">In alcune situazioni, potrebbero essere esigenze diverse versioni memorizzate nella cache dello stesso contenuto.</span><span class="sxs-lookup"><span data-stu-id="add3c-178">In some situations, you might want different cached versions of the very same content.</span></span> <span data-ttu-id="add3c-179">Si supponga, ad esempio, che si sta creando una pagina master/dettaglio.</span><span class="sxs-lookup"><span data-stu-id="add3c-179">Imagine, for example, that you are creating a master/detail page.</span></span> <span data-ttu-id="add3c-180">La pagina master visualizza un elenco di titoli di film.</span><span class="sxs-lookup"><span data-stu-id="add3c-180">The master page displays a list of movie titles.</span></span> <span data-ttu-id="add3c-181">Quando si fa clic su un titolo, si ottengono i dettagli per il filmato selezionato.</span><span class="sxs-lookup"><span data-stu-id="add3c-181">When you click a title, you get details for the selected movie.</span></span>

<span data-ttu-id="add3c-182">Se si memorizza nella cache la pagina dei dettagli, verranno visualizzati i dettagli per lo stesso filmato, indipendentemente dal filmato su cui si fa clic.</span><span class="sxs-lookup"><span data-stu-id="add3c-182">If you cache the details page, then the details for the same movie will be displayed no matter which movie you click.</span></span> <span data-ttu-id="add3c-183">Il primo filmato selezionato dal primo utente verrà visualizzato a tutti gli utenti futuri.</span><span class="sxs-lookup"><span data-stu-id="add3c-183">The first movie selected by the first user will be displayed to all future users.</span></span>

<span data-ttu-id="add3c-184">È possibile risolvere questo problema sfruttando la proprietà &lt;VaryByParam dell'attributo OutputCache.&gt;</span><span class="sxs-lookup"><span data-stu-id="add3c-184">You can fix this problem by taking advantage of the VaryByParam property of the &lt;OutputCache&gt; attribute.</span></span> <span data-ttu-id="add3c-185">Questa proprietà consente di creare diverse versioni memorizzate nella cache dello stesso contenuto quando un parametro del form o un parametro di stringa di query varia.</span><span class="sxs-lookup"><span data-stu-id="add3c-185">This property enables you to create different cached versions of the very same content when a form parameter or query string parameter varies.</span></span>

<span data-ttu-id="add3c-186">Ad esempio, il controller nel listato 5 espone due azioni denominate Master() e Details().</span><span class="sxs-lookup"><span data-stu-id="add3c-186">For example, the controller in Listing 5 exposes two actions named Master() and Details().</span></span> <span data-ttu-id="add3c-187">L'azione Master() restituisce un elenco di titoli di film e l'azione Details() restituisce i dettagli per il filmato selezionato.</span><span class="sxs-lookup"><span data-stu-id="add3c-187">The Master() action returns a list of movie titles and the Details() action returns the details for the selected movie.</span></span>

<span data-ttu-id="add3c-188">**Listato 5 – Controller-MoviesController.vb**</span><span class="sxs-lookup"><span data-stu-id="add3c-188">**Listing 5 – Controllers\MoviesController.vb**</span></span>

[!code-vb[Main](improving-performance-with-output-caching-vb/samples/sample5.vb)]

<span data-ttu-id="add3c-189">L'azione Master() include una proprietà VaryByParam con il valore "none".</span><span class="sxs-lookup"><span data-stu-id="add3c-189">The Master() action includes a VaryByParam property with the value "none".</span></span> <span data-ttu-id="add3c-190">Quando viene richiamata l'azione Master(), viene restituita la stessa versione memorizzata nella cache della visualizzazione master.</span><span class="sxs-lookup"><span data-stu-id="add3c-190">When the Master() action is invoked, the same cached version of the Master view is returned.</span></span> <span data-ttu-id="add3c-191">Tutti i parametri del modulo o i parametri della stringa di query vengono ignorati (vedere la Figura 2).</span><span class="sxs-lookup"><span data-stu-id="add3c-191">Any form parameters or query string parameters are ignored (see Figure 2).</span></span>

<span data-ttu-id="add3c-192">**Figura 2 – La vista /Movies/Master**</span><span class="sxs-lookup"><span data-stu-id="add3c-192">**Figure 2 – The /Movies/Master view**</span></span>

![clip_image004](improving-performance-with-output-caching-vb/_static/image2.jpg)

<span data-ttu-id="add3c-194">**Figura 3 – La visualizzazione /Movies/Details**</span><span class="sxs-lookup"><span data-stu-id="add3c-194">**Figure 3 – The /Movies/Details view**</span></span>

![clip_image006](improving-performance-with-output-caching-vb/_static/image3.jpg)

<span data-ttu-id="add3c-196">L'azione Details() include una proprietà VaryByParam con il valore "Id".</span><span class="sxs-lookup"><span data-stu-id="add3c-196">The Details() action includes a VaryByParam property with the value "Id".</span></span> <span data-ttu-id="add3c-197">Quando diversi valori del Id parametro vengono passati all'azione del controller, vengono generate diverse versioni memorizzate nella cache della visualizzazione Dettagli.</span><span class="sxs-lookup"><span data-stu-id="add3c-197">When different values of the Id parameter are passed to the controller action, different cached versions of the Details view are generated.</span></span>

<span data-ttu-id="add3c-198">È importante comprendere che l'utilizzo della proprietà VaryByParam comporta una maggiore memorizzazione nella cache e non meno.</span><span class="sxs-lookup"><span data-stu-id="add3c-198">It is important to understand that using the VaryByParam property results in more caching and not less.</span></span> <span data-ttu-id="add3c-199">Viene creata una versione memorizzata nella cache diversa della visualizzazione Dettagli per ogni versione diversa del parametro Id.</span><span class="sxs-lookup"><span data-stu-id="add3c-199">A different cached version of the Details view is created for each different version of the Id parameter.</span></span>

<span data-ttu-id="add3c-200">È possibile impostare la proprietà VaryByParam sui valori seguenti:</span><span class="sxs-lookup"><span data-stu-id="add3c-200">You can set the VaryByParam property to the following values:</span></span>

> <span data-ttu-id="add3c-201">\*Creare una versione memorizzata nella cache diversa ogni volta che un parametro di stringa di query o modulo varia.</span><span class="sxs-lookup"><span data-stu-id="add3c-201">\* = Create a different cached version whenever a form or query string parameter varies.</span></span>
> 
> <span data-ttu-id="add3c-202">none - Non creare mai versioni memorizzate nella cache diverse</span><span class="sxs-lookup"><span data-stu-id="add3c-202">none = Never create different cached versions</span></span>
> 
> <span data-ttu-id="add3c-203">Elenco di parametri a punto e virgola: consente di creare versioni memorizzate nella cache diverse ogni volta che uno qualsiasi dei parametri della stringa di query o del modulo nell'elenco varia</span><span class="sxs-lookup"><span data-stu-id="add3c-203">Semicolon list of parameters = Create different cached versions whenever any of the form or query string parameters in the list varies</span></span>

#### <a name="creating-a-cache-profile"></a><span data-ttu-id="add3c-204">Creazione di un profilo di cacheCreating a Cache Profile</span><span class="sxs-lookup"><span data-stu-id="add3c-204">Creating a Cache Profile</span></span>

<span data-ttu-id="add3c-205">In alternativa alla configurazione delle proprietà della &lt;cache&gt; di output modificando le proprietà dell'attributo OutputCache, è possibile creare un profilo della cache nel file di configurazione Web (web.config).</span><span class="sxs-lookup"><span data-stu-id="add3c-205">As an alternative to configuring output cache properties by modifying properties of the &lt;OutputCache&gt; attribute, you can create a cache profile in the web configuration (web.config) file.</span></span> <span data-ttu-id="add3c-206">La creazione di un profilo di cache nel file di configurazione Web offre un paio di vantaggi importanti.</span><span class="sxs-lookup"><span data-stu-id="add3c-206">Creating a cache profile in the web configuration file offers a couple of important advantages.</span></span>

<span data-ttu-id="add3c-207">In primo luogo, configurando la memorizzazione nella cache di output nel file di configurazione Web, è possibile controllare il modo in cui le azioni del controller memorizzano il contenuto nella cache in un'unica posizione centrale.</span><span class="sxs-lookup"><span data-stu-id="add3c-207">First, by configuring output caching in the web configuration file, you can control how controller actions cache content in one central location.</span></span> <span data-ttu-id="add3c-208">È possibile creare un profilo di cache e applicare il profilo a diversi controller o azioni controller.</span><span class="sxs-lookup"><span data-stu-id="add3c-208">You can create one cache profile and apply the profile to several controllers or controller actions.</span></span>

<span data-ttu-id="add3c-209">In secondo luogo, è possibile modificare il file di configurazione Web senza ricompilare l'applicazione.</span><span class="sxs-lookup"><span data-stu-id="add3c-209">Second, you can modify the web configuration file without recompiling your application.</span></span> <span data-ttu-id="add3c-210">Se è necessario disabilitare la memorizzazione nella cache per un'applicazione che è già stata distribuita nell'ambiente di produzione, è sufficiente modificare i profili di cache definiti nel file di configurazione Web.</span><span class="sxs-lookup"><span data-stu-id="add3c-210">If you need to disable caching for an application that has already been deployed to production, then you can simply modify the cache profiles defined in the web configuration file.</span></span> <span data-ttu-id="add3c-211">Eventuali modifiche al file di configurazione Web verranno rilevate automaticamente e applicate.</span><span class="sxs-lookup"><span data-stu-id="add3c-211">Any changes to the web configuration file will be detected automatically and applied.</span></span>

<span data-ttu-id="add3c-212">Ad esempio, &lt;la&gt; sezione di configurazione Web di memorizzazione nella cache nel listato 6 definisce un profilo di cache denominato Cache1Hour.For example, the caching web configuration section in Listing 6 defines a cache profile named Cache1Hour.</span><span class="sxs-lookup"><span data-stu-id="add3c-212">For example, the &lt;caching&gt; web configuration section in Listing 6 defines a cache profile named Cache1Hour.</span></span> <span data-ttu-id="add3c-213">La &lt;sezione&gt; di memorizzazione &lt;nella&gt; cache deve essere visualizzata all'interno della sezione system.web di un file di configurazione Web.</span><span class="sxs-lookup"><span data-stu-id="add3c-213">The &lt;caching&gt; section must appear within the &lt;system.web&gt; section of a web configuration file.</span></span>

<span data-ttu-id="add3c-214">**Listato 6 – Sezione di memorizzazione nella cache per web.configListing 6 – Caching section for web.config**</span><span class="sxs-lookup"><span data-stu-id="add3c-214">**Listing 6 – Caching section for web.config**</span></span>

[!code-xml[Main](improving-performance-with-output-caching-vb/samples/sample6.xml)]

<span data-ttu-id="add3c-215">Il controller nel listato 7 illustra come è possibile applicare il &lt;profilo&gt; Cache1Hour a un'azione del controller con il OutputCache attributo.</span><span class="sxs-lookup"><span data-stu-id="add3c-215">The controller in Listing 7 illustrates how you can apply the Cache1Hour profile to a controller action with the &lt;OutputCache&gt; attribute.</span></span>

<span data-ttu-id="add3c-216">**Listato 7 – Controllers-ProfileController.vb**</span><span class="sxs-lookup"><span data-stu-id="add3c-216">**Listing 7 – Controllers\ProfileController.vb**</span></span>

[!code-vb[Main](improving-performance-with-output-caching-vb/samples/sample7.vb)]

<span data-ttu-id="add3c-217">Se si richiama l'azione Index() esposta dal controller nel listato 7, verrà restituito lo stesso orario per 1 ora.</span><span class="sxs-lookup"><span data-stu-id="add3c-217">If you invoke the Index() action exposed by the controller in Listing 7 then the same time will be returned for 1 hour.</span></span>

#### <a name="summary"></a><span data-ttu-id="add3c-218">Riepilogo</span><span class="sxs-lookup"><span data-stu-id="add3c-218">Summary</span></span>

<span data-ttu-id="add3c-219">La memorizzazione nella cache di output offre un metodo molto semplice per migliorare notevolmente le prestazioni delle applicazioni MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="add3c-219">Output caching provides you with a very easy method of dramatically improving the performance of your ASP.NET MVC applications.</span></span> <span data-ttu-id="add3c-220">In questa esercitazione è stato &lt;illustrato&gt; come usare l'attributo OutputCache per memorizzare nella cache l'output delle azioni del controller.</span><span class="sxs-lookup"><span data-stu-id="add3c-220">In this tutorial, you learned how to use the &lt;OutputCache&gt; attribute to cache the output of controller actions.</span></span> <span data-ttu-id="add3c-221">È stato inoltre illustrato come &lt;modificare&gt; le proprietà dell'attributo OutputCache, ad esempio le proprietà Duration e VaryByParam, per modificare la modalità di memorizzazione nella cache del contenuto.</span><span class="sxs-lookup"><span data-stu-id="add3c-221">You also learned how to modify properties of the &lt;OutputCache&gt; attribute such as the Duration and VaryByParam properties to modify how content gets cached.</span></span> <span data-ttu-id="add3c-222">Infine, è stato illustrato come definire i profili di cache nel file di configurazione Web.</span><span class="sxs-lookup"><span data-stu-id="add3c-222">Finally, you learned how to define cache profiles in the web configuration file.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="add3c-223">[Successivo](understanding-action-filters-vb.md)
> [precedente](adding-dynamic-content-to-a-cached-page-vb.md)</span><span class="sxs-lookup"><span data-stu-id="add3c-223">[Previous](understanding-action-filters-vb.md)
[Next](adding-dynamic-content-to-a-cached-page-vb.md)</span></span>
