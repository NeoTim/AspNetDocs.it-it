---
uid: mvc/overview/older-versions/creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript
title: Creazione di un'applicazione MVC 3 con Razor e JavaScript discreto Documenti Microsoft
author: rick-anderson
description: L'applicazione Web di esempio Elenco utenti dimostra quanto sia semplice creare ASP.NET applicazioni MVC 3 usando il motore di visualizzazione Razor.The User List sample web application demonstrates how simple it is to create ASP.NET MVC 3 applications using the Razor view engine. L'applicazione di esempio s...
ms.author: riande
ms.date: 11/01/2010
ms.assetid: 658b149b-d770-46bf-8b4b-4e47cca242f3
msc.legacyurl: /mvc/overview/older-versions/creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript
msc.type: authoredcontent
ms.openlocfilehash: c57e19f8eeca15e3676b3d490b08f69786d44f93
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542456"
---
# <a name="creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript"></a><span data-ttu-id="08847-104">Creazione di un'applicazione MVC 3 con Razor e JavaScript discreto</span><span class="sxs-lookup"><span data-stu-id="08847-104">Creating a MVC 3 Application with Razor and Unobtrusive JavaScript</span></span>

<span data-ttu-id="08847-105">da parte [di Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="08847-105">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="08847-106">L'applicazione Web di esempio Elenco utenti dimostra quanto sia semplice creare ASP.NET applicazioni MVC 3 usando il motore di visualizzazione Razor.The User List sample web application demonstrates how simple it is to create ASP.NET MVC 3 applications using the Razor view engine.</span><span class="sxs-lookup"><span data-stu-id="08847-106">The User List sample web application demonstrates how simple it is to create ASP.NET MVC 3 applications using the Razor view engine.</span></span> <span data-ttu-id="08847-107">L'applicazione di esempio mostra come usare il nuovo motore di visualizzazione Razor con ASP.NET MVC versione 3 e Visual Studio 2010 per creare un sito Web fittizio elenco utenti che include funzionalità quali la creazione, la visualizzazione, la modifica e l'eliminazione di utenti.</span><span class="sxs-lookup"><span data-stu-id="08847-107">The sample application shows how to use the new Razor view engine with ASP.NET MVC version 3 and Visual Studio 2010 to create a fictional User List website that includes functionality such as creating, displaying, editing, and deleting users.</span></span>
> 
> <span data-ttu-id="08847-108">Questa esercitazione descrive i passaggi eseguiti per compilare l'esempio di elenco utenti ASP.NETapplicazione MVC 3.</span><span class="sxs-lookup"><span data-stu-id="08847-108">This tutorial describes the steps that were taken in order to build the User List sample ASP.NET MVC 3 application.</span></span> <span data-ttu-id="08847-109">Per accompagnare questo argomento è disponibile un progetto di Visual Studio con il codice sorgente di C e VB: [Download](https://code.msdn.microsoft.com/aspnetmvcsamples/Release/ProjectReleases.aspx?ReleaseId=5114).</span><span class="sxs-lookup"><span data-stu-id="08847-109">A Visual Studio project with C# and VB source code is available to accompany this topic: [Download](https://code.msdn.microsoft.com/aspnetmvcsamples/Release/ProjectReleases.aspx?ReleaseId=5114).</span></span> <span data-ttu-id="08847-110">Se hai domande su questo tutorial, puoi pubblicarli nel [forum MVC](https://forums.asp.net/1146.aspx).</span><span class="sxs-lookup"><span data-stu-id="08847-110">If you have questions about this tutorial, please post them to the [MVC forum](https://forums.asp.net/1146.aspx).</span></span>

## <a name="overview"></a><span data-ttu-id="08847-111">Panoramica</span><span class="sxs-lookup"><span data-stu-id="08847-111">Overview</span></span>

<span data-ttu-id="08847-112">L'applicazione che verrà costruito è un semplice sito web elenco utenti.</span><span class="sxs-lookup"><span data-stu-id="08847-112">The application you'll be building is a simple user list website.</span></span> <span data-ttu-id="08847-113">Gli utenti possono immettere, visualizzare e aggiornare le informazioni utente.</span><span class="sxs-lookup"><span data-stu-id="08847-113">Users can enter, view, and update user information.</span></span>

![Sito di esempio](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image1.png)

<span data-ttu-id="08847-115">È possibile scaricare il progetto di VB e C, completato [qui](https://code.msdn.microsoft.com/Creating-a-MVC-3-28883c0f).</span><span class="sxs-lookup"><span data-stu-id="08847-115">You can download the VB and C# completed project [here](https://code.msdn.microsoft.com/Creating-a-MVC-3-28883c0f).</span></span>

## <a name="creating-the-web-application"></a><span data-ttu-id="08847-116">Creazione dell'applicazione Web</span><span class="sxs-lookup"><span data-stu-id="08847-116">Creating the Web Application</span></span>

<span data-ttu-id="08847-117">Per avviare l'esercitazione, aprire Visual Studio 2010 e creare un nuovo progetto utilizzando il modello *di applicazione Web ASP.NET MVC 3.*</span><span class="sxs-lookup"><span data-stu-id="08847-117">To start the tutorial, open Visual Studio 2010 and create a new project using the *ASP.NET MVC 3 Web Application* template.</span></span> <span data-ttu-id="08847-118">Denominare &quot;l'applicazione&quot;Mvc3Razor .</span><span class="sxs-lookup"><span data-stu-id="08847-118">Name the application &quot;Mvc3Razor&quot;.</span></span>

<span data-ttu-id="08847-119">[![Nuovo progetto MVC 3](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image3.png)](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image2.png)</span><span class="sxs-lookup"><span data-stu-id="08847-119">[![New MVC 3 project](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image3.png)](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image2.png)</span></span>

<span data-ttu-id="08847-120">Nella finestra di dialogo **Nuovo ASP.NET progetto MVC 3** selezionare Applicazione **Internet**, selezionare il motore di visualizzazione Razor e quindi fare clic su **OK**.</span><span class="sxs-lookup"><span data-stu-id="08847-120">In the **New ASP.NET MVC 3 Project** dialog, select **Internet Application**, select the Razor view engine, and then click **OK**.</span></span>

![Nuova finestra di dialogo Progetto MVC 3 ASP.NET](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image4.png)

<span data-ttu-id="08847-122">In questa esercitazione non si utilizzerà il provider di appartenenze ASP.NET, pertanto è possibile eliminare tutti i file associati all'accesso e all'appartenenza.</span><span class="sxs-lookup"><span data-stu-id="08847-122">In this tutorial you will not be using the ASP.NET membership provider, so you can delete all the files associated with logon and membership.</span></span> <span data-ttu-id="08847-123">In **Esplora soluzioni**rimuovere i file e le directory seguenti:</span><span class="sxs-lookup"><span data-stu-id="08847-123">In **Solution Explorer**, remove the following files and directories:</span></span>

- <span data-ttu-id="08847-124">*Controller Di Controllo (Controllers/AccountController)*</span><span class="sxs-lookup"><span data-stu-id="08847-124">*Controllers\AccountController*</span></span>
- <span data-ttu-id="08847-125">*Modelli: AccountModel*</span><span class="sxs-lookup"><span data-stu-id="08847-125">*Models\AccountModels*</span></span>
- <span data-ttu-id="08847-126">*Viste -\\_LogOnPartial condivise*</span><span class="sxs-lookup"><span data-stu-id="08847-126">*Views\Shared\\_LogOnPartial*</span></span>
- <span data-ttu-id="08847-127">*Visualizzazioni- Account* (e tutti i file in questa directory)</span><span class="sxs-lookup"><span data-stu-id="08847-127">*Views\Account* (and all the files in this directory)</span></span>

![Soln Exp](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image5.png)

<span data-ttu-id="08847-129">Modificare `<div>` il `logindisplay` <em>&quot;</em>&quot; <em> \_file Layout.cshtml</em> e sostituire il markup all'interno dell'elemento denominato con il messaggio Login Disabled .</span><span class="sxs-lookup"><span data-stu-id="08847-129">Edit the <em>\_Layout.cshtml</em> file and replace the markup inside the `<div>` element named `logindisplay` with the message <em>&quot;</em>Login Disabled&quot;.</span></span> <span data-ttu-id="08847-130">L'esempio seguente mostra il nuovo markup:The following example shows the new markup:</span><span class="sxs-lookup"><span data-stu-id="08847-130">The following example shows the new markup:</span></span>

[!code-cshtml[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample1.cshtml)]

## <a name="adding-the-model"></a><span data-ttu-id="08847-131">Aggiunta del modello</span><span class="sxs-lookup"><span data-stu-id="08847-131">Adding the Model</span></span>

<span data-ttu-id="08847-132">In **Esplora soluzioni**fare clic con il pulsante destro del mouse sulla cartella *Modelli* , scegliere **Aggiungi**, quindi **Classe**.</span><span class="sxs-lookup"><span data-stu-id="08847-132">In **Solution Explorer**, right-click the *Models* folder, select **Add**, and then click **Class**.</span></span>

![Classe New User Mdl](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image6.png)

<span data-ttu-id="08847-134">Denominare la classe `UserModel`.</span><span class="sxs-lookup"><span data-stu-id="08847-134">Name the class `UserModel`.</span></span> <span data-ttu-id="08847-135">Sostituire il contenuto del file *UserModel* con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="08847-135">Replace the contents of the *UserModel* file with the following code:</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample2.cs)]

<span data-ttu-id="08847-136">La `UserModel` classe rappresenta gli utenti.</span><span class="sxs-lookup"><span data-stu-id="08847-136">The `UserModel` class represents users.</span></span> <span data-ttu-id="08847-137">Ogni membro della classe è annotato con l'attributo [Required](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) dello spazio dei nomi [DataAnnotations.](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.aspx)</span><span class="sxs-lookup"><span data-stu-id="08847-137">Each member of the class is annotated with the [Required](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) attribute from the [DataAnnotations](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.aspx) namespace.</span></span> <span data-ttu-id="08847-138">Gli attributi nello spazio dei nomi [DataAnnotations](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.aspx) forniscono la convalida automatica lato client e lato server per le applicazioni Web.</span><span class="sxs-lookup"><span data-stu-id="08847-138">The attributes in the [DataAnnotations](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.aspx) namespace provide automatic client- and server-side validation for web applications.</span></span>

<span data-ttu-id="08847-139">Aprire `HomeController` la classe `using` e aggiungere una direttiva in modo da poter accedere alle classi `UserModel` e `Users` :</span><span class="sxs-lookup"><span data-stu-id="08847-139">Open the `HomeController` class and add a `using` directive so that you can access the `UserModel` and `Users` classes:</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample3.cs)]

<span data-ttu-id="08847-140">Subito dopo `HomeController` la dichiarazione, aggiungere il `Users` commento seguente e il riferimento a una classe:</span><span class="sxs-lookup"><span data-stu-id="08847-140">Just after the `HomeController` declaration, add the following comment and the reference to a `Users` class:</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample4.cs)]

<span data-ttu-id="08847-141">La `Users` classe è un archivio dati in memoria semplificato che verrà usato in questa esercitazione.</span><span class="sxs-lookup"><span data-stu-id="08847-141">The `Users` class is a simplified, in-memory data store that you'll use in this tutorial.</span></span> <span data-ttu-id="08847-142">In un'applicazione reale si utilizzerebbe un database per archiviare le informazioni utente.</span><span class="sxs-lookup"><span data-stu-id="08847-142">In a real application you would use a database to store user information.</span></span> <span data-ttu-id="08847-143">Nell'esempio seguente `HomeController` vengono illustrate le prime righe del file:</span><span class="sxs-lookup"><span data-stu-id="08847-143">The first few lines of the `HomeController` file are shown in the following example:</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample5.cs)]

<span data-ttu-id="08847-144">Compilare l'applicazione in modo che il modello utente sia disponibile per la procedura guidata di scaffolding nel passaggio successivo.</span><span class="sxs-lookup"><span data-stu-id="08847-144">Build the application so that the user model will be available to the scaffolding wizard in the next step.</span></span>

## <a name="creating-the-default-view"></a><span data-ttu-id="08847-145">Creazione della vista di default</span><span class="sxs-lookup"><span data-stu-id="08847-145">Creating the Default View</span></span>

<span data-ttu-id="08847-146">Il passaggio successivo consiste nell'aggiungere un metodo di azione e una visualizzazione per visualizzare gli utenti.</span><span class="sxs-lookup"><span data-stu-id="08847-146">The next step is to add an action method and view to display the users.</span></span>

<span data-ttu-id="08847-147">Eliminare il file *esistente Views,Home-Index.*</span><span class="sxs-lookup"><span data-stu-id="08847-147">Delete the existing *Views\Home\Index* file.</span></span> <span data-ttu-id="08847-148">Verrà creato un nuovo file *di indice* per visualizzare gli utenti.</span><span class="sxs-lookup"><span data-stu-id="08847-148">You will create a new *Index* file to display the users.</span></span>

<span data-ttu-id="08847-149">Nella `HomeController` classe sostituire il contenuto `Index` del metodo con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="08847-149">In the `HomeController` class, replace the contents of the `Index` method with the following code:</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample6.cs)]

<span data-ttu-id="08847-150">Fare clic con `Index` il pulsante destro del mouse all'interno del metodo, quindi **scegliere Aggiungi visualizzazione**.</span><span class="sxs-lookup"><span data-stu-id="08847-150">Right-click inside the `Index` method and then click **Add View**.</span></span>

![Aggiungi vista](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image7.png)

<span data-ttu-id="08847-152">Selezionare l'opzione **Crea una vista fortemente tipata.**</span><span class="sxs-lookup"><span data-stu-id="08847-152">Select the **Create a strongly-typed view** option.</span></span> <span data-ttu-id="08847-153">Per **Visualizza classe dati**, selezionare **Mvc3Razor.Models.UserModel**.</span><span class="sxs-lookup"><span data-stu-id="08847-153">For **View data class**, select **Mvc3Razor.Models.UserModel**.</span></span> <span data-ttu-id="08847-154">Se **mvc3Razor.Models.UserModel** non viene visualizzato nella casella **Visualizza classe di dati,** è necessario compilare il progetto. Assicurarsi che il motore di visualizzazione sia impostato su **Razor**.</span><span class="sxs-lookup"><span data-stu-id="08847-154">(If you don't see **Mvc3Razor.Models.UserModel** in the **View data class** box, you need to build the project.) Make sure that the view engine is set to **Razor**.</span></span> <span data-ttu-id="08847-155">Impostare **Visualizza contenuto** su **Elenco,** quindi fare clic su **Aggiungi**.</span><span class="sxs-lookup"><span data-stu-id="08847-155">Set **View content** to **List** and then click **Add**.</span></span>

![Aggiungi visualizzazione indice](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image8.png)

<span data-ttu-id="08847-157">La nuova visualizzazione esegue automaticamente lo scaffolding `Index` dei dati utente passati alla visualizzazione.</span><span class="sxs-lookup"><span data-stu-id="08847-157">The new view automatically scaffolds the user data that's passed to the `Index` view.</span></span> <span data-ttu-id="08847-158">Esaminare il file *Views,Home o Index* appena generato.</span><span class="sxs-lookup"><span data-stu-id="08847-158">Examine the newly generated *Views\Home\Index* file.</span></span> <span data-ttu-id="08847-159">I collegamenti **Crea nuovo**, **Modifica**, **Dettagli**ed Eliminazione non **funzionano,** ma il resto della pagina è funzionale.</span><span class="sxs-lookup"><span data-stu-id="08847-159">The **Create New**, **Edit**, **Details**, and **Delete** links don't work, but the rest of the page is functional.</span></span> <span data-ttu-id="08847-160">Eseguire la pagina.</span><span class="sxs-lookup"><span data-stu-id="08847-160">Run the page.</span></span> <span data-ttu-id="08847-161">Viene visualizzato un elenco di utenti.</span><span class="sxs-lookup"><span data-stu-id="08847-161">You see a list of users.</span></span>

![Pagina Indice](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image9.png)

<span data-ttu-id="08847-163">Aprire il file *Index.cshtml* e sostituire `ActionLink` il markup per **Modifica**, **Dettagli**ed **Elimina** con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="08847-163">Open the *Index.cshtml* file and replace the `ActionLink` markup for **Edit**, **Details**, and **Delete** with the following code:</span></span>

[!code-cshtml[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample7.cshtml)]

<span data-ttu-id="08847-164">Il nome utente viene utilizzato come ID per trovare il record selezionato nei collegamenti **Modifica**, **Dettagli**ed **Elimina.**</span><span class="sxs-lookup"><span data-stu-id="08847-164">The user name is used as the ID to find the selected record in the **Edit**, **Details**, and **Delete** links.</span></span>

## <a name="creating-the-details-view"></a><span data-ttu-id="08847-165">Creazione della vista Dettagli</span><span class="sxs-lookup"><span data-stu-id="08847-165">Creating the Details View</span></span>

<span data-ttu-id="08847-166">Il passaggio successivo `Details` consiste nell'aggiungere un metodo di azione e una visualizzazione per visualizzare i dettagli dell'utente.</span><span class="sxs-lookup"><span data-stu-id="08847-166">The next step is to add a `Details` action method and view in order to display user details.</span></span>

![Dettagli](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image10.png)

<span data-ttu-id="08847-168">Aggiungere il `Details` seguente metodo al controller principale:</span><span class="sxs-lookup"><span data-stu-id="08847-168">Add the following `Details` method to the home controller:</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample8.cs)]

<span data-ttu-id="08847-169">Fare clic con `Details` il pulsante destro del mouse all'interno del metodo, quindi <strong>scegliere Aggiungi visualizzazione</strong>.</span><span class="sxs-lookup"><span data-stu-id="08847-169">Right-click inside the `Details` method and then select <strong>Add View</strong>.</span></span> <span data-ttu-id="08847-170">Verificare che la casella <strong>Visualizza classe di dati</strong> contenga <strong>Mvc3Razor.Models.UserModel</strong><em>.</em></span><span class="sxs-lookup"><span data-stu-id="08847-170">Verify that the <strong>View data class</strong> box contains <strong>Mvc3Razor.Models.UserModel</strong><em>.</em></span></span> <span data-ttu-id="08847-171">Impostare <strong>Visualizza contenuto</strong> su <strong>Dettagli</strong> e quindi fare clic su <strong>Aggiungi</strong>.</span><span class="sxs-lookup"><span data-stu-id="08847-171">Set <strong>View content</strong> to <strong>Details</strong> and then click <strong>Add</strong>.</span></span>

![Aggiungere la visualizzazione dei dettagli](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image11.png)

<span data-ttu-id="08847-173">Eseguire l'applicazione e selezionare un collegamento ai dettagli.</span><span class="sxs-lookup"><span data-stu-id="08847-173">Run the application and select a details link.</span></span> <span data-ttu-id="08847-174">Lo scaffolding automatico mostra ogni proprietà nel modello.</span><span class="sxs-lookup"><span data-stu-id="08847-174">The automatic scaffolding shows each property in the model.</span></span>

![Dettagli](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image12.png)

## <a name="creating-the-edit-view"></a><span data-ttu-id="08847-176">Creazione della vista di modifica</span><span class="sxs-lookup"><span data-stu-id="08847-176">Creating the Edit View</span></span>

<span data-ttu-id="08847-177">Aggiungere il `Edit` metodo seguente al controller principale.</span><span class="sxs-lookup"><span data-stu-id="08847-177">Add the following `Edit` method to the home controller.</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample9.cs)]

<span data-ttu-id="08847-178">Aggiungere una visualizzazione come nei passaggi precedenti, ma impostare **Visualizza contenuto** su **Modifica**.</span><span class="sxs-lookup"><span data-stu-id="08847-178">Add a view as in the previous steps, but set **View content** to **Edit**.</span></span>

![Aggiungi visualizzazione di modifica](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image13.png)

<span data-ttu-id="08847-180">Eseguire l'applicazione e modificare il nome e il cognome di uno degli utenti.</span><span class="sxs-lookup"><span data-stu-id="08847-180">Run the application and edit the first and last name of one of the users.</span></span> <span data-ttu-id="08847-181">Se si violano i `DataAnnotation` vincoli `UserModel` applicati alla classe, quando si invia il modulo verranno visualizzati errori di convalida generati dal codice server.</span><span class="sxs-lookup"><span data-stu-id="08847-181">If you violate any `DataAnnotation` constraints that have been applied to the `UserModel` class, when you submit the form, you will see validation errors that are produced by server code.</span></span> <span data-ttu-id="08847-182">Ad esempio, se si &quot;modifica&quot; &quot;il&quot;nome Ann in A , quando si invia il modulo, nel modulo verrà visualizzato il seguente errore:</span><span class="sxs-lookup"><span data-stu-id="08847-182">For example, if you change the first name &quot;Ann&quot; to &quot;A&quot;, when you submit the form, the following error is displayed on the form:</span></span>

`The field First Name must be a string with a minimum length of 3 and a maximum length of 8.`

<span data-ttu-id="08847-183">In questa esercitazione il nome utente verrà trattato come chiave primaria.</span><span class="sxs-lookup"><span data-stu-id="08847-183">In this tutorial, you're treating the user name as the primary key.</span></span> <span data-ttu-id="08847-184">Pertanto, la proprietà del nome utente non può essere modificata.</span><span class="sxs-lookup"><span data-stu-id="08847-184">Therefore, the user name property cannot be changed.</span></span> <span data-ttu-id="08847-185">Nel file *Edit.cshtml,* subito `Html.BeginForm` dopo l'istruzione, impostare il nome utente come campo nascosto.</span><span class="sxs-lookup"><span data-stu-id="08847-185">In the *Edit.cshtml* file, just after the `Html.BeginForm` statement, set the user name to be a hidden field.</span></span> <span data-ttu-id="08847-186">In questo modo la proprietà da passare nel modello.</span><span class="sxs-lookup"><span data-stu-id="08847-186">This causes the property to be passed in the model.</span></span> <span data-ttu-id="08847-187">Il frammento di codice `Hidden` seguente mostra la posizione dell'istruzione:</span><span class="sxs-lookup"><span data-stu-id="08847-187">The following code fragment shows the placement of the `Hidden` statement:</span></span>

[!code-cshtml[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample10.cshtml)]

<span data-ttu-id="08847-188">Sostituire `TextBoxFor` il `ValidationMessageFor` e markup per `DisplayFor` il nome utente con una chiamata.</span><span class="sxs-lookup"><span data-stu-id="08847-188">Replace the `TextBoxFor` and `ValidationMessageFor` markup for the user name with a `DisplayFor` call.</span></span> <span data-ttu-id="08847-189">Il `DisplayFor` metodo visualizza la proprietà come elemento di sola lettura.</span><span class="sxs-lookup"><span data-stu-id="08847-189">The `DisplayFor` method displays the property as a read-only element.</span></span> <span data-ttu-id="08847-190">Nell'esempio seguente viene illustrato il markup completato.</span><span class="sxs-lookup"><span data-stu-id="08847-190">The following example shows the completed markup.</span></span> <span data-ttu-id="08847-191">`TextBoxFor` L'originale `ValidationMessageFor` e le chiamate sono commentati con i caratteri`@* *@`di commento di inizio e fine Razor ( )</span><span class="sxs-lookup"><span data-stu-id="08847-191">The original `TextBoxFor` and `ValidationMessageFor` calls are commented out with the Razor begin-comment and end-comment characters (`@* *@`)</span></span>

[!code-cshtml[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample11.cshtml)]

## <a name="enabling-client-side-validation"></a><span data-ttu-id="08847-192">Abilitazione della convalida lato clientEnabling Client-Side Validation</span><span class="sxs-lookup"><span data-stu-id="08847-192">Enabling Client-Side Validation</span></span>

<span data-ttu-id="08847-193">Per abilitare la convalida lato client in ASP.NET MVC 3, è necessario impostare due flag ed è necessario includere tre file JavaScript.</span><span class="sxs-lookup"><span data-stu-id="08847-193">To enable client-side validation in ASP.NET MVC 3, you must set two flags and you must include three JavaScript files.</span></span>

<span data-ttu-id="08847-194">Aprire il file *Web.config* dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="08847-194">Open the application's *Web.config* file.</span></span> <span data-ttu-id="08847-195">`that ClientValidationEnabled` Verificare `UnobtrusiveJavaScriptEnabled` e sono impostati su true nelle impostazioni dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="08847-195">Verify `that ClientValidationEnabled` and `UnobtrusiveJavaScriptEnabled` are set to true in the application settings.</span></span> <span data-ttu-id="08847-196">Nel frammento seguente del file *Web.config* radice vengono visualizzate le impostazioni corrette:</span><span class="sxs-lookup"><span data-stu-id="08847-196">The following fragment from the root *Web.config* file shows the correct settings:</span></span>

[!code-xml[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample12.xml)]

<span data-ttu-id="08847-197">L'impostazione `UnobtrusiveJavaScriptEnabled` su true consente la convalida client Ajax discreta e discreta.</span><span class="sxs-lookup"><span data-stu-id="08847-197">Setting `UnobtrusiveJavaScriptEnabled` to true enables unobtrusive Ajax and unobtrusive client validation.</span></span> <span data-ttu-id="08847-198">Quando si utilizza la convalida discreta, le regole di convalida vengono trasformate in attributi HTML5.</span><span class="sxs-lookup"><span data-stu-id="08847-198">When you use unobtrusive validation, the validation rules are turned into HTML5 attributes.</span></span> <span data-ttu-id="08847-199">I nomi degli attributi HTML5 possono essere costituiti solo da lettere minuscole, numeri e trattini.</span><span class="sxs-lookup"><span data-stu-id="08847-199">HTML5 attribute names can consist of only lowercase letters, numbers, and dashes.</span></span>

<span data-ttu-id="08847-200">L'impostazione `ClientValidationEnabled` su true consente la convalida lato client.</span><span class="sxs-lookup"><span data-stu-id="08847-200">Setting `ClientValidationEnabled` to true enables client-side validation.</span></span> <span data-ttu-id="08847-201">Impostando queste chiavi nel file *Web.config* dell'applicazione, si abilita la convalida client e JavaScript discreto per l'intera applicazione.</span><span class="sxs-lookup"><span data-stu-id="08847-201">By setting these keys in the application *Web.config* file, you enable client validation and unobtrusive JavaScript for the entire application.</span></span> <span data-ttu-id="08847-202">È inoltre possibile abilitare o disabilitare queste impostazioni in singole visualizzazioni o nei metodi del controller utilizzando il codice seguente:You can also enable or disable these settings in individual views or in controller methods using the following code:</span><span class="sxs-lookup"><span data-stu-id="08847-202">You can also enable or disable these settings in individual views or in controller methods using the following code:</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample13.cs)]

<span data-ttu-id="08847-203">È inoltre necessario includere diversi file JavaScript nella visualizzazione sottoposta a rendering.</span><span class="sxs-lookup"><span data-stu-id="08847-203">You also need to include several JavaScript files in the rendered view.</span></span> <span data-ttu-id="08847-204">Un modo semplice per includere il codice JavaScript in tutte le visualizzazioni consiste nell'aggiungerle al file *_Layout.cshtml\\di Views.*</span><span class="sxs-lookup"><span data-stu-id="08847-204">An easy way to include the JavaScript in all views is to add them to the *Views\Shared\\_Layout.cshtml* file.</span></span> <span data-ttu-id="08847-205">Sostituire `<head>` l'elemento del \* \_file Layout.cshtml\* con il codice seguente:</span><span class="sxs-lookup"><span data-stu-id="08847-205">Replace the `<head>` element of the *\_Layout.cshtml* file with the following code:</span></span>

[!code-cshtml[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample14.cshtml)]

<span data-ttu-id="08847-206">I primi due script jQuery sono ospitati dalla Microsoft Ajax Content Delivery Network (CDN).</span><span class="sxs-lookup"><span data-stu-id="08847-206">The first two jQuery scripts are hosted by the Microsoft Ajax Content Delivery Network (CDN).</span></span> <span data-ttu-id="08847-207">Sfruttando la rete CDN di Microsoft Ajax, è possibile migliorare significativamente le prestazioni di primo impatto delle applicazioni.</span><span class="sxs-lookup"><span data-stu-id="08847-207">By taking advantage of the Microsoft Ajax CDN, you can significantly improve the first-hit performance of your applications.</span></span>

<span data-ttu-id="08847-208">Eseguire l'applicazione e fare clic su un collegamento di modifica.</span><span class="sxs-lookup"><span data-stu-id="08847-208">Run the application and click an edit link.</span></span> <span data-ttu-id="08847-209">Visualizzare l'origine della pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="08847-209">View the page's source in the browser.</span></span> <span data-ttu-id="08847-210">L'origine del browser mostra `data-val` molti attributi del modulo (per la convalida dei dati).</span><span class="sxs-lookup"><span data-stu-id="08847-210">The browser source shows many attributes of the form `data-val` (for data validation).</span></span> <span data-ttu-id="08847-211">Quando la convalida client e JavaScript discreto è abilitato, i campi `data-val="true"` di input con una regola di convalida client contengono l'attributo per attivare la convalida client discreta.</span><span class="sxs-lookup"><span data-stu-id="08847-211">When client validation and unobtrusive JavaScript is enabled, input fields with a client-validation rule contain the `data-val="true"` attribute to trigger unobtrusive client validation.</span></span> <span data-ttu-id="08847-212">Ad esempio, `City` il campo nel modello è stato decorato con l'attributo [Obbligatorio,](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) che determina il codice HTML illustrato nell'esempio seguente:</span><span class="sxs-lookup"><span data-stu-id="08847-212">For example, the `City` field in the model was decorated with the [Required](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx) attribute, which results in the HTML shown in the following example:</span></span>

[!code-cshtml[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample15.cshtml)]

<span data-ttu-id="08847-213">Per ogni regola di convalida client, viene `data-val-rulename="message"`aggiunto un attributo con il modulo .</span><span class="sxs-lookup"><span data-stu-id="08847-213">For each client-validation rule, an attribute is added that has the form `data-val-rulename="message"`.</span></span> <span data-ttu-id="08847-214">Utilizzando `City` l'esempio di campo illustrato in precedenza, la regola di convalida client richiesta genera l'attributo `data-val-required` e il messaggio &quot;Il campo Città è obbligatorio.&quot;</span><span class="sxs-lookup"><span data-stu-id="08847-214">Using the `City` field example shown earlier, the required client-validation rule generates the `data-val-required` attribute and the message &quot;The City field is required&quot;.</span></span> <span data-ttu-id="08847-215">Eseguire l'applicazione, modificare uno degli `City` utenti e cancellare il campo.</span><span class="sxs-lookup"><span data-stu-id="08847-215">Run the application, edit one of the users, and clear the `City` field.</span></span> <span data-ttu-id="08847-216">Quando si esce dalla scheda all'esterno del campo, viene visualizzato un messaggio di errore di convalida lato client.</span><span class="sxs-lookup"><span data-stu-id="08847-216">When you tab out of the field, you see a client-side validation error message.</span></span>

![Città richiesta](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image14.png)

<span data-ttu-id="08847-218">Analogamente, per ogni parametro nella regola di convalida client, viene aggiunto un attributo con il formato `data-val-rulename-paramname=paramvalue`.</span><span class="sxs-lookup"><span data-stu-id="08847-218">Similarly, for each parameter in the client-validation rule, an attribute is added that has the form `data-val-rulename-paramname=paramvalue`.</span></span> <span data-ttu-id="08847-219">Ad esempio, `FirstName` la proprietà viene annotata con il [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) attributo e specifica una lunghezza minima di 3 e una lunghezza massima di 8.</span><span class="sxs-lookup"><span data-stu-id="08847-219">For example, the `FirstName` property is annotated with the [StringLength](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx) attribute and specifies a minimum length of 3 and a maximum length of 8.</span></span> <span data-ttu-id="08847-220">La regola di `length` convalida dei `max` dati denominata ha il nome del parametro e il valore del parametro 8.The data validation rule named has the parameter name and the parameter value 8.</span><span class="sxs-lookup"><span data-stu-id="08847-220">The data validation rule named `length` has the parameter name `max` and the parameter value 8.</span></span> <span data-ttu-id="08847-221">Di seguito viene illustrato il `FirstName` codice HTML generato per il campo quando si modifica uno degli utenti:</span><span class="sxs-lookup"><span data-stu-id="08847-221">The following shows the HTML that is generated for the `FirstName` field when you edit one of the users:</span></span>

[!code-cshtml[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample16.cshtml)]

<span data-ttu-id="08847-222">Per ulteriori informazioni sulla convalida client discreta, vedere la voce [Unabtrusive Client Validation in ASP.NET MVC 3](http://bradwilson.typepad.com/blog/2010/10/mvc3-unobtrusive-validation.html) nel blog di Brad Wilson.</span><span class="sxs-lookup"><span data-stu-id="08847-222">For more information about unobtrusive client validation, see the entry [Unobtrusive Client Validation in ASP.NET MVC 3](http://bradwilson.typepad.com/blog/2010/10/mvc3-unobtrusive-validation.html) in Brad Wilson's blog.</span></span>

> [!NOTE]
> <span data-ttu-id="08847-223">In ASP.NET MVC 3 Beta, a volte è necessario inviare il modulo per avviare la convalida lato client.</span><span class="sxs-lookup"><span data-stu-id="08847-223">In ASP.NET MVC 3 Beta, you sometimes need to submit the form in order to start client-side validation.</span></span> <span data-ttu-id="08847-224">Questo potrebbe essere modificato per la versione finale.</span><span class="sxs-lookup"><span data-stu-id="08847-224">This might be changed for the final release.</span></span>

## <a name="creating-the-create-view"></a><span data-ttu-id="08847-225">Creazione della vista Crea</span><span class="sxs-lookup"><span data-stu-id="08847-225">Creating the Create View</span></span>

<span data-ttu-id="08847-226">Il passaggio successivo `Create` consiste nell'aggiungere un metodo di azione e una visualizzazione per consentire all'utente di creare un nuovo utente.</span><span class="sxs-lookup"><span data-stu-id="08847-226">The next step is to add a `Create` action method and view in order to enable the user to create a new user.</span></span> <span data-ttu-id="08847-227">Aggiungere il `Create` seguente metodo al controller principale:</span><span class="sxs-lookup"><span data-stu-id="08847-227">Add the following `Create` method to the home controller:</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample17.cs)]

<span data-ttu-id="08847-228">Aggiungere una visualizzazione come nei passaggi precedenti, ma impostare **Visualizza contenuto** su **Crea**.</span><span class="sxs-lookup"><span data-stu-id="08847-228">Add a view as in the previous steps, but set **View content** to **Create**.</span></span>

![Create View](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image15.png)

<span data-ttu-id="08847-230">Eseguire l'applicazione, selezionare il collegamento **Crea** e aggiungere un nuovo utente.</span><span class="sxs-lookup"><span data-stu-id="08847-230">Run the application, select the **Create** link, and add a new user.</span></span> <span data-ttu-id="08847-231">Il `Create` metodo sfrutta automaticamente la convalida lato client e lato server.</span><span class="sxs-lookup"><span data-stu-id="08847-231">The `Create` method automatically takes advantage of client-side and server-side validation.</span></span> <span data-ttu-id="08847-232">Provare a immettere un nome utente &quot;contenente&quot;spazi vuoti, ad esempio Ben X .</span><span class="sxs-lookup"><span data-stu-id="08847-232">Try to enter a user name that contains white space, such as &quot;Ben X&quot;.</span></span> <span data-ttu-id="08847-233">Quando si esce dal campo del nome utente,`White space is not allowed`viene visualizzato un errore di convalida lato client ( ).</span><span class="sxs-lookup"><span data-stu-id="08847-233">When you tab out of the user name field, a client-side validation error (`White space is not allowed`) is displayed.</span></span>

## <a name="add-the-delete-method"></a><span data-ttu-id="08847-234">Aggiungere il metodo Delete</span><span class="sxs-lookup"><span data-stu-id="08847-234">Add the Delete method</span></span>

<span data-ttu-id="08847-235">Per completare l'esercitazione, aggiungere il metodo seguente `Delete` al controller principale:</span><span class="sxs-lookup"><span data-stu-id="08847-235">To complete the tutorial, add the following `Delete` method to the home controller:</span></span>

[!code-csharp[Main](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/samples/sample18.cs)]

<span data-ttu-id="08847-236">Aggiungere `Delete` una visualizzazione come nei passaggi precedenti, impostando **Visualizza contenuto** su **Elimina**.</span><span class="sxs-lookup"><span data-stu-id="08847-236">Add a `Delete` view as in the previous steps, setting **View content** to **Delete**.</span></span>

![Eliminare la visualizzazione](creating-a-mvc-3-application-with-razor-and-unobtrusive-javascript/_static/image16.png)

<span data-ttu-id="08847-238">Si dispone ora di un'applicazione MVC 3 ASP.NET semplice ma completamente funzionale con convalida.</span><span class="sxs-lookup"><span data-stu-id="08847-238">You now have a simple but fully functional ASP.NET MVC 3 application with validation.</span></span>
