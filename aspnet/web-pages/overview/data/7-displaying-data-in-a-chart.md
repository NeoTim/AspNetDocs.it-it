---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Visualizzazione dei dati in un grafico con ASP.NET pagine Web (Razor) Documenti Microsoft
author: rick-anderson
description: Questo capitolo spiega come visualizzare i dati in un grafico. Nei capitoli precedenti è stato illustrato come visualizzare i dati manualmente e in una griglia. Questo capitolo spiega...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: ad55d4898ee39e0239ef8b0bd5eea6387af3c816
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542885"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="69886-105">Visualizzazione di dati in un grafico con ASP.NET pagine Web (Razor)</span><span class="sxs-lookup"><span data-stu-id="69886-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="69886-106">da parte [di Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="69886-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="69886-107">In questo articolo viene illustrato come utilizzare un grafico per visualizzare i dati `Chart` in un sito Web di pagine Web ASP.NET (Razor) utilizzando l'helper.</span><span class="sxs-lookup"><span data-stu-id="69886-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="69886-108">**Cosa imparerai**:</span><span class="sxs-lookup"><span data-stu-id="69886-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="69886-109">Come visualizzare i dati in un grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="69886-110">Come applicare uno stile ai grafici utilizzando i temi incorporati.</span><span class="sxs-lookup"><span data-stu-id="69886-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="69886-111">Come salvare i grafici e come memorizzarli nella cache per migliorare le prestazioni.</span><span class="sxs-lookup"><span data-stu-id="69886-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="69886-112">Di seguito sono riportate le funzionalità di programmazione ASP.NET introdotte nell'articolo:</span><span class="sxs-lookup"><span data-stu-id="69886-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="69886-113">L'aiutante. `Chart`</span><span class="sxs-lookup"><span data-stu-id="69886-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="69886-114">Le informazioni contenute in questo articolo si applicano a ASP.NET pagine Web 1.0 e pagine Web 2.</span><span class="sxs-lookup"><span data-stu-id="69886-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="69886-115">L'helper del grafico</span><span class="sxs-lookup"><span data-stu-id="69886-115">The Chart Helper</span></span>

<span data-ttu-id="69886-116">Quando si desidera visualizzare i dati in `Chart` forma grafica, è possibile utilizzare l'helper.</span><span class="sxs-lookup"><span data-stu-id="69886-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="69886-117">L'helper `Chart` può eseguire il rendering di un'immagine che visualizza i dati in diversi tipi di grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="69886-118">Supporta molte opzioni per la formattazione e l'etichettatura.</span><span class="sxs-lookup"><span data-stu-id="69886-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="69886-119">L'helper `Chart` può eseguire il rendering di più di 30 tipi di grafici, inclusi tutti i tipi di grafici che potrebbero avere familiarità con Microsoft Excel o altri strumenti &#8212; grafici ad area, grafici a barre, istogrammi, grafici a linee e grafici a torta, insieme a grafici più specializzati come i grafici azionari.</span><span class="sxs-lookup"><span data-stu-id="69886-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="69886-120">**Grafico ad area** Descrizione: Immagine del tipo di grafico ad area ![](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="69886-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="69886-121">**Grafico a barre** ![Descrizione: Immagine del tipo di grafico a barre](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="69886-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="69886-122">**Istogramma** ![Descrizione: Immagine del tipo di istogramma](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="69886-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="69886-123">**Grafico a linee** ![Descrizione: Immagine del tipo di grafico a linee](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="69886-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="69886-124">**Grafico a torta** ![Descrizione: Immagine del tipo di grafico a torta](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="69886-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="69886-125">**Stock chart** ![Descrizione: Immagine del tipo di grafico azionario](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="69886-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="69886-126">Elementi del grafico</span><span class="sxs-lookup"><span data-stu-id="69886-126">Chart Elements</span></span>

<span data-ttu-id="69886-127">I grafici mostrano dati ed elementi aggiuntivi come legende, assi, serie e così via.</span><span class="sxs-lookup"><span data-stu-id="69886-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="69886-128">L'immagine seguente mostra molti degli elementi del grafico `Chart` che è possibile personalizzare quando si utilizza l'helper.</span><span class="sxs-lookup"><span data-stu-id="69886-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="69886-129">In questo articolo viene illustrato come impostare alcuni (non tutti) di questi elementi.</span><span class="sxs-lookup"><span data-stu-id="69886-129">This article shows you how to set some (not all) of these elements.</span></span>

![Descrizione: Immagine che mostra gli elementi del grafico](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="69886-131">Creazione di un grafico dai datiCreating a Chart from Data</span><span class="sxs-lookup"><span data-stu-id="69886-131">Creating a Chart from Data</span></span>

<span data-ttu-id="69886-132">I dati visualizzati in un grafico possono provengono da una matrice, dai risultati restituiti da un database o dai dati in un file XML.</span><span class="sxs-lookup"><span data-stu-id="69886-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="69886-133">Utilizzo di un array</span><span class="sxs-lookup"><span data-stu-id="69886-133">Using an Array</span></span>

<span data-ttu-id="69886-134">Come illustrato in [Introduzione alla programmazione di pagine Web ASP.NET utilizzando la sintassi Razor](https://go.microsoft.com/fwlink/?LinkId=202890), una matrice consente di archiviare un insieme di elementi simili in un'unica variabile.</span><span class="sxs-lookup"><span data-stu-id="69886-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="69886-135">È possibile utilizzare le matrici per contenere i dati che si desidera includere nel grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="69886-136">In questa procedura viene illustrato come creare un grafico dai dati nelle matrici, utilizzando il tipo di grafico predefinito.</span><span class="sxs-lookup"><span data-stu-id="69886-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="69886-137">Viene inoltre illustrato come visualizzare il grafico all'interno della pagina.</span><span class="sxs-lookup"><span data-stu-id="69886-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="69886-138">Creare un nuovo file denominato *ChartArrayBasic.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="69886-139">Sostituire il contenuto esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="69886-140">Il codice crea innanzitutto un nuovo grafico e ne imposta la larghezza e l'altezza.</span><span class="sxs-lookup"><span data-stu-id="69886-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="69886-141">Specificare il titolo del `AddTitle` grafico utilizzando il metodo .</span><span class="sxs-lookup"><span data-stu-id="69886-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="69886-142">Per aggiungere dati, `AddSeries` utilizzare il metodo .</span><span class="sxs-lookup"><span data-stu-id="69886-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="69886-143">In questo esempio vengono `name` `xValue`utilizzati `yValues` i `AddSeries` parametri , e del metodo .</span><span class="sxs-lookup"><span data-stu-id="69886-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="69886-144">Il `name` parametro viene visualizzato nella legenda del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="69886-145">Il `xValue` parametro contiene una matrice di dati visualizzata lungo l'asse orizzontale del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="69886-146">Il `yValues` parametro contiene una matrice di dati utilizzata per tracciare i punti verticali del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="69886-147">Il `Write` metodo esegue effettivamente il rendering del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="69886-148">In questo caso, poiché non è stato `Chart` specificato un tipo di grafico, l'helper esegue il rendering del grafico predefinito, ovvero un istogramma.</span><span class="sxs-lookup"><span data-stu-id="69886-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="69886-149">Eseguire la pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="69886-149">Run the page in the browser.</span></span> <span data-ttu-id="69886-150">Il browser visualizza il grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="69886-151">Utilizzo di una query di database per i dati del graficoUsing a Database Query for Chart Data</span><span class="sxs-lookup"><span data-stu-id="69886-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="69886-152">Se le informazioni che si desidera creare in un grafico si trova in un database, è possibile eseguire una query di database e quindi utilizzare i dati dei risultati per creare il grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="69886-153">In questa procedura viene illustrato come leggere e visualizzare i dati del database creato nell'articolo [Introduzione all'utilizzo di un database in siti di pagine Web ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="69886-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="69886-154">Aggiungere una cartella *\_App Data* alla radice del sito Web se la cartella non esiste già.</span><span class="sxs-lookup"><span data-stu-id="69886-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="69886-155">Nella cartella *App\_Data* aggiungere il file di database denominato *SmallBakery.sdf* descritto in Introduzione [all'utilizzo](https://go.microsoft.com/fwlink/?LinkId=202893)di un database in ASP.NET siti di pagine Web .</span><span class="sxs-lookup"><span data-stu-id="69886-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="69886-156">Creare un nuovo file denominato *ChartDataQuery.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="69886-157">Sostituire il contenuto esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="69886-158">Il codice apre innanzitutto il database SmallBakery `db`e lo assegna a una variabile denominata .</span><span class="sxs-lookup"><span data-stu-id="69886-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="69886-159">Questa variabile `Database` rappresenta un oggetto che può essere utilizzato per leggere e scrivere nel database.</span><span class="sxs-lookup"><span data-stu-id="69886-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="69886-160">Successivamente, il codice esegue una query SQL per ottenere il nome e il prezzo di ogni prodotto.</span><span class="sxs-lookup"><span data-stu-id="69886-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="69886-161">Il codice crea un nuovo grafico e vi passa la `DataBindTable` query di database chiamando il metodo del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="69886-162">Questo metodo accetta due `dataSource` parametri: il parametro è `xField` per i dati della query e il parametro consente di impostare la colonna di dati utilizzata per l'asse x del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="69886-163">In alternativa all'utilizzo del `DataBindTable` metodo `AddSeries` , è `Chart` possibile utilizzare il metodo dell'helper.</span><span class="sxs-lookup"><span data-stu-id="69886-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="69886-164">Il `AddSeries` metodo consente `xValue` di `yValues` impostare i parametri e .</span><span class="sxs-lookup"><span data-stu-id="69886-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="69886-165">Ad esempio, invece `DataBindTable` di usare il metodo in questo modo:</span><span class="sxs-lookup"><span data-stu-id="69886-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="69886-166">È possibile `AddSeries` utilizzare il metodo in questo modo:</span><span class="sxs-lookup"><span data-stu-id="69886-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="69886-167">Entrambi eseguono il rendering degli stessi risultati.</span><span class="sxs-lookup"><span data-stu-id="69886-167">Both render the same results.</span></span> <span data-ttu-id="69886-168">Il `AddSeries` metodo è più flessibile perché è possibile specificare `DataBindTable` il tipo di grafico e i dati in modo più esplicito, ma il metodo è più facile da usare se non è necessaria la flessibilità aggiuntiva.</span><span class="sxs-lookup"><span data-stu-id="69886-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="69886-169">Eseguire la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="69886-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="69886-170">Utilizzo di dati XML</span><span class="sxs-lookup"><span data-stu-id="69886-170">Using XML Data</span></span>

<span data-ttu-id="69886-171">La terza opzione per la creazione di grafici consiste nell'utilizzare un file XML come dati per il grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="69886-172">Ciò richiede che il file XML disponga anche di un file di schema (file*con estensione xsd)* che descrive la struttura XML.</span><span class="sxs-lookup"><span data-stu-id="69886-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="69886-173">In questa procedura viene illustrato come leggere i dati da un file XML.</span><span class="sxs-lookup"><span data-stu-id="69886-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="69886-174">Nella *cartella\_App Data* creare un nuovo file XML denominato *data.xml*.</span><span class="sxs-lookup"><span data-stu-id="69886-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="69886-175">Sostituire il codice XML esistente con il codice seguente, ovvero alcuni dati XML relativi ai dipendenti di una società fittizia.</span><span class="sxs-lookup"><span data-stu-id="69886-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="69886-176">Nella *cartella\_App Data* creare un nuovo file XML denominato *data.xsd*.</span><span class="sxs-lookup"><span data-stu-id="69886-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="69886-177">Si noti che l'estensione questa volta è *.xsd*.)</span><span class="sxs-lookup"><span data-stu-id="69886-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="69886-178">Sostituire il codice XML esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="69886-179">Nella radice del sito Web creare un nuovo file denominato *ChartDataXML.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="69886-180">Sostituire il contenuto esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="69886-181">Il codice crea `DataSet` innanzitutto un oggetto.</span><span class="sxs-lookup"><span data-stu-id="69886-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="69886-182">Questo oggetto viene utilizzato per gestire i dati letti dal file XML e organizzarlo in base alle informazioni contenute nel file di schema.</span><span class="sxs-lookup"><span data-stu-id="69886-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="69886-183">(Si noti che la parte `using SystemData`superiore del codice include l'istruzione .</span><span class="sxs-lookup"><span data-stu-id="69886-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="69886-184">Questa operazione è necessaria per poter `DataSet` lavorare con l'oggetto.</span><span class="sxs-lookup"><span data-stu-id="69886-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="69886-185">Per altre informazioni, vedere [ &quot;Utilizzo&quot; ](#SB_UsingStatements) di istruzioni e nomi completi più avanti in questo articolo.)</span><span class="sxs-lookup"><span data-stu-id="69886-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="69886-186">Successivamente, il codice `DataView` crea un oggetto basato sul set di dati.</span><span class="sxs-lookup"><span data-stu-id="69886-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="69886-187">La visualizzazione dati fornisce un oggetto che il grafico può associare a &#8212; ovvero leggere e tracciare.</span><span class="sxs-lookup"><span data-stu-id="69886-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="69886-188">Il grafico si associa ai dati utilizzando `AddSeries` il metodo , come illustrato in `xValue` precedenza durante la creazione di un grafico dei dati della matrice, ad eccezione del fatto che questa volta i parametri e `yValues` vengono impostati sull'oggetto `DataView` .</span><span class="sxs-lookup"><span data-stu-id="69886-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="69886-189">In questo esempio viene inoltre illustrato come specificare un particolare tipo di grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="69886-190">Quando i dati vengono `AddSeries` aggiunti `chartType` nel metodo, il parametro viene impostato anche per visualizzare un grafico a torta.</span><span class="sxs-lookup"><span data-stu-id="69886-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="69886-191">Eseguire la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="69886-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="69886-192">Istruzioni "Using" e nomi completi</span><span class="sxs-lookup"><span data-stu-id="69886-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="69886-193">Il .NET Framework che ASP.NET pagine Web con sintassi Razor è basato su è costituito da molte migliaia di componenti (classi).</span><span class="sxs-lookup"><span data-stu-id="69886-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="69886-194">Per renderlo gestibile per l'utilizzo con tutte queste classi, sono organizzati in *spazi dei nomi*, che sono un po 'come le librerie.</span><span class="sxs-lookup"><span data-stu-id="69886-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="69886-195">Ad esempio, `System.Web` lo spazio dei nomi contiene `System.Xml` classi che supportano la comunicazione browser/server, `System.Data` lo spazio dei nomi contiene classi utilizzate per creare e leggere file XML e lo spazio dei nomi contiene classi che consentono di utilizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="69886-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="69886-196">Per accedere a qualsiasi classe specificata in .NET Framework, il codice deve conoscere non solo il nome della classe, ma anche lo spazio dei nomi in cui si trova la classe.</span><span class="sxs-lookup"><span data-stu-id="69886-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="69886-197">Ad esempio, per utilizzare `Chart` l'helper, il `System.Web.Helpers.Chart` codice deve trovare la`System.Web.Helpers`classe , che`Chart`combina lo spazio dei nomi ( ) con il nome della classe ( ).</span><span class="sxs-lookup"><span data-stu-id="69886-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="69886-198">Questo è noto come il nome *completo* della classe &#8212; la sua posizione completa e non ambigua all'interno della vastità di .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="69886-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="69886-199">Nel codice, questo sarebbe simile al seguente:In code, this would look like the following:</span><span class="sxs-lookup"><span data-stu-id="69886-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="69886-200">Tuttavia, è ingombrante (e soggetto a errori) dover utilizzare questi nomi lunghi e completi ogni volta che si desidera fare riferimento a una classe o un helper.</span><span class="sxs-lookup"><span data-stu-id="69886-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="69886-201">Pertanto, per semplificare l'utilizzo dei nomi di classe, è possibile *importare* gli spazi dei nomi che ti interessano, che in genere è solo una manciata tra i molti spazi dei nomi in .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="69886-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="69886-202">Se è stato importato uno spazio dei nomi,`Chart`è possibile utilizzare solo`System.Web.Helpers.Chart`un nome di classe ( ) anziché il nome completo ( ).</span><span class="sxs-lookup"><span data-stu-id="69886-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="69886-203">Quando il codice viene eseguito e rileva un nome di classe, può cercare solo gli spazi dei nomi importati per trovare tale classe.</span><span class="sxs-lookup"><span data-stu-id="69886-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="69886-204">Quando si utilizza ASP.NET pagine Web con sintassi Razor per creare pagine Web, `WebPage` in genere si utilizza ogni volta lo stesso set di classi, tra cui la classe, i vari helper e così via.</span><span class="sxs-lookup"><span data-stu-id="69886-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="69886-205">Per risparmiare il lavoro di importazione degli spazi dei nomi pertinenti ogni volta che si crea un sito Web, ASP.NET è configurato in modo da importare automaticamente un set di spazi dei nomi principali per ogni sito Web.</span><span class="sxs-lookup"><span data-stu-id="69886-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="69886-206">Ecco perché non hai avuto a che fare con gli spazi dei nomi o l'importazione fino ad ora; tutte le classi con cui hai lavorato si trovano negli spazi dei nomi che sono già stati importati per l'utente.</span><span class="sxs-lookup"><span data-stu-id="69886-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="69886-207">Tuttavia, a volte è necessario lavorare con una classe che non si trovi in uno spazio dei nomi importato automaticamente.</span><span class="sxs-lookup"><span data-stu-id="69886-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="69886-208">In tal caso, è possibile utilizzare il nome completo di tale classe oppure importare manualmente lo spazio dei nomi che contiene la classe.</span><span class="sxs-lookup"><span data-stu-id="69886-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="69886-209">Per importare uno spazio `using` dei`import` nomi, utilizzare l'istruzione ( in Visual Basic), come illustrato in un esempio precedente dell'articolo.</span><span class="sxs-lookup"><span data-stu-id="69886-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="69886-210">Ad esempio, `DataSet` la classe `System.Data` si trova nello spazio dei nomi .</span><span class="sxs-lookup"><span data-stu-id="69886-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="69886-211">Lo `System.Data` spazio dei nomi non è automaticamente disponibile per ASP.NET pagine Razor.The namespace is not automatically available to ASP.NET Razor pages.</span><span class="sxs-lookup"><span data-stu-id="69886-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="69886-212">Pertanto, per `DataSet` utilizzare la classe utilizzando il nome completo, è possibile utilizzare codice simile al seguente:Therefore, to work with the class using its fully qualified name, you can use code like this:</span><span class="sxs-lookup"><span data-stu-id="69886-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="69886-213">Se è necessario `DataSet` utilizzare ripetutamente la classe è possibile importare uno spazio dei nomi come questo e quindi utilizzare solo il nome della classe nel codice:If you have to use the class repeatedly you can import a namespace like this and then use just the class name in code:</span><span class="sxs-lookup"><span data-stu-id="69886-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="69886-214">È possibile `using` aggiungere istruzioni per qualsiasi altro spazio dei nomi .NET Framework a cui si desidera fare riferimento.</span><span class="sxs-lookup"><span data-stu-id="69886-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="69886-215">Tuttavia, come indicato, non sarà necessario eseguire questa operazione spesso, perché la maggior parte delle classi che si lavorerà con sono in spazi dei nomi che vengono importati automaticamente da ASP.NET per l'utilizzo nelle pagine *.cshtml* e *.vbhtml.*</span><span class="sxs-lookup"><span data-stu-id="69886-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="69886-216">Visualizzazione di grafici all'interno di una pagina Web</span><span class="sxs-lookup"><span data-stu-id="69886-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="69886-217">Negli esempi visti finora, si crea un grafico e quindi il rendering del grafico viene eseguito direttamente nel browser come elemento grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="69886-218">In molti casi, tuttavia, si desidera visualizzare un grafico come parte di una pagina, non solo da solo nel browser.</span><span class="sxs-lookup"><span data-stu-id="69886-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="69886-219">Per farlo richiede un processo in due fasi.</span><span class="sxs-lookup"><span data-stu-id="69886-219">To do that requires a two-step process.</span></span> <span data-ttu-id="69886-220">Il primo passaggio consiste nel creare una pagina che genera il grafico, come già visto.</span><span class="sxs-lookup"><span data-stu-id="69886-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="69886-221">Il secondo passaggio consiste nel visualizzare l'immagine risultante in un'altra pagina.</span><span class="sxs-lookup"><span data-stu-id="69886-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="69886-222">Per visualizzare l'immagine, `<img>` si utilizza un elemento HTML, nello stesso modo in cui si visualizza qualsiasi immagine.</span><span class="sxs-lookup"><span data-stu-id="69886-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="69886-223">Tuttavia, anziché fare riferimento a un file `<img>` con estensione *jpg* o *png,* l'elemento fa riferimento al file con estensione *cshtml* che contiene l'helper `Chart` che crea il grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="69886-224">Quando viene eseguita `<img>` la pagina di `Chart` visualizzazione, l'elemento ottiene l'output dell'helper ed esegue il rendering del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="69886-225">Creare un file denominato *ShowChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="69886-226">Sostituire il contenuto esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="69886-227">Il codice `<img>` utilizza l'elemento per visualizzare il grafico creato in precedenza nel file *ChartArrayBasic.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="69886-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="69886-228">Eseguire la pagina Web in un browser.</span><span class="sxs-lookup"><span data-stu-id="69886-228">Run the web page in a browser.</span></span> <span data-ttu-id="69886-229">Il file *ShowChart.cshtml* visualizza l'immagine del grafico in base al codice contenuto nel file *ChartArrayBasic.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="69886-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="69886-230">Applicazione di stili a un grafico</span><span class="sxs-lookup"><span data-stu-id="69886-230">Styling a Chart</span></span>

<span data-ttu-id="69886-231">L'helper `Chart` supporta un numero elevato di opzioni che consentono di personalizzare l'aspetto del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="69886-232">È possibile impostare colori, tipi di carattere, bordi e così via.</span><span class="sxs-lookup"><span data-stu-id="69886-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="69886-233">Un modo semplice per personalizzare l'aspetto di un grafico consiste nell'utilizzare un *tema*.</span><span class="sxs-lookup"><span data-stu-id="69886-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="69886-234">I temi sono raccolte di informazioni che specificano come eseguire il rendering di un grafico utilizzando caratteri, colori, etichette, tavolozze ed effetti.</span><span class="sxs-lookup"><span data-stu-id="69886-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="69886-235">Si noti che lo stile di un grafico non indica il tipo di grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="69886-236">Nella tabella seguente sono elencati i temi incorporati.</span><span class="sxs-lookup"><span data-stu-id="69886-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="69886-237">Tema</span><span class="sxs-lookup"><span data-stu-id="69886-237">Theme</span></span> | <span data-ttu-id="69886-238">Descrizione</span><span class="sxs-lookup"><span data-stu-id="69886-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="69886-239">Visualizza le colonne rosse su uno sfondo bianco.</span><span class="sxs-lookup"><span data-stu-id="69886-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="69886-240">Visualizza colonne blu su uno sfondo sfumato blu.</span><span class="sxs-lookup"><span data-stu-id="69886-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="69886-241">Visualizza le colonne blu su uno sfondo sfumato verde.</span><span class="sxs-lookup"><span data-stu-id="69886-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="69886-242">Visualizza le colonne arancioni su uno sfondo sfumato giallo.</span><span class="sxs-lookup"><span data-stu-id="69886-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="69886-243">Visualizza le colonne rosse 3D su uno sfondo bianco.</span><span class="sxs-lookup"><span data-stu-id="69886-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="69886-244">È possibile specificare il tema da utilizzare quando si crea un nuovo grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="69886-245">Creare un nuovo file denominato *ChartStyleGreen.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="69886-246">Sostituire il contenuto esistente nella pagina con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="69886-247">Questo codice è lo stesso dell'esempio precedente che utilizza `theme` il database `Chart` per i dati, ma aggiunge il parametro quando crea l'oggetto.</span><span class="sxs-lookup"><span data-stu-id="69886-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="69886-248">Di seguito viene illustrato il codice modificato:The following shows the changed code:</span><span class="sxs-lookup"><span data-stu-id="69886-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="69886-249">Eseguire la pagina in un browser.</span><span class="sxs-lookup"><span data-stu-id="69886-249">Run the page in a browser.</span></span> <span data-ttu-id="69886-250">Vengono visualizzati gli stessi dati di prima, ma il grafico sembra più lucido:</span><span class="sxs-lookup"><span data-stu-id="69886-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="69886-251">Salvataggio di un grafico</span><span class="sxs-lookup"><span data-stu-id="69886-251">Saving a Chart</span></span>

<span data-ttu-id="69886-252">Quando si `Chart` utilizza l'helper come si è visto finora in questo articolo, l'helper ricrea il grafico da zero ogni volta che viene richiamato.</span><span class="sxs-lookup"><span data-stu-id="69886-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="69886-253">Se necessario, il codice per il grafico esegue nuovamente una query sul database o rilegge il file XML per ottenere i dati.</span><span class="sxs-lookup"><span data-stu-id="69886-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="69886-254">In alcuni casi, questa operazione può essere un'operazione complessa, ad esempio se il database su cui si sta eseguendo la query è di grandi dimensioni o se il file XML contiene molti dati.</span><span class="sxs-lookup"><span data-stu-id="69886-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="69886-255">Anche se il grafico non include molti dati, il processo di creazione dinamica di un'immagine occupa risorse del server e se molte persone richiedono la pagina o le pagine che visualizzano il grafico, potrebbe esserci un impatto sulle prestazioni del tuo sito web.</span><span class="sxs-lookup"><span data-stu-id="69886-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="69886-256">Per ridurre il potenziale impatto sulle prestazioni della creazione di un grafico, è possibile creare un grafico la prima volta che è necessario e quindi salvarlo.</span><span class="sxs-lookup"><span data-stu-id="69886-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="69886-257">Quando il grafico è necessario di nuovo, anziché rigenerarlo, è sufficiente recuperare la versione salvata ed eseguirne il rendering.</span><span class="sxs-lookup"><span data-stu-id="69886-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="69886-258">È possibile salvare un grafico nei modi seguenti:You can save a chart in these ways:</span><span class="sxs-lookup"><span data-stu-id="69886-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="69886-259">Memorizzare il grafico nella memoria del computer (sul server).</span><span class="sxs-lookup"><span data-stu-id="69886-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="69886-260">Salvare il grafico come file di immagine.</span><span class="sxs-lookup"><span data-stu-id="69886-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="69886-261">Salvare il grafico come file XML.</span><span class="sxs-lookup"><span data-stu-id="69886-261">Save the chart as an XML file.</span></span> <span data-ttu-id="69886-262">Questa opzione consente di modificare il grafico prima di salvarlo.</span><span class="sxs-lookup"><span data-stu-id="69886-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="69886-263">Memorizzazione nella cache di un graficoCaching a Chart</span><span class="sxs-lookup"><span data-stu-id="69886-263">Caching a Chart</span></span>

<span data-ttu-id="69886-264">Dopo aver creato un grafico, è possibile memorizzarlo nella cache.</span><span class="sxs-lookup"><span data-stu-id="69886-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="69886-265">La memorizzazione nella cache di un grafico significa che non deve essere ricreato se deve essere visualizzato nuovamente.</span><span class="sxs-lookup"><span data-stu-id="69886-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="69886-266">Quando si salva un grafico nella cache, si assegna una chiave che deve essere univoca per tale grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="69886-267">I grafici salvati nella cache potrebbero essere rimossi se la memoria del server è insufficiente.</span><span class="sxs-lookup"><span data-stu-id="69886-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="69886-268">Inoltre, la cache viene cancellata se l'applicazione viene riavviata per qualsiasi motivo.</span><span class="sxs-lookup"><span data-stu-id="69886-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="69886-269">Pertanto, il modo standard per lavorare con un grafico memorizzato nella cache consiste nel controllare sempre prima se è disponibile nella cache e, in caso contrario, quindi crearlo o ricrearlo.</span><span class="sxs-lookup"><span data-stu-id="69886-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="69886-270">Nella directory principale del sito Web creare un file denominato *ShowCachedChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="69886-271">Sostituire il contenuto esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="69886-272">Il `<img>` tag `src` include un attributo che punta al file *ChartSaveToCache.cshtml* e passa una chiave alla pagina come stringa di query.</span><span class="sxs-lookup"><span data-stu-id="69886-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="69886-273">La chiave contiene &quot;il&quot;valore myChartKey .</span><span class="sxs-lookup"><span data-stu-id="69886-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="69886-274">Il file *ChartSaveToCache.cshtml* contiene l'helper `Chart` che crea il grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="69886-275">Questa pagina verrà creata in un attimo.</span><span class="sxs-lookup"><span data-stu-id="69886-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="69886-276">Alla fine della pagina è presente un collegamento a una pagina denominata *ClearCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="69886-277">Questa è una pagina che creerai anche a breve.</span><span class="sxs-lookup"><span data-stu-id="69886-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="69886-278">È necessario il *ClearCache.cshtml* solo per testare la memorizzazione nella cache per questo esempio, non è un collegamento o una pagina che normalmente si include quando si lavora con grafici memorizzati nella cache.</span><span class="sxs-lookup"><span data-stu-id="69886-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="69886-279">Nella directory principale del sito Web creare un nuovo file denominato *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="69886-280">Sostituire il contenuto esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="69886-281">Il codice controlla innanzitutto se è stato passato qualcosa come valore della chiave nella stringa di query.</span><span class="sxs-lookup"><span data-stu-id="69886-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="69886-282">In tal caso, il codice tenta di leggere `GetFromCache` un grafico dalla cache chiamando il metodo e passandogli la chiave .</span><span class="sxs-lookup"><span data-stu-id="69886-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="69886-283">Se si scopre che non c'è nulla nella cache sotto quella chiave (che sarebbe accaduto la prima volta che viene richiesto il grafico), il codice crea il grafico come al solito.</span><span class="sxs-lookup"><span data-stu-id="69886-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="69886-284">Al termine del grafico, il codice lo salva `SaveToCache`nella cache chiamando .</span><span class="sxs-lookup"><span data-stu-id="69886-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="69886-285">Tale metodo richiede una chiave (in modo che il grafico può essere richiesto in un secondo momento) e la quantità di tempo che il grafico deve essere salvato nella cache.</span><span class="sxs-lookup"><span data-stu-id="69886-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="69886-286">L'ora esatta in cui un grafico deve memorizzare nella cache dipende dalla frequenza con cui si è pensato che i dati che rappresentassero potrebbero cambiare. Il `SaveToCache` metodo richiede `slidingExpiration` anche un parametro &#8212; se è impostato su true, il contatore di timeout viene reimpostato ogni volta che si accede al grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="69886-287">In questo caso, in effetti significa che la voce della cache del grafico scade 2 minuti dopo l'ultima volta che un utente ha effettuato l'accesso al grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="69886-288">L'alternativa alla scadenza variabile è la scadenza assoluta, il che significa che la voce della cache scadrebbe esattamente 2 minuti dopo essere stata inserita nella cache, indipendentemente dalla frequenza con cui è stato effettuato l'accesso.</span><span class="sxs-lookup"><span data-stu-id="69886-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="69886-289">Infine, il codice `WriteFromCache` utilizza il metodo per recuperare ed eseguire il rendering del grafico dalla cache.</span><span class="sxs-lookup"><span data-stu-id="69886-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="69886-290">Si noti che `if` questo metodo è esterno al blocco che controlla la cache, perché otterrà il grafico dalla cache se il grafico era lì per iniziare o doveva essere generato e salvato nella cache.</span><span class="sxs-lookup"><span data-stu-id="69886-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="69886-291">Si noti che `AddTitle` nell'esempio, il metodo include un timestamp.</span><span class="sxs-lookup"><span data-stu-id="69886-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="69886-292">Aggiunge la data e l'ora &#8212; `DateTime.Now` &#8212; correnti al titolo.</span><span class="sxs-lookup"><span data-stu-id="69886-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="69886-293">Creare una nuova pagina denominata *ClearCache.cshtml e sostituirne* il contenuto con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="69886-294">In questa `WebCache` pagina viene utilizzato l'helper per rimuovere il grafico memorizzato nella cache in *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="69886-295">Come notato in precedenza, normalmente non è necessario avere una pagina come questa.</span><span class="sxs-lookup"><span data-stu-id="69886-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="69886-296">Si sta creando qui solo per rendere più facile testare la memorizzazione nella cache.</span><span class="sxs-lookup"><span data-stu-id="69886-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="69886-297">Eseguire la pagina Web *ShowCachedChart.cshtml* in un browser.</span><span class="sxs-lookup"><span data-stu-id="69886-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="69886-298">Nella pagina viene visualizzata l'immagine del grafico in base al codice contenuto nel file *ChartSaveToCache.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="69886-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="69886-299">Prendere nota di ciò che il timestamp dice nel titolo del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Descrizione: Immagine del grafico di base con timestamp nel titolo del grafico](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="69886-301">Chiudere il browser.</span><span class="sxs-lookup"><span data-stu-id="69886-301">Close the browser.</span></span>
8. <span data-ttu-id="69886-302">Eseguire nuovamente il *file ShowCachedChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="69886-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="69886-303">Si noti che il timestamp è lo stesso di prima, che indica che il grafico non è stato rigenerato, ma è stato invece letto dalla cache.</span><span class="sxs-lookup"><span data-stu-id="69886-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="69886-304">In *ShowCachedChart.cshtml*fare clic sul collegamento **Cancella cache.**</span><span class="sxs-lookup"><span data-stu-id="69886-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="69886-305">Verrà visualizzato *ClearCache.cshtml*, che segnala che la cache è stata cancellata.</span><span class="sxs-lookup"><span data-stu-id="69886-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="69886-306">Fare clic sul collegamento **Torna a ShowCachedChart.cshtml** oppure rieseguire *ShowCachedChart.cshtml* da WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="69886-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="69886-307">Si noti che questa volta il timestamp è stato modificato, perché la cache è stata cancellata.</span><span class="sxs-lookup"><span data-stu-id="69886-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="69886-308">Pertanto, il codice doveva rigenerare il grafico e inserirlo nuovamente nella cache.</span><span class="sxs-lookup"><span data-stu-id="69886-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="69886-309">Salvataggio di un grafico come file di immagine</span><span class="sxs-lookup"><span data-stu-id="69886-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="69886-310">È inoltre possibile salvare un grafico come file di immagine (ad esempio, come file *con estensione jpg)* sul server.</span><span class="sxs-lookup"><span data-stu-id="69886-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="69886-311">È quindi possibile utilizzare il file di immagine come si farebbe con qualsiasi immagine.</span><span class="sxs-lookup"><span data-stu-id="69886-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="69886-312">Il vantaggio è che il file viene memorizzato anziché salvato in una cache temporanea.</span><span class="sxs-lookup"><span data-stu-id="69886-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="69886-313">È possibile salvare una nuova immagine del grafico in momenti diversi (ad esempio, ogni ora) e quindi tenere un record permanente delle modifiche che si verificano nel tempo.</span><span class="sxs-lookup"><span data-stu-id="69886-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="69886-314">Si noti che è necessario assicurarsi che l'applicazione web disponga dell'autorizzazione per salvare un file nella cartella sul server in cui si desidera inserire il file di immagine.</span><span class="sxs-lookup"><span data-stu-id="69886-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="69886-315">Nella directory principale del sito Web, creare una cartella denominata \* \_ChartFiles\* se non esiste già.</span><span class="sxs-lookup"><span data-stu-id="69886-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="69886-316">Nella directory principale del sito Web creare un nuovo file denominato *ChartSave.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="69886-317">Sostituire il contenuto esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="69886-318">Il codice verifica innanzitutto *.jpg* se il file `File.Exists` jpg esiste chiamando il metodo .</span><span class="sxs-lookup"><span data-stu-id="69886-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="69886-319">Se il file non esiste, il `Chart` codice crea una nuova da una matrice.</span><span class="sxs-lookup"><span data-stu-id="69886-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="69886-320">Questa volta, il `Save` codice chiama `path` il metodo e passa il parametro per specificare il percorso del file e il nome del file in cui salvare il grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="69886-321">Nel corpo della pagina, `<img>` un elemento utilizza il percorso per puntare al file *jpg* da visualizzare.</span><span class="sxs-lookup"><span data-stu-id="69886-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="69886-322">Eseguire il file *ChartSave.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="69886-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="69886-323">Tornare a WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="69886-323">Return to WebMatrix.</span></span> <span data-ttu-id="69886-324">Si noti che un file di immagine denominato *chart01.jpg* è stato salvato nella \* \_cartella ChartFiles.\*</span><span class="sxs-lookup"><span data-stu-id="69886-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="69886-325">Salvataggio di un grafico come file XML</span><span class="sxs-lookup"><span data-stu-id="69886-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="69886-326">Infine, è possibile salvare un grafico come file XML sul server.</span><span class="sxs-lookup"><span data-stu-id="69886-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="69886-327">Un vantaggio dell'utilizzo di questo metodo rispetto alla memorizzazione nella cache del grafico o il salvataggio del grafico in un file è che è possibile modificare il codice XML prima di visualizzare il grafico, se lo si desidera.</span><span class="sxs-lookup"><span data-stu-id="69886-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="69886-328">L'applicazione deve disporre delle autorizzazioni di lettura/scrittura per la cartella sul server in cui si desidera inserire il file di immagine.</span><span class="sxs-lookup"><span data-stu-id="69886-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="69886-329">Nella directory principale del sito Web creare un nuovo file denominato *ChartSaveXml.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="69886-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="69886-330">Sostituire il contenuto esistente con quanto segue:</span><span class="sxs-lookup"><span data-stu-id="69886-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="69886-331">Questo codice è simile al codice visualizzato in precedenza per l'archiviazione di un grafico nella cache, ad eccezione del fatto che utilizza un file XML.</span><span class="sxs-lookup"><span data-stu-id="69886-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="69886-332">Il codice verifica innanzitutto se il file `File.Exists` XML esiste chiamando il metodo .</span><span class="sxs-lookup"><span data-stu-id="69886-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="69886-333">Se il file esiste, il `Chart` codice crea un nuovo `themePath` oggetto e passa il nome del file come parametro.</span><span class="sxs-lookup"><span data-stu-id="69886-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="69886-334">In questo modo il grafico viene creato in base a qualsiasi elemento presente nel file XML.</span><span class="sxs-lookup"><span data-stu-id="69886-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="69886-335">Se il file XML non esiste già, il codice crea `SaveXml` un grafico come normale e quindi chiama per salvarlo.</span><span class="sxs-lookup"><span data-stu-id="69886-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="69886-336">Il rendering del `Write` grafico viene eseguito utilizzando il metodo , come si è visto in precedenza.</span><span class="sxs-lookup"><span data-stu-id="69886-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="69886-337">Come per la pagina che mostrava la memorizzazione nella cache, questo codice include un timestamp nel titolo del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="69886-338">Creare una nuova pagina denominata ChartDisplayXMLChart.cshtml e aggiungervi il markup seguente:Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span><span class="sxs-lookup"><span data-stu-id="69886-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="69886-339">Eseguire la pagina *ChartDisplayXMLChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="69886-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="69886-340">Viene visualizzato il grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-340">The chart is displayed.</span></span> <span data-ttu-id="69886-341">Prendere nota del timestamp nel titolo del grafico.</span><span class="sxs-lookup"><span data-stu-id="69886-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="69886-342">Chiudere il browser.</span><span class="sxs-lookup"><span data-stu-id="69886-342">Close the browser.</span></span>
6. <span data-ttu-id="69886-343">In WebMatrix fare clic con il **Refresh** \* \_\* pulsante destro del mouse sulla cartella ChartFiles , scegliere Aggiorna , quindi aprire la cartella.</span><span class="sxs-lookup"><span data-stu-id="69886-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="69886-344">Il file *XMLChart.xml* in questa `Chart` cartella è stato creato dall'helper.</span><span class="sxs-lookup"><span data-stu-id="69886-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Descrizione: la cartella _ChartFiles che mostra il file XMLChart.xml creato dall'helper Chart.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="69886-346">Eseguire nuovamente la pagina *ChartDisplayXMLChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="69886-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="69886-347">Il grafico mostra lo stesso timestamp della prima esecuzione della pagina.</span><span class="sxs-lookup"><span data-stu-id="69886-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="69886-348">Questo perché il grafico viene generato dal codice XML salvato in precedenza.</span><span class="sxs-lookup"><span data-stu-id="69886-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="69886-349">In WebMatrix aprire \* \_\* la cartella ChartFiles ed eliminare il file *XMLChart.xml.*</span><span class="sxs-lookup"><span data-stu-id="69886-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="69886-350">Eseguire nuovamente la pagina *ChartDisplayXMLChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="69886-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="69886-351">Questa volta, il timestamp viene `Chart` aggiornato, perché l'helper ha dovuto ricreare il file XML.</span><span class="sxs-lookup"><span data-stu-id="69886-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="69886-352">Se lo si \* \_\* desidera, controllare la cartella ChartFiles e notare che il file XML è tornato.</span><span class="sxs-lookup"><span data-stu-id="69886-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="69886-353">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="69886-353">Additional Resources</span></span>

- [<span data-ttu-id="69886-354">Introduzione all'utilizzo di un database in siti di pagine Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="69886-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="69886-355">Utilizzo della memorizzazione nella cache in ASP.NET siti di pagine Web per migliorare le prestazioniUsing Caching in ASP.NET Web Pages Sites to Improve Performance</span><span class="sxs-lookup"><span data-stu-id="69886-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="69886-356">[Classe Chart](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET riferimento all'API Web Pages su MSDN)</span><span class="sxs-lookup"><span data-stu-id="69886-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
