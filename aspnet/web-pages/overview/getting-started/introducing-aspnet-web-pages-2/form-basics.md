---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Introduzione ASP.NET pagine Web - Nozioni di base sui moduli HTML Documenti Microsoft
author: Rick-Anderson
description: In questa esercitazione vengono illustrate le nozioni di base su come creare un modulo di input e su come gestire l'input dell'utente quando si utilizza ASP.NET pagine Web (Razor). E ora che...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/06/2020
ms.locfileid: "80676328"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="bfe8a-104">Introduzione a pagine Web ASP.NET - Nozioni di base sui moduli HTML</span><span class="sxs-lookup"><span data-stu-id="bfe8a-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="bfe8a-105"> di [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="bfe8a-106">In questa esercitazione vengono illustrate le nozioni di base su come creare un modulo di input e su come gestire l'input dell'utente quando si utilizza ASP.NET pagine Web (Razor).</span><span class="sxs-lookup"><span data-stu-id="bfe8a-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="bfe8a-107">E ora che hai un database, userai le tue abilità di modulo per consentire agli utenti di trovare film specifici nel database.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="bfe8a-108">Si presuppone che la serie sia stata completata [tramite Introduzione alla visualizzazione dei dati mediante ASP.NET pagine Web](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="bfe8a-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="bfe8a-109">Contenuto dell'esercitazione:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="bfe8a-110">Come creare un modulo utilizzando elementi HTML standard.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="bfe8a-111">Come leggere l'input dell'utente in un modulo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="bfe8a-112">Come creare una query SQL che ottiene in modo selettivo i dati utilizzando un termine di ricerca fornito dall'utente.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="bfe8a-113">Come avere campi nella pagina "ricordare" ciò che l'utente ha inserito.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="bfe8a-114">Caratteristiche/tecnologie discusse:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="bfe8a-115">Oggetto `Request`.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-115">The `Request` object.</span></span>
> - <span data-ttu-id="bfe8a-116">Clausola `Where` SQL.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="bfe8a-117">Scopo dell'esercitazione</span><span class="sxs-lookup"><span data-stu-id="bfe8a-117">What You'll Build</span></span>

<span data-ttu-id="bfe8a-118">Nell'esercitazione precedente è stato creato un database, vi `WebGrid` sono stati aggiunti dati e quindi è stato usato l'helper per visualizzare i dati.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="bfe8a-119">In questa esercitazione si aggiungerà una casella di ricerca che consente di trovare filmati di un genere specifico o il cui titolo contiene qualsiasi parola immessa.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="bfe8a-120">(Ad esempio, sarete in grado di trovare tutti i film il cui genere è "Azione" o il cui titolo contiene "Harry" o "Avventura.")</span><span class="sxs-lookup"><span data-stu-id="bfe8a-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="bfe8a-121">Quando hai finito con questo tutorial, avrai una pagina come questa:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Pagina Film con ricerca di generi e titoli](form-basics/_static/image1.png)

<span data-ttu-id="bfe8a-123">La parte elenco della pagina è la &mdash; stessa dell'ultima esercitazione di una griglia.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="bfe8a-124">La differenza sarà che la griglia mostrerà solo i film che hai cercato.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="bfe8a-125">Informazioni sui moduli HTML</span><span class="sxs-lookup"><span data-stu-id="bfe8a-125">About HTML Forms</span></span>

<span data-ttu-id="bfe8a-126">(Se hai esperienza con la creazione di moduli `GET` `POST`HTML e con la differenza tra e , puoi saltare questa sezione.)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="bfe8a-127">Un modulo include &mdash; caselle di testo, pulsanti, pulsanti di opzione, caselle di controllo, elenchi a discesa e così via per un modulo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="bfe8a-128">Gli utenti compilano questi controlli o effettuano selezioni e quindi inviano il modulo facendo clic su un pulsante.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="bfe8a-129">La sintassi HTML di base di un modulo è illustrata da questo esempio:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="bfe8a-130">Quando questo markup viene eseguito in una pagina, crea un modulo semplice simile a questa illustrazione:When this markup runs in a page, it creates a simple form that looks like this illustration:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Modulo HTML di base come visualizzato nel browser](form-basics/_static/image2.png)

<span data-ttu-id="bfe8a-132">L'elemento `<form>` racchiude gli elementi HTML da inviare.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="bfe8a-133">(Un errore facile da fare è quello di aggiungere elementi `<form>` alla pagina, ma poi dimenticare di metterli all'interno di un elemento.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="bfe8a-134">In tal caso, non viene presentato nulla.) L'attributo `method` indica al browser come inviare l'input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="bfe8a-135">Impostare questa `post` opzione su se si sta eseguendo `get` un aggiornamento sul server o su se si stanno solo recuperando dati dal server.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="bfe8a-136">**Sicurezza dei verbi GET, POST e HTTP**</span><span class="sxs-lookup"><span data-stu-id="bfe8a-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="bfe8a-137">HTTP, il protocollo che i browser e i server utilizzano per lo scambio di informazioni, è notevolmente semplice nelle sue operazioni di base.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="bfe8a-138">I browser utilizzano solo pochi verbi per effettuare richieste ai server.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="bfe8a-139">Quando si scrive codice per il Web, è utile comprendere questi verbi e come il browser e il server li utilizzano.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="bfe8a-140">Di lontano i verbi più comunemente utilizzati sono i seguenti:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="bfe8a-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-141">`GET`.</span></span> <span data-ttu-id="bfe8a-142">Il browser utilizza questo verbo per recuperare qualcosa dal server.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="bfe8a-143">Ad esempio, quando si digita un URL nel `GET` browser, il browser esegue un'operazione per richiedere la pagina desiderata.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="bfe8a-144">Se la pagina include elementi `GET` grafici, il browser esegue operazioni aggiuntive per ottenere le immagini.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="bfe8a-145">Se `GET` l'operazione deve passare informazioni al server, le informazioni vengono passate come parte dell'URL nella stringa di query.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="bfe8a-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-146">`POST`.</span></span> <span data-ttu-id="bfe8a-147">Il browser `POST` invia una richiesta per inviare i dati da aggiungere o modificare sul server.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="bfe8a-148">Ad esempio, `POST` il verbo viene utilizzato per creare record in un database o modificare quelli esistenti.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="bfe8a-149">La maggior parte delle volte, quando si compila un modulo `POST` e si fa clic sul pulsante Invia, il browser esegue un'operazione.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="bfe8a-150">In `POST` un'operazione, i dati passati al server si trovano nel corpo della pagina.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="bfe8a-151">Una distinzione importante tra questi `GET` verbi è che un'operazione non dovrebbe cambiare nulla sul server `GET` - o per metterlo in modo leggermente più astratto, un'operazione non comporta una modifica dello stato sul server.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="bfe8a-152">È possibile `GET` eseguire un'operazione sulle stesse risorse tutte le volte che si desidera e tali risorse non cambiano.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="bfe8a-153">(Un'operazione `GET` è spesso detta "sicura", o per usare un termine tecnico, è *idempotente*.) Al contrario, naturalmente, una `POST` richiesta cambia qualcosa sul server ogni volta che si esegue l'operazione.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="bfe8a-154">Due esempi aiuteranno a illustrare questa distinzione.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="bfe8a-155">Quando si esegue una ricerca utilizzando un motore come Bing o Google, si compila un modulo costituito da una casella di testo e quindi si fa clic sul pulsante di ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="bfe8a-156">Il browser `GET` esegue un'operazione, con il valore immesso nella casella passato come parte dell'URL.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="bfe8a-157">L'utilizzo di un'operazione `GET` per questo tipo di modulo va bene, perché un'operazione di ricerca non modifica le risorse sul server, ma solo recupera le informazioni.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="bfe8a-158">Ora considera il processo di ordinare qualcosa online.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="bfe8a-159">Compilare i dettagli dell'ordine e quindi fare clic sul pulsante Invia.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="bfe8a-160">Questa operazione sarà `POST` una richiesta, perché l'operazione comporterà modifiche sul server, ad esempio un nuovo record di ordine, una modifica delle informazioni dell'account e forse molte altre modifiche.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="bfe8a-161">A `GET` differenza dell'operazione, `POST` non è possibile ripetere la richiesta: se lo è stato, ogni volta che la richiesta viene inviata nuovamente, si genererà un nuovo ordine sul server.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="bfe8a-162">(In casi come questo, i siti web spesso ti avvisano di non fare clic su un pulsante di invio più di una volta, o disabiliteranno il pulsante di invio in modo da non inviare nuovamente il modulo accidentalmente.)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="bfe8a-163">Nel corso di questa esercitazione, si `GET` userà `POST` sia un'operazione che un'operazione per lavorare con i moduli HTML.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="bfe8a-164">Spiegheremo in ogni caso perché il verbo che usi è quello appropriato.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="bfe8a-165">Per ulteriori informazioni sui verbi HTTP, vedere l'articolo [sulle definizioni](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) dei metodi nel sito W3C.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="bfe8a-166">La maggior parte `<input>` degli elementi di input dell'utente sono elementi HTML.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="bfe8a-167">Hanno un `<input type="type" name="name">,` aspetto simile al punto in cui *il tipo* indica il tipo di controllo di input dell'utente desiderato.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="bfe8a-168">Questi elementi sono quelli comuni:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-168">These elements are the common ones:</span></span>

- <span data-ttu-id="bfe8a-169">Casella di testo:`<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="bfe8a-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="bfe8a-170">Casella di controllo:`<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="bfe8a-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="bfe8a-171">Pulsante di opzione:`<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="bfe8a-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="bfe8a-172">Pulsante:`<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="bfe8a-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="bfe8a-173">Pulsante Invia:`<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="bfe8a-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="bfe8a-174">È inoltre possibile `<textarea>` utilizzare l'elemento per creare `<select>` una casella di testo su più righe e l'elemento per creare un elenco a discesa o un elenco scorrevole.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="bfe8a-175">Per ulteriori informazioni sugli elementi modulo HTML, vedere [Moduli HTML e input](http://www.w3schools.com/html/html_forms.asp) nel sito W3Schools.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="bfe8a-176">L'attributo `name` è molto importante, perché il nome è come si otterrà il valore dell'elemento in un secondo momento, come si vedrà a breve.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="bfe8a-177">La parte interessante è ciò che tu, lo sviluppatore della pagina, fai con l'input dell'utente.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="bfe8a-178">Non esiste alcun comportamento incorporato associato a questi elementi.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="bfe8a-179">Invece, è necessario ottenere i valori che l'utente ha inserito o selezionato ed eseguire un'operazione con loro.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="bfe8a-180">Questo è quello che imparerai in questo tutorial.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="bfe8a-181">**HTML5 e moduli di input**</span><span class="sxs-lookup"><span data-stu-id="bfe8a-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="bfe8a-182">Come forse saprai, HTML è in transizione e la versione più recente (HTML5) include il supporto per modi più intuitivi per gli utenti di inserire informazioni.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="bfe8a-183">Ad esempio, in HTML5, puoi dire alla pagina che vuoi che l'utente immetta una data.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="bfe8a-184">Il browser può quindi visualizzare automaticamente un calendario anziché richiedere all'utente di immettere una data manualmente.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="bfe8a-185">Tuttavia, HTML5 è nuovo e non è ancora supportato in tutti i browser.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="bfe8a-186">ASP.NET pagine Web supporta l'input HTML5 nella misura in cui il browser dell'utente supporta.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="bfe8a-187">Per un'idea dei nuovi `<input>` attributi per l'elemento in HTML5, consultate Tipo di [input &lt;&gt; HTML Attribute](http://www.w3schools.com/html/html_form_input_types.asp) nel sito W3Schools.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="bfe8a-188">Creazione del form</span><span class="sxs-lookup"><span data-stu-id="bfe8a-188">Creating the Form</span></span>

<span data-ttu-id="bfe8a-189">In WebMatrix, nell'area di lavoro **File,** aprire la pagina *Movies.cshtml.In* WebMatrix, in the Files workspace, open the Movies.cshtml page.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="bfe8a-190">Dopo il `</h1>` tag di `<div>` chiusura `grid.GetHtml` e prima del tag di apertura della chiamata, aggiungere il markup seguente:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="bfe8a-191">Questo markup crea un modulo con `searchGenre` una casella di testo denominata e un pulsante di invio.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="bfe8a-192">La casella di testo e il `<form>` pulsante di invio sono racchiusi in un elemento il cui `method` attributo è impostato su `get`.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="bfe8a-193">(Ricorda che se non si inserisce la casella `<form>` di testo e il pulsante di invio all'interno di un elemento, non verrà inviato nulla quando si fa clic sul pulsante.) Si utilizza `GET` il verbo qui perché si sta creando un modulo che non apporta alcuna modifica sul server - si traduce solo in una ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="bfe8a-194">(Nell'esercitazione precedente, `post` è stato utilizzato un metodo, che è il modo in cui si inviano le modifiche al server.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="bfe8a-195">Vedrai che nel prossimo tutorial di nuovo.)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="bfe8a-196">Eseguire la pagina.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-196">Run the page.</span></span> <span data-ttu-id="bfe8a-197">Anche se non è stato definito alcun comportamento per il modulo, è possibile visualizzarne l'aspetto:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Pagina Film con casella di ricerca per Genere](form-basics/_static/image3.png)

<span data-ttu-id="bfe8a-199">Immettere un valore nella casella di testo, ad esempio "Commedia".</span><span class="sxs-lookup"><span data-stu-id="bfe8a-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="bfe8a-200">Quindi fare clic su **Cerca genere**.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="bfe8a-201">Prendere nota dell'URL della pagina.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-201">Take note of the URL of the page.</span></span> <span data-ttu-id="bfe8a-202">Poiché si `<form>` imposta `method` l'attributo dell'elemento su `get`, il valore immesso fa ora parte della stringa di query nell'URL, in questo modo:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="bfe8a-203">Lettura dei valori dei moduli</span><span class="sxs-lookup"><span data-stu-id="bfe8a-203">Reading Form Values</span></span>

<span data-ttu-id="bfe8a-204">La pagina contiene già del codice che ottiene i dati del database e visualizza i risultati in una griglia.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="bfe8a-205">A questo punto è necessario aggiungere del codice che legge il valore della casella di testo in modo da poter eseguire una query SQL che include il termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="bfe8a-206">Poiché si imposta il `get`metodo del form su , è possibile leggere il valore immesso nella casella di testo utilizzando codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="bfe8a-207">L'oggetto `Request.QueryString` `QueryString` (la `Request` proprietà dell'oggetto) include i valori degli `GET` elementi inviati come parte dell'operazione.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="bfe8a-208">La `Request.QueryString` proprietà contiene una *raccolta* (un elenco) dei valori inviati nel modulo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="bfe8a-209">Per ottenere un singolo valore, specificare il nome dell'elemento desiderato.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="bfe8a-210">Ecco perché è necessario disporre `name` di `<input>` un`searchTerm`attributo sull'elemento ( ) che crea la casella di testo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="bfe8a-211">(Per ulteriori `Request` informazioni sull'oggetto, vedere la [barra laterale](#BKMK_TheRequestObject) più avanti.)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="bfe8a-212">È abbastanza semplice leggere il valore della casella di testo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="bfe8a-213">Ma se l'utente non ha inserito nulla nella casella di testo, ma ha fatto clic su **Cerca** comunque, è possibile ignorare tale clic, dal momento che non c'è nulla da cercare.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="bfe8a-214">Il codice seguente è un esempio che illustra come implementare queste condizioni.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="bfe8a-215">(Non è necessario aggiungere questo codice ancora; lo farai in un momento.)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="bfe8a-216">Il test si interrompe in questo modo:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="bfe8a-217">Ottenere il `Request.QueryString["searchGenre"]`valore di , ovvero il `<input>` valore `searchGenre`immesso nell'elemento denominato .</span><span class="sxs-lookup"><span data-stu-id="bfe8a-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="bfe8a-218">Scopri se è vuoto utilizzando `IsEmpty` il metodo .</span><span class="sxs-lookup"><span data-stu-id="bfe8a-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="bfe8a-219">Questo metodo è il modo standard per determinare se un elemento (ad esempio, un elemento form) contiene un valore.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="bfe8a-220">Ma in realtà, ti importa solo se *non* è vuoto, quindi ...</span><span class="sxs-lookup"><span data-stu-id="bfe8a-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="bfe8a-221">Aggiungere `!` l'operatore `IsEmpty` prima del test.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="bfe8a-222">(L'operatore `!` significa NOT logico).</span><span class="sxs-lookup"><span data-stu-id="bfe8a-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="bfe8a-223">In parole povere, l'intera `if` condizione si traduce nel seguente: Se *l'elemento searchGenre del modulo non è vuoto, allora ...*</span><span class="sxs-lookup"><span data-stu-id="bfe8a-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="bfe8a-224">Questo blocco imposta la fase per la creazione di una query che utilizza il termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="bfe8a-225">Questa operazione viene illustrata nella sezione seguente.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="bfe8a-226">**Oggetto Request**</span><span class="sxs-lookup"><span data-stu-id="bfe8a-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="bfe8a-227">L'oggetto `Request` contiene tutte le informazioni che il browser invia all'applicazione quando viene richiesta o inviata una pagina.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="bfe8a-228">Questo oggetto include tutte le informazioni fornite dall'utente, ad esempio i valori delle caselle di testo o un file da caricare.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="bfe8a-229">Include anche tutti i tipi di informazioni aggiuntive, come i cookie, i valori nella stringa di query URL (se presente), il percorso del file della pagina in esecuzione, il tipo di browser che l'utente sta utilizzando, l'elenco delle lingue impostate nel browser e molto altro ancora.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="bfe8a-230">L'oggetto `Request` è una *raccolta* (elenco) di valori.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="bfe8a-231">Si ottiene un singolo valore dalla raccolta specificandone il nome:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="bfe8a-232">L'oggetto `Request` espone effettivamente diversi sottoinsiemi.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="bfe8a-233">Ad esempio:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-233">For example:</span></span>
> 
> - <span data-ttu-id="bfe8a-234">`Request.Form`fornisce i valori degli `<form>` elementi all'interno `POST` dell'elemento inviato se la richiesta è una richiesta.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="bfe8a-235">`Request.QueryString`fornisce solo i valori nella stringa di query dell'URL.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="bfe8a-236">In un URL `http://mysite/myapp/page?searchGenre=action&page=2`come `?searchGenre=action&page=2` , la sezione dell'URL è la stringa di query.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="bfe8a-237">`Request.Cookies`la raccolta consente di accedere ai cookie che il browser ha inviato.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="bfe8a-238">Per ottenere un valore che si sa è `Request["name"]`nel modulo inviato, è possibile utilizzare .</span><span class="sxs-lookup"><span data-stu-id="bfe8a-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="bfe8a-239">In alternativa, è possibile utilizzare `Request.Form["name"]` le `POST` versioni `Request.QueryString["name"]` più `GET` specifiche (per le richieste) o (per le richieste).</span><span class="sxs-lookup"><span data-stu-id="bfe8a-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="bfe8a-240">Naturalmente, *nome* è il nome dell'elemento da ottenere.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="bfe8a-241">Il nome dell'elemento che si desidera ottenere deve essere univoco all'interno della raccolta in uso.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="bfe8a-242">Ecco perché l'oggetto `Request` fornisce i `Request.Form` `Request.QueryString`sottoinsiemi come e .</span><span class="sxs-lookup"><span data-stu-id="bfe8a-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="bfe8a-243">Si supponga che la pagina `userName` contenga un `userName`elemento form denominato e *che contenga anche* un cookie denominato .</span><span class="sxs-lookup"><span data-stu-id="bfe8a-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="bfe8a-244">Se si `Request["userName"]`ottiene , è ambiguo se si desidera il valore del modulo o il cookie.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="bfe8a-245">Tuttavia, se `Request.Form["userName"]` `Request.Cookie["userName"]`si ottiene o , si sta essendo esplicito su quale valore ottenere.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="bfe8a-246">È buona norma essere specifici e utilizzare `Request` il sottoinsieme di che `Request.Form` `Request.QueryString`ti interessa, come o .</span><span class="sxs-lookup"><span data-stu-id="bfe8a-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="bfe8a-247">Per le pagine semplici che si sta creando in questo tutorial, probabilmente non fa alcuna differenza.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="bfe8a-248">Tuttavia, quando si creano pagine più `Request.Form` `Request.QueryString` complesse, utilizzando la versione esplicita o può aiutare a evitare problemi che possono verificarsi quando la pagina contiene un modulo (o più moduli), cookie, valori di stringa di query e così via.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="bfe8a-249">Creazione di una query tramite un termine di ricerca</span><span class="sxs-lookup"><span data-stu-id="bfe8a-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="bfe8a-250">Ora che si conosce come ottenere il termine di ricerca immesso dall'utente, è possibile creare una query che lo utilizza.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="bfe8a-251">Tenere presente che per ottenere tutti gli elementi del filmato dal database, si sta utilizzando una query SQL che assomiglia a questa istruzione:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="bfe8a-252">Per ottenere solo determinati filmati, è necessario `Where` utilizzare una query che include una clausola.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="bfe8a-253">Questa clausola consente di impostare una condizione sulle righe restituite dalla query.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="bfe8a-254">Ad esempio:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="bfe8a-255">Il formato `WHERE column = value`di base è .</span><span class="sxs-lookup"><span data-stu-id="bfe8a-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="bfe8a-256">È possibile utilizzare diversi `=`operatori `>` oltre a `<` , come `<>` (maggiore di), `<=` (minore di), (non uguale a), (minore o uguale a), ecc., a seconda di ciò che si sta cercando.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="bfe8a-257">Nel caso in cui vi state chiedendo, &mdash; `SELECT` le istruzioni `Select` SQL `select`non sono case sensitive è lo stesso come (o anche ).</span><span class="sxs-lookup"><span data-stu-id="bfe8a-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="bfe8a-258">Tuttavia, gli utenti spesso sfruttano `SELECT` in `WHERE`maiuscolo le parole chiave in un'istruzione SQL, ad esempio e , per semplificarne la lettura.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="bfe8a-259">Passaggio del termine di ricerca come parametroPassing the search term as a parameter</span><span class="sxs-lookup"><span data-stu-id="bfe8a-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="bfe8a-260">La ricerca di un genere`WHERE Genre = 'Action'`specifico è abbastanza facile ( ), ma si desidera essere in grado di cercare qualsiasi genere che l'utente immette.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="bfe8a-261">A tale scopo, creare come query SQL che include un segnaposto per il valore da cercare.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="bfe8a-262">Sarà simile a questo comando:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="bfe8a-263">Il segnaposto `@` è il carattere seguito da zero.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="bfe8a-264">Come si può intuire, una query può contenere `@1` `@2`più segnaposto e sarebbero denominati `@0`, , , e così via.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="bfe8a-265">Per impostare la query ed effettivamente passarla il valore, utilizzare il codice simile al seguente:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="bfe8a-266">Questo codice è simile a quello già fatto per visualizzare i dati nella griglia.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="bfe8a-267">Le uniche differenze sono:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-267">The only differences are:</span></span>

- <span data-ttu-id="bfe8a-268">La query contiene`WHERE Genre = @0"`un segnaposto ( ).</span><span class="sxs-lookup"><span data-stu-id="bfe8a-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="bfe8a-269">La query viene inserita`selectCommand`in una variabile ( ); in precedenza, la query `db.Query` è stata passata direttamente al metodo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="bfe8a-270">Quando si `db.Query` chiama il metodo, si passano sia la query che il valore da utilizzare per il segnaposto.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="bfe8a-271">Se la query contiene più segnaposto, è necessario passarli tutti come valori separati al metodo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="bfe8a-272">Se si mettono insieme tutti questi elementi, si ottiene il codice seguente:If you put all these elements together, you get the following code:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="bfe8a-273">**Importante!**</span><span class="sxs-lookup"><span data-stu-id="bfe8a-273">**Important!**</span></span> <span data-ttu-id="bfe8a-274">L'utilizzo di `@0`segnaposto (ad esempio ) per passare valori a un comando SQL è *estremamente importante* per la sicurezza.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="bfe8a-275">Il modo in cui lo vedi qui, con segnaposto per i dati variabili, è l'unico modo in cui dovresti costruire i comandi SQL.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="bfe8a-276">Non costruire mai un'istruzione SQL mettendo insieme (concatenando) il testo letterale e i valori ottenuti dall'utente.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="bfe8a-277">Concatenando l'input dell'utente in un'istruzione SQL apre il sito a un *attacco SQL injection* in cui un utente malintenzionato invia valori alla pagina che violano il database.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="bfe8a-278">(È possibile leggere di più nell'articolo [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) il sito Web MSDN.)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="bfe8a-279">Aggiornamento della pagina Film con il codice di ricerca</span><span class="sxs-lookup"><span data-stu-id="bfe8a-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="bfe8a-280">A questo punto è possibile aggiornare il codice nel file *Movies.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="bfe8a-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="bfe8a-281">Per iniziare, sostituisci il codice nel blocco di codice nella parte superiore della pagina con questo codice:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="bfe8a-282">La differenza qui è che hai inserito la query nella `selectCommand` `db.Query` variabile, che si passerà in seguito.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="bfe8a-283">L'inserimento dell'istruzione SQL in una variabile consente di modificare l'istruzione, che è ciò che si farà per eseguire la ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="bfe8a-284">Hai anche rimosso queste due righe, che verrà rimesso in seguito:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="bfe8a-285">Non si desidera eseguire ancora la query, `db.Query`ovvero chiamare , e non `WebGrid` si desidera inizializzare l'helper.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="bfe8a-286">Si farà queste cose dopo aver capito quale istruzione SQL deve essere eseguita.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="bfe8a-287">Dopo questo blocco riscritto, è possibile aggiungere la nuova logica per la gestione della ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="bfe8a-288">Il codice completato sarà simile al seguente.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-288">The completed code will look like the following.</span></span> <span data-ttu-id="bfe8a-289">Aggiornare il codice nella pagina in modo che corrisponda a questo esempio:Update the code in your page so it matches this example:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="bfe8a-290">La pagina ora funziona in questo modo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-290">The page now works like this.</span></span> <span data-ttu-id="bfe8a-291">Ogni volta che la pagina viene eseguita, il codice apre il database e la `selectCommand` variabile viene impostata sull'istruzione SQL che ottiene tutti i record dalla `Movies` tabella.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="bfe8a-292">Il codice inizializza `searchTerm` anche la variabile.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="bfe8a-293">Tuttavia, se la richiesta corrente `searchGenre` include un `selectCommand` valore per l'elemento, il codice `Where` imposta una query diversa, ovvero una che include la clausola per cercare un genere.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="bfe8a-294">Si imposta `searchTerm` anche a tutto ciò che è stato passato per la casella di ricerca (che potrebbe essere nulla).</span><span class="sxs-lookup"><span data-stu-id="bfe8a-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="bfe8a-295">Indipendentemente dall'istruzione SQL `selectCommand`in , `db.Query` il codice chiama per eseguire `searchTerm`la query, passando tutto ciò che si trova in .</span><span class="sxs-lookup"><span data-stu-id="bfe8a-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="bfe8a-296">Se non c'è niente in `searchTerm`, non importa, perché in questo caso `selectCommand` non c'è nessun parametro per passare il valore a comunque.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="bfe8a-297">Infine, il codice `WebGrid` inizializza l'helper usando i risultati della query, proprio come prima.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="bfe8a-298">Si può vedere che inserendo l'istruzione SQL e il termine di ricerca in variabili, è stata aggiunta flessibilità al codice.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="bfe8a-299">Come si vedrà più avanti in questa esercitazione, è possibile usare questo framework di base e continuare ad aggiungere logica per diversi tipi di ricerche.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="bfe8a-300">Test della funzionalità Search-by-Genre</span><span class="sxs-lookup"><span data-stu-id="bfe8a-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="bfe8a-301">In WebMatrix eseguire la pagina *Movies.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="bfe8a-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="bfe8a-302">Viene visualizzata la pagina con la casella di testo per genere.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="bfe8a-303">Inserisci un genere che hai inserito per uno dei record di test, quindi fai clic su **Cerca**.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="bfe8a-304">Questa volta vedi un elenco dei solo film che corrispondono a quel genere:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-304">This time you see a listing of just the movies that match that genre:</span></span>

![Elenco della pagina dei film dopo la ricerca del genere 'Comedies'](form-basics/_static/image4.png)

<span data-ttu-id="bfe8a-306">Inserisci un genere diverso e cerca di nuovo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-306">Enter a different genre and search again.</span></span> <span data-ttu-id="bfe8a-307">Provare a immettere il genere utilizzando tutte le lettere minuscole o tutte maiuscole in modo da vedere che la ricerca non fa distinzione tra maiuscole e minuscole.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="bfe8a-308">"Ricordare" ciò che l'utente ha inserito</span><span class="sxs-lookup"><span data-stu-id="bfe8a-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="bfe8a-309">Potresti aver notato che dopo aver inserito un genere e cliccato **Search Genre**, hai visto un elenco per quel genere.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="bfe8a-310">Tuttavia, la casella &mdash; di testo di ricerca era vuota, in altre parole, non ricordava ciò che avevi inserito.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="bfe8a-311">È importante comprendere perché si verifica questo comportamento.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="bfe8a-312">Quando si invia una pagina, il browser invia una richiesta al server Web.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="bfe8a-313">Quando ASP.NET ottiene la richiesta, crea una nuova istanza della pagina, esegue il codice in essa contenuto e quindi esegue nuovamente il rendering della pagina nel browser.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="bfe8a-314">In effetti, però, la pagina non sa che stavi solo lavorando con una versione precedente di se stessa.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="bfe8a-315">Tutto quello che sa è che ha ottenuto una richiesta che aveva alcuni dati del modulo in esso.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="bfe8a-316">Ogni volta che &mdash; richiedi una pagina, sia per &mdash; la prima volta che inviandola, riceverai una nuova pagina.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="bfe8a-317">Il server Web non ha memoria dell'ultima richiesta.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="bfe8a-318">Né ASP.NET, e nemmeno il browser.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="bfe8a-319">L'unica connessione tra queste istanze separate della pagina è qualsiasi dato trasmesso tra di essi.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="bfe8a-320">Se si invia una pagina, ad esempio, la nuova istanza della pagina può ottenere i dati del modulo inviati dall'istanza precedente.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="bfe8a-321">(Un altro modo per passare i dati tra le pagine consiste nell'utilizzare i cookie.)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="bfe8a-322">Un modo formale per descrivere questa situazione consiste nel dire che le pagine web sono *senza stato.*</span><span class="sxs-lookup"><span data-stu-id="bfe8a-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="bfe8a-323">I server Web e le pagine stesse e gli elementi nella pagina non mantengono alcuna informazione sullo stato precedente di una pagina.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="bfe8a-324">Il web è stato progettato in questo modo perché mantenere lo stato per le singole richieste esaurirebbe rapidamente le risorse dei server web, che spesso gestiscono migliaia, forse anche centinaia di migliaia, di richieste al secondo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="bfe8a-325">Ecco perché la casella di testo era vuota.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-325">So that's why the text box was empty.</span></span> <span data-ttu-id="bfe8a-326">Dopo aver inviato la pagina, ASP.NET creata una nuova istanza della pagina ed è stata eseguita attraverso il codice e il markup.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="bfe8a-327">Non c'era nulla in quel codice che ASP.NET detto di inserire un valore nella casella di testo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="bfe8a-328">Quindi ASP.NET non ha fatto nulla, e la casella di testo è stato eseguito il rendering senza un valore in esso.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="bfe8a-329">C'è in realtà un modo semplice per aggirare questo problema.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="bfe8a-330">Il genere immesso nella *is* casella di testo &mdash; è disponibile `Request.QueryString["searchGenre"]`nel codice in .</span><span class="sxs-lookup"><span data-stu-id="bfe8a-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="bfe8a-331">Aggiornare il markup per la `value` casella di `searchTerm`testo in modo che l'attributo ottenga il relativo valore da , come nell'esempio seguente:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="bfe8a-332">In questa pagina, potresti `value` anche impostare l'attributo sulla `searchTerm` variabile, poiché tale variabile contiene anche il genere che hai inserito.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="bfe8a-333">Ma utilizzando `Request` l'oggetto `value` per impostare l'attributo come illustrato di seguito è il modo standard per eseguire questa attività.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="bfe8a-334">(Supponendo che si desidera &mdash; anche eseguire questa operazione in alcune situazioni, si potrebbe desiderare di eseguire il rendering della pagina *senza* valori nei campi.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="bfe8a-335">Tutto dipende da cosa sta succedendo con la tua app.)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="bfe8a-336">Non è possibile "ricordare" il valore di una casella di testo utilizzata per le password.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="bfe8a-337">Sarebbe un buco di sicurezza per consentire alle persone di compilare un campo password utilizzando il codice.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="bfe8a-338">Eseguire nuovamente la pagina, immettere un genere e fare clic su **Cerca genere**.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="bfe8a-339">Questa volta non solo vedi i risultati della ricerca, ma la casella di testo ricorda ciò che hai inserito l'ultima volta:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Pagina che mostra che la casella di testo ha 'ricordato' la voce precedente](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="bfe8a-341">Ricerca di qualsiasi parola nel titolo</span><span class="sxs-lookup"><span data-stu-id="bfe8a-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="bfe8a-342">È ora possibile cercare qualsiasi genere, ma si potrebbe anche desiderare di cercare un titolo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="bfe8a-343">È difficile ottenere un titolo esattamente corretto quando si esegue la ricerca, quindi è possibile cercare una parola che appare in qualsiasi punto all'interno di un titolo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="bfe8a-344">A tale scopo in SQL, utilizzare l'operatore `LIKE` e la sintassi come il seguente:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="bfe8a-345">Questo comando ottiene tutti i film i cui titoli contengono "avventura".</span><span class="sxs-lookup"><span data-stu-id="bfe8a-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="bfe8a-346">Quando si `LIKE` utilizza l'operatore, `%` si include il carattere jolly come parte del termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="bfe8a-347">La `LIKE 'adventure%'` ricerca significa "iniziare con 'avventura'".</span><span class="sxs-lookup"><span data-stu-id="bfe8a-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="bfe8a-348">(Tecnicamente, significa "La stringa 'avventura' seguita da qualsiasi cosa.") Allo stesso modo, `LIKE '%adventure'` il termine di ricerca significa "qualsiasi cosa seguita dalla stringa 'avventura'", che è un altro modo per dire "termina con 'avventura'".</span><span class="sxs-lookup"><span data-stu-id="bfe8a-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="bfe8a-349">Il termine `LIKE '%adventure%'` di ricerca significa quindi "con 'avventura' in qualsiasi punto del titolo".</span><span class="sxs-lookup"><span data-stu-id="bfe8a-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="bfe8a-350">(Tecnicamente, "qualsiasi cosa nel titolo, seguita da 'avventura', seguita da qualsiasi cosa.")</span><span class="sxs-lookup"><span data-stu-id="bfe8a-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="bfe8a-351">All'interno dell'elemento, `<form>` aggiungere il `</div>` seguente markup proprio sotto il `</form>` tag di chiusura per la ricerca di genere (appena prima dell'elemento di chiusura):</span><span class="sxs-lookup"><span data-stu-id="bfe8a-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="bfe8a-352">Il codice per gestire questa ricerca è simile al codice per la `LIKE` ricerca di generi, ad eccezione del fatto che è necessario assemblare la ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="bfe8a-353">All'interno del blocco di codice nella `if` parte superiore `if` della pagina, aggiungi questo blocco subito dopo il blocco per la ricerca di generi:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="bfe8a-354">Questo codice usa la stessa logica vista in `LIKE` precedenza, ad`%`eccezione del fatto che la ricerca utilizza un operatore e il codice inserisce " " prima e dopo il termine di ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="bfe8a-355">Si noti come è stato facile aggiungere un'altra ricerca alla pagina.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="bfe8a-356">Tutto quello che dovevi fare era:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-356">All you had to do was:</span></span>

- <span data-ttu-id="bfe8a-357">Creare `if` un blocco che teda per verificare se la casella di ricerca pertinente ha un valore.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="bfe8a-358">Impostare `selectCommand` la variabile su una nuova istruzione SQL.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="bfe8a-359">Impostare `searchTerm` la variabile sul valore da passare alla query.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="bfe8a-360">Ecco il blocco di codice completo, che contiene la nuova logica per una ricerca del titolo:Here's the complete code block, which contains the new logic for a title search:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="bfe8a-361">Ecco un riepilogo delle operazioni eseguite da questo codice:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="bfe8a-362">Le `searchTerm` variabili `selectCommand` e vengono inizializzate nella parte superiore.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="bfe8a-363">Si imposterà queste variabili sul termine di ricerca appropriato (se presente) e sul comando SQL appropriato in base alle operazioni eseguite dall'utente nella pagina.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="bfe8a-364">La ricerca predefinita è il semplice caso di ottenere tutti i film dal database.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="bfe8a-365">Nei test `searchGenre` per `searchTitle`e , `searchTerm` il codice imposta il valore che si desidera cercare.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="bfe8a-366">Tali blocchi di `selectCommand` codice vengono inoltre impostati su un comando SQL appropriato per tale ricerca.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="bfe8a-367">Il `db.Query` metodo viene richiamato una sola volta, utilizzando qualsiasi comando SQL è in `selectedCommand` e qualsiasi valore è in `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="bfe8a-368">Se non è presente alcun termine di ricerca (nessun genere e nessuna parola titolo), il valore di `searchTerm` è una stringa vuota.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="bfe8a-369">Tuttavia, questo non importa, perché in questo caso la query non richiede un parametro.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="bfe8a-370">Test della funzionalità di ricerca del titolo</span><span class="sxs-lookup"><span data-stu-id="bfe8a-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="bfe8a-371">Ora puoi testare la tua pagina di ricerca completata.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-371">Now you can test your completed search page.</span></span> <span data-ttu-id="bfe8a-372">Eseguire *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="bfe8a-373">Immettere un genere e fare clic su **Cerca genere**.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="bfe8a-374">La griglia visualizza filmati di quel genere, come prima.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="bfe8a-375">Immettere una parola titolo e fare clic su **Cerca titolo**.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="bfe8a-376">Nella griglia vengono visualizzati i filmati con tale parola nel titolo.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-376">The grid displays movies that have that word in the title.</span></span>

![Elenco della pagina dei film dopo aver cercato 'The' nel titolo](form-basics/_static/image6.png)

<span data-ttu-id="bfe8a-378">Lasciare vuote entrambe le caselle di testo e fare clic su uno dei pulsanti.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="bfe8a-379">La griglia visualizza tutti i filmati.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="bfe8a-380">Combinazione delle query</span><span class="sxs-lookup"><span data-stu-id="bfe8a-380">Combining the Queries</span></span>

<span data-ttu-id="bfe8a-381">Potresti notare che le ricerche che puoi eseguire sono esclusive.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="bfe8a-382">Non è possibile cercare il titolo e il genere contemporaneamente, anche se entrambe le caselle di ricerca con.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="bfe8a-383">Ad esempio, non è possibile cercare tutti i film d'azione il cui titolo contiene "Avventura".</span><span class="sxs-lookup"><span data-stu-id="bfe8a-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="bfe8a-384">Poiché la pagina è codificata ora, se si immettono valori sia per il genere che per il titolo, la ricerca del titolo ha la precedenza. Per creare una ricerca che combina le condizioni, è necessario creare una query SQL con sintassi simile alla seguente:</span><span class="sxs-lookup"><span data-stu-id="bfe8a-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="bfe8a-385">E si avrebbe dovuto eseguire la query utilizzando un'istruzione simile alla seguente (approssimativamente parlando):</span><span class="sxs-lookup"><span data-stu-id="bfe8a-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="bfe8a-386">La creazione di logica per consentire molte permutazioni dei criteri di ricerca può coinvolgere un po', come si può vedere.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="bfe8a-387">Pertanto, ci fermeremo qui.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="bfe8a-388">Prossimamente</span><span class="sxs-lookup"><span data-stu-id="bfe8a-388">Coming Up Next</span></span>

<span data-ttu-id="bfe8a-389">Nell'esercitazione successiva verrà creata una pagina che usa un modulo per consentire agli utenti di aggiungere filmati al database.</span><span class="sxs-lookup"><span data-stu-id="bfe8a-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="bfe8a-390">Elenco completo per la pagina del filmato (aggiornato con la ricerca)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="bfe8a-391">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="bfe8a-391">Additional Resources</span></span>

- [<span data-ttu-id="bfe8a-392">Introduzione alla programmazione Web ASP.NET utilizzando la sintassi Razor</span><span class="sxs-lookup"><span data-stu-id="bfe8a-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="bfe8a-393">[Clausola SQL WHERE](http://www.w3schools.com/sql/sql_where.asp) nel sito W3Schools</span><span class="sxs-lookup"><span data-stu-id="bfe8a-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="bfe8a-394">[Articolo sulle definizioni](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) dei metodi nel sito W3C</span><span class="sxs-lookup"><span data-stu-id="bfe8a-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="bfe8a-395">[Successivo](displaying-data.md)
> [precedente](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="bfe8a-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
