---
uid: signalr/overview/security/introduction-to-security
title: Introduzione alla sicurezza SignalR Documenti Microsoft
author: bradygaster
description: Descrive i problemi di sicurezza da considerare quando si sviluppa un'applicazione SignalR.
ms.author: bradyg
ms.date: 06/10/2014
ms.assetid: ed562717-8591-4936-8e10-c7e63dcb570a
msc.legacyurl: /signalr/overview/security/introduction-to-security
msc.type: authoredcontent
ms.openlocfilehash: 24ce20b45543468de28ad017ba62d2f6e5a00f3b
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 04/06/2020
ms.locfileid: "80676195"
---
# <a name="introduction-to-signalr-security"></a><span data-ttu-id="9e859-103">Introduzione alla sicurezza di SignalR</span><span class="sxs-lookup"><span data-stu-id="9e859-103">Introduction to SignalR Security</span></span>

<span data-ttu-id="9e859-104">di [Patrick Fletcher](https://github.com/pfletcher), [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="9e859-104">by [Patrick Fletcher](https://github.com/pfletcher), [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

[!INCLUDE [Consider ASP.NET Core SignalR](~/includes/signalr/signalr-version-disambiguation.md)]

> <span data-ttu-id="9e859-105">In questo articolo vengono descritti i problemi di sicurezza da considerare quando si sviluppa un'applicazione SignalR.</span><span class="sxs-lookup"><span data-stu-id="9e859-105">This article describes the security issues you must consider when developing a SignalR application.</span></span>
>
> ## <a name="software-versions-used-in-this-topic"></a><span data-ttu-id="9e859-106">Versioni software utilizzate in questo argomento</span><span class="sxs-lookup"><span data-stu-id="9e859-106">Software versions used in this topic</span></span>
>
>
> - [<span data-ttu-id="9e859-107">Visual Studio 2013</span><span class="sxs-lookup"><span data-stu-id="9e859-107">Visual Studio 2013</span></span>](https://my.visualstudio.com/Downloads?q=visual%20studio%202013)
> - <span data-ttu-id="9e859-108">.NET 4.5</span><span class="sxs-lookup"><span data-stu-id="9e859-108">.NET 4.5</span></span>
> - <span data-ttu-id="9e859-109">SignalR versione 2</span><span class="sxs-lookup"><span data-stu-id="9e859-109">SignalR version 2</span></span>
>
>
>
> ## <a name="previous-versions-of-this-topic"></a><span data-ttu-id="9e859-110">Versioni precedenti di questo argomento</span><span class="sxs-lookup"><span data-stu-id="9e859-110">Previous versions of this topic</span></span>
>
> <span data-ttu-id="9e859-111">Per informazioni sulle versioni precedenti di SignalR, vedere [SignalR Older Versions](../older-versions/index.md).</span><span class="sxs-lookup"><span data-stu-id="9e859-111">For information about earlier versions of SignalR, see [SignalR Older Versions](../older-versions/index.md).</span></span>
>
> ## <a name="questions-and-comments"></a><span data-ttu-id="9e859-112">Domande e commenti</span><span class="sxs-lookup"><span data-stu-id="9e859-112">Questions and comments</span></span>
>
> <span data-ttu-id="9e859-113">Si prega di lasciare un feedback su come ti è piaciuto questo tutorial e cosa potremmo migliorare nei commenti in fondo alla pagina.</span><span class="sxs-lookup"><span data-stu-id="9e859-113">Please leave feedback on how you liked this tutorial and what we could improve in the comments at the bottom of the page.</span></span> <span data-ttu-id="9e859-114">Se avete domande che non sono direttamente correlati al tutorial, è possibile pubblicarli al [forum ASP.NET SignalR](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) o [StackOverflow.com](http://stackoverflow.com/).</span><span class="sxs-lookup"><span data-stu-id="9e859-114">If you have questions that are not directly related to the tutorial, you can post them to the [ASP.NET SignalR forum](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) or [StackOverflow.com](http://stackoverflow.com/).</span></span>

## <a name="overview"></a><span data-ttu-id="9e859-115">Panoramica</span><span class="sxs-lookup"><span data-stu-id="9e859-115">Overview</span></span>

<span data-ttu-id="9e859-116">Questo documento contiene le seguenti sezioni:</span><span class="sxs-lookup"><span data-stu-id="9e859-116">This document contains the following sections:</span></span>

- [<span data-ttu-id="9e859-117">Concetti di sicurezza SignalR</span><span class="sxs-lookup"><span data-stu-id="9e859-117">SignalR Security Concepts</span></span>](#concepts)

    - [<span data-ttu-id="9e859-118">Autenticazione e autorizzazione</span><span class="sxs-lookup"><span data-stu-id="9e859-118">Authentication and authorization</span></span>](#authentication)
    - [<span data-ttu-id="9e859-119">Token di connessione</span><span class="sxs-lookup"><span data-stu-id="9e859-119">Connection token</span></span>](#connectiontoken)
    - [<span data-ttu-id="9e859-120">Ricongiungirsi ai gruppi durante la riconnessione</span><span class="sxs-lookup"><span data-stu-id="9e859-120">Rejoining groups when reconnecting</span></span>](#rejoingroup)
- [<span data-ttu-id="9e859-121">Come SignalR impedisce la contraffazione delle richieste tra siti</span><span class="sxs-lookup"><span data-stu-id="9e859-121">How SignalR prevents Cross-Site Request Forgery</span></span>](#csrf)
- [<span data-ttu-id="9e859-122">Raccomandazioni di sicurezza SignalR</span><span class="sxs-lookup"><span data-stu-id="9e859-122">SignalR Security Recommendations</span></span>](#recommendations)

    - [<span data-ttu-id="9e859-123">Protocollo SSL (Secure Socket Layers)</span><span class="sxs-lookup"><span data-stu-id="9e859-123">Secure Socket Layers (SSL) protocol</span></span>](#ssl)
    - [<span data-ttu-id="9e859-124">Non utilizzare i gruppi come meccanismo di sicurezza</span><span class="sxs-lookup"><span data-stu-id="9e859-124">Do not use groups as a security mechanism</span></span>](#groupsecurity)
    - [<span data-ttu-id="9e859-125">Gestione sicura dell'input dai client</span><span class="sxs-lookup"><span data-stu-id="9e859-125">Safely handling input from clients</span></span>](#input)
    - [<span data-ttu-id="9e859-126">Riconciliare una modifica dello stato utente con una connessione attiva</span><span class="sxs-lookup"><span data-stu-id="9e859-126">Reconciling a change in user status with an active connection</span></span>](#reconcile)
    - [<span data-ttu-id="9e859-127">File proxy JavaScript generati automaticamente</span><span class="sxs-lookup"><span data-stu-id="9e859-127">Automatically generated JavaScript proxy files</span></span>](#autogen)
    - [<span data-ttu-id="9e859-128">Eccezioni</span><span class="sxs-lookup"><span data-stu-id="9e859-128">Exceptions</span></span>](#exceptions)

<a id="concepts"></a>

## <a name="signalr-security-concepts"></a><span data-ttu-id="9e859-129">Concetti di sicurezza SignalR</span><span class="sxs-lookup"><span data-stu-id="9e859-129">SignalR Security Concepts</span></span>

<a id="authentication"></a>

### <a name="authentication-and-authorization"></a><span data-ttu-id="9e859-130">Autenticazione e autorizzazione</span><span class="sxs-lookup"><span data-stu-id="9e859-130">Authentication and authorization</span></span>

<span data-ttu-id="9e859-131">SignalR non fornisce alcuna funzionalità per l'autenticazione degli utenti.</span><span class="sxs-lookup"><span data-stu-id="9e859-131">SignalR does not provide any features for authenticating users.</span></span> <span data-ttu-id="9e859-132">Al contrario, si integrano le funzionalità SignalR nella struttura di autenticazione esistente per un'applicazione.</span><span class="sxs-lookup"><span data-stu-id="9e859-132">Instead, you integrate the SignalR features into the existing authentication structure for an application.</span></span> <span data-ttu-id="9e859-133">Gli utenti vengono autenticati come di consueto nell'applicazione e si utilizzano i risultati dell'autenticazione nel codice SignalR.</span><span class="sxs-lookup"><span data-stu-id="9e859-133">You authenticate users as you would normally in your application, and work with the results of the authentication in your SignalR code.</span></span> <span data-ttu-id="9e859-134">Ad esempio, è possibile autenticare gli utenti con ASP.NET l'autenticazione basata su form e quindi nell'hub imporre quali utenti o ruoli sono autorizzati a chiamare un metodo.</span><span class="sxs-lookup"><span data-stu-id="9e859-134">For example, you might authenticate your users with ASP.NET forms authentication, and then in your hub, enforce which users or roles are authorized to call a method.</span></span> <span data-ttu-id="9e859-135">Nell'hub è inoltre possibile passare al client le informazioni di autenticazione, ad esempio il nome utente o se un utente appartiene a un ruolo.</span><span class="sxs-lookup"><span data-stu-id="9e859-135">In your hub, you can also pass authentication information, such as user name or whether a user belongs to a role, to the client.</span></span>

<span data-ttu-id="9e859-136">SignalR fornisce l'attributo [Authorize](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) per specificare quali utenti hanno accesso a un hub o a un metodo.</span><span class="sxs-lookup"><span data-stu-id="9e859-136">SignalR provides the [Authorize](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) attribute to specify which users have access to a hub or method.</span></span> <span data-ttu-id="9e859-137">Si applica l'attributo Authorize a un hub o a metodi particolari in un hub.</span><span class="sxs-lookup"><span data-stu-id="9e859-137">You apply the Authorize attribute to either a hub or particular methods in a hub.</span></span> <span data-ttu-id="9e859-138">Senza l'attributo Authorize, tutti i metodi pubblici nell'hub sono disponibili per un client connesso all'hub.</span><span class="sxs-lookup"><span data-stu-id="9e859-138">Without the Authorize attribute, all public methods on the hub are available to a client that is connected to the hub.</span></span> <span data-ttu-id="9e859-139">Per ulteriori informazioni sugli hub, vedere [Autenticazione e autorizzazione per hub SignalR](hub-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="9e859-139">For more information about hubs, see [Authentication and Authorization for SignalR Hubs](hub-authorization.md).</span></span>

<span data-ttu-id="9e859-140">Si applica `Authorize` l'attributo agli hub, ma non alle connessioni permanenti.</span><span class="sxs-lookup"><span data-stu-id="9e859-140">You apply the `Authorize` attribute to hubs, but not persistent connections.</span></span> <span data-ttu-id="9e859-141">Per applicare le regole `PersistentConnection` di autorizzazione `AuthorizeRequest` quando si utilizza un è necessario eseguire l'override del metodo.</span><span class="sxs-lookup"><span data-stu-id="9e859-141">To enforce authorization rules when using a `PersistentConnection` you must override the `AuthorizeRequest` method.</span></span> <span data-ttu-id="9e859-142">Per ulteriori informazioni sulle connessioni permanenti, vedere [Autenticazione e autorizzazione per connessioni persistenti SignalR](persistent-connection-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="9e859-142">For more information about persistent connections, see [Authentication and Authorization for SignalR Persistent Connections](persistent-connection-authorization.md).</span></span>

<a id="connectiontoken"></a>

### <a name="connection-token"></a><span data-ttu-id="9e859-143">Token di connessione</span><span class="sxs-lookup"><span data-stu-id="9e859-143">Connection token</span></span>

<span data-ttu-id="9e859-144">SignalR riduce il rischio di esecuzione di comandi dannosi convalidando l'identità del mittente.</span><span class="sxs-lookup"><span data-stu-id="9e859-144">SignalR mitigates the risk of executing malicious commands by validating the identity of the sender.</span></span> <span data-ttu-id="9e859-145">Per ogni richiesta, il client e il server passano un token di connessione che contiene l'ID di connessione e il nome utente per gli utenti autenticati.</span><span class="sxs-lookup"><span data-stu-id="9e859-145">For each request, the client and server pass a connection token which contains the connection id and username for authenticated users.</span></span> <span data-ttu-id="9e859-146">L'ID di connessione identifica in modo univoco ogni client connesso.</span><span class="sxs-lookup"><span data-stu-id="9e859-146">The connection id uniquely identifies each connected client.</span></span> <span data-ttu-id="9e859-147">Il server genera in modo casuale l'ID di connessione quando viene creata una nuova connessione e mantiene tale ID per la durata della connessione.</span><span class="sxs-lookup"><span data-stu-id="9e859-147">The server randomly generates the connection id when a new connection is created, and persists that id for the duration of the connection.</span></span> <span data-ttu-id="9e859-148">Il meccanismo di autenticazione per l'applicazione web fornisce il nome utente.</span><span class="sxs-lookup"><span data-stu-id="9e859-148">The authentication mechanism for the web application provides the username.</span></span> <span data-ttu-id="9e859-149">SignalR utilizza la crittografia e una firma digitale per proteggere il token di connessione.</span><span class="sxs-lookup"><span data-stu-id="9e859-149">SignalR uses encryption and a digital signature to protect the connection token.</span></span>

![](introduction-to-security/_static/image2.png)

<span data-ttu-id="9e859-150">Per ogni richiesta, il server convalida il contenuto del token per garantire che la richiesta provenga dall'utente specificato.</span><span class="sxs-lookup"><span data-stu-id="9e859-150">For each request, the server validates the contents of the token to ensure that the request is coming from the specified user.</span></span> <span data-ttu-id="9e859-151">Il nome utente deve corrispondere all'ID di connessione. Convalidando sia l'ID di connessione che il nome utente, SignalR impedisce a un utente malintenzionato di impersonare facilmente un altro utente.</span><span class="sxs-lookup"><span data-stu-id="9e859-151">The username must correspond to the connection id. By validating both the connection id and the username, SignalR prevents a malicious user from easily impersonating another user.</span></span> <span data-ttu-id="9e859-152">Se il server non è in grado di convalidare il token di connessione, la richiesta ha esito negativo.</span><span class="sxs-lookup"><span data-stu-id="9e859-152">If the server cannot validate the connection token, the request fails.</span></span>

![](introduction-to-security/_static/image4.png)

<span data-ttu-id="9e859-153">Poiché l'ID di connessione fa parte del processo di verifica, non è consigliabile rivelare l'ID di connessione di un utente ad altri utenti o archiviare il valore nel client, ad esempio in un cookie.</span><span class="sxs-lookup"><span data-stu-id="9e859-153">Because the connection id is part of the verification process, you should not reveal one user's connection id to other users or store the value on the client, such as in a cookie.</span></span>

#### <a name="connection-tokens-vs-other-token-types"></a><span data-ttu-id="9e859-154">Token di connessione e altri tipi di tokenConnection tokens vs.</span><span class="sxs-lookup"><span data-stu-id="9e859-154">Connection tokens vs. other token types</span></span>

<span data-ttu-id="9e859-155">I token di connessione vengono occasionalmente contrassegnati dagli strumenti di sicurezza perché sembrano essere token di sessione o token di autenticazione, il che rappresenta un rischio se esposti.</span><span class="sxs-lookup"><span data-stu-id="9e859-155">Connection tokens are occasionally flagged by security tools because they appear to be session tokens or authentication tokens, which poses a risk if exposed.</span></span>

<span data-ttu-id="9e859-156">Il token di connessione di SignalR non è un token di autenticazione.</span><span class="sxs-lookup"><span data-stu-id="9e859-156">SignalR's connection token isn't an authentication token.</span></span> <span data-ttu-id="9e859-157">Viene utilizzato per confermare che l'utente che effettua la richiesta è la stessa che ha creato la connessione.</span><span class="sxs-lookup"><span data-stu-id="9e859-157">It is used to confirm that the user making this request is the same one that created the connection.</span></span> <span data-ttu-id="9e859-158">Il token di connessione è necessario perché ASP.NET SignalR consente lo spostamento delle connessioni tra i server.</span><span class="sxs-lookup"><span data-stu-id="9e859-158">The connection token is necessary because ASP.NET SignalR allows connections to move between servers.</span></span> <span data-ttu-id="9e859-159">Il token associa la connessione a un utente specifico ma non assiafferma l'identità dell'utente che effettua la richiesta.</span><span class="sxs-lookup"><span data-stu-id="9e859-159">The token associates the connection with a particular user but doesn't assert the identity of the user making the request.</span></span> <span data-ttu-id="9e859-160">Affinché una richiesta SignalR venga autenticata correttamente, deve disporre di un altro token che asserisce l'identità dell'utente, ad esempio un cookie o un token di connessione.</span><span class="sxs-lookup"><span data-stu-id="9e859-160">For a SignalR request to be properly authenticated, it must have some other token that asserts the identity of the user, such as a cookie or bearer token.</span></span> <span data-ttu-id="9e859-161">Tuttavia, il token di connessione stesso non fa alcuna affermazione che la richiesta è stata effettuata da tale utente, solo che l'ID di connessione contenuto all'interno del token è associato a tale utente.</span><span class="sxs-lookup"><span data-stu-id="9e859-161">However, the connection token itself makes no claim that the request was made by that user, only that the connection ID contained within the token is associated with that user.</span></span>

<span data-ttu-id="9e859-162">Poiché il token di connessione non fornisce alcuna attestazione di autenticazione propria, non è considerato un token "sessione" o "autenticazione".</span><span class="sxs-lookup"><span data-stu-id="9e859-162">Since the connection token provides no authentication claim of its own, it isn't considered a "session" or "authentication" token.</span></span> <span data-ttu-id="9e859-163">Prendendo il token di connessione di un determinato utente e riproducendolo in una richiesta autenticata come un utente diverso (o una richiesta non autenticata) avrà esito negativo, perché l'identità utente della richiesta e l'identità archiviata nel token non corrisponderà.</span><span class="sxs-lookup"><span data-stu-id="9e859-163">Taking a given user's connection token and replaying it in a request authenticated as a different user (or an unauthenticated request) will fail, because the user identity of the request and the identity stored in the token won't match.</span></span>

<a id="rejoingroup"></a>

### <a name="rejoining-groups-when-reconnecting"></a><span data-ttu-id="9e859-164">Ricongiungirsi ai gruppi durante la riconnessione</span><span class="sxs-lookup"><span data-stu-id="9e859-164">Rejoining groups when reconnecting</span></span>

<span data-ttu-id="9e859-165">Per impostazione predefinita, l'applicazione SignalR riassegnerà automaticamente un utente ai gruppi appropriati quando si riconnette da un'interruzione temporanea, ad esempio quando una connessione viene interrotta e ristabilita prima del timeout della connessione. Quando si riconnette, il client passa un token di gruppo che include l'ID di connessione e i gruppi assegnati.</span><span class="sxs-lookup"><span data-stu-id="9e859-165">By default, the SignalR application will automatically re-assign a user to the appropriate groups when reconnecting from a temporary disruption, such as when a connection is dropped and re-established before the connection times out. When reconnecting, the client passes a group token that includes the connection id and the assigned groups.</span></span> <span data-ttu-id="9e859-166">Il token di gruppo è firmato digitalmente e crittografato.</span><span class="sxs-lookup"><span data-stu-id="9e859-166">The group token is digitally signed and encrypted.</span></span> <span data-ttu-id="9e859-167">Il client mantiene lo stesso ID di connessione dopo una riconnessione; pertanto, l'ID di connessione passato dal client riconnesso deve corrispondere all'ID di connessione precedente utilizzato dal client.</span><span class="sxs-lookup"><span data-stu-id="9e859-167">The client retains the same connection id after a reconnection; therefore, the connection id passed from the reconnected client must match the previous connection id used by the client.</span></span> <span data-ttu-id="9e859-168">Questa verifica impedisce a un utente malintenzionato di passare le richieste di unirsi a gruppi non autorizzati durante la riconnessione.</span><span class="sxs-lookup"><span data-stu-id="9e859-168">This verification prevents a malicious user from passing requests to join unauthorized groups when reconnecting.</span></span>

<span data-ttu-id="9e859-169">Tuttavia, è importante notare che il token di gruppo non scade.</span><span class="sxs-lookup"><span data-stu-id="9e859-169">However, it's important to note, that the group token does not expire.</span></span> <span data-ttu-id="9e859-170">Se un utente apparteneva a un gruppo in passato, ma è stato escluso da tale gruppo, tale utente potrebbe essere in grado di simulare un token di gruppo che include il gruppo non consentito.</span><span class="sxs-lookup"><span data-stu-id="9e859-170">If a user belonged to a group in the past, but was banned from that group, that user may be able to mimic a group token that includes the prohibited group.</span></span> <span data-ttu-id="9e859-171">Se è necessario gestire in modo sicuro quali utenti appartengono a quali gruppi, è necessario archiviare i dati nel server, ad esempio in un database.</span><span class="sxs-lookup"><span data-stu-id="9e859-171">If you need to securely manage which users belong to which groups, you need to store that data on the server, such as in a database.</span></span> <span data-ttu-id="9e859-172">Aggiungere quindi la logica all'applicazione che verifica sul server se un utente appartiene a un gruppo.</span><span class="sxs-lookup"><span data-stu-id="9e859-172">Then, add logic to your application that verifies on the server whether a user belongs to a group.</span></span> <span data-ttu-id="9e859-173">Per un esempio di verifica dell'appartenenza ai gruppi, vedere [Utilizzo dei gruppi.](../guide-to-the-api/working-with-groups.md)</span><span class="sxs-lookup"><span data-stu-id="9e859-173">For an example of verifying group membership, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<span data-ttu-id="9e859-174">Il ricongiungito automaticamente i gruppi si applica solo quando una connessione viene riconnessa dopo un'interruzione temporanea.</span><span class="sxs-lookup"><span data-stu-id="9e859-174">Automatically rejoining groups only applies when a connection is reconnected after a temporary disruption.</span></span> <span data-ttu-id="9e859-175">Se un utente si disconnette allontanandosi dall'applicazione o l'applicazione viene riavviata, l'applicazione deve gestire come aggiungere l'utente ai gruppi corretti.</span><span class="sxs-lookup"><span data-stu-id="9e859-175">If a user disconnects by navigating away from the application or the application restarts, your application must handle how to add that user to the correct groups.</span></span> <span data-ttu-id="9e859-176">Per ulteriori informazioni, consultate Operazioni con i [gruppi.](../guide-to-the-api/working-with-groups.md)</span><span class="sxs-lookup"><span data-stu-id="9e859-176">For more information, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="csrf"></a>

## <a name="how-signalr-prevents-cross-site-request-forgery"></a><span data-ttu-id="9e859-177">Come SignalR impedisce la contraffazione delle richieste tra siti</span><span class="sxs-lookup"><span data-stu-id="9e859-177">How SignalR prevents Cross-Site Request Forgery</span></span>

<span data-ttu-id="9e859-178">Cross-Site Request Forgery (CSRF) è un attacco in cui un sito dannoso invia una richiesta a un sito vulnerabile in cui l'utente è attualmente connesso.</span><span class="sxs-lookup"><span data-stu-id="9e859-178">Cross-Site Request Forgery (CSRF) is an attack where a malicious site sends a request to a vulnerable site where the user is currently logged in.</span></span> <span data-ttu-id="9e859-179">SignalR impedisce CSRF rendendo estremamente improbabile per un sito dannoso creare una richiesta valida per l'applicazione SignalR.</span><span class="sxs-lookup"><span data-stu-id="9e859-179">SignalR prevents CSRF by making it extremely unlikely for a malicious site to create a valid request for your SignalR application.</span></span>

### <a name="description-of-csrf-attack"></a><span data-ttu-id="9e859-180">Descrizione dell'attacco CSRF</span><span class="sxs-lookup"><span data-stu-id="9e859-180">Description of CSRF attack</span></span>

<span data-ttu-id="9e859-181">Ecco un esempio di attacco CSRF:</span><span class="sxs-lookup"><span data-stu-id="9e859-181">Here is an example of a CSRF attack:</span></span>

1. <span data-ttu-id="9e859-182">Un utente accede a www.example.com, utilizzando l'autenticazione basata su form.</span><span class="sxs-lookup"><span data-stu-id="9e859-182">A user logs into www.example.com, using forms authentication.</span></span>
2. <span data-ttu-id="9e859-183">Il server autentica l'utente.</span><span class="sxs-lookup"><span data-stu-id="9e859-183">The server authenticates the user.</span></span> <span data-ttu-id="9e859-184">La risposta dal server include un cookie di autenticazione.</span><span class="sxs-lookup"><span data-stu-id="9e859-184">The response from the server includes an authentication cookie.</span></span>
3. <span data-ttu-id="9e859-185">Senza disconnettersi, l'utente visita un sito web dannoso.</span><span class="sxs-lookup"><span data-stu-id="9e859-185">Without logging out, the user visits a malicious web site.</span></span> <span data-ttu-id="9e859-186">Questo sito dannoso contiene il seguente modulo HTML:</span><span class="sxs-lookup"><span data-stu-id="9e859-186">This malicious site contains the following HTML form:</span></span>

    [!code-html[Main](introduction-to-security/samples/sample1.html)]

   <span data-ttu-id="9e859-187">Si noti che l'azione modulo invia al sito vulnerabile, non al sito dannoso.</span><span class="sxs-lookup"><span data-stu-id="9e859-187">Notice that the form action posts to the vulnerable site, not to the malicious site.</span></span> <span data-ttu-id="9e859-188">Questa è la parte "cross-site" di CSRF.</span><span class="sxs-lookup"><span data-stu-id="9e859-188">This is the "cross-site" part of CSRF.</span></span>
4. <span data-ttu-id="9e859-189">L'utente fa clic sul pulsante di invio.</span><span class="sxs-lookup"><span data-stu-id="9e859-189">The user clicks the submit button.</span></span> <span data-ttu-id="9e859-190">Il browser include il cookie di autenticazione con la richiesta.</span><span class="sxs-lookup"><span data-stu-id="9e859-190">The browser includes the authentication cookie with the request.</span></span>
5. <span data-ttu-id="9e859-191">La richiesta viene eseguita sul server example.com con il contesto di autenticazione dell'utente e può eseguire qualsiasi operazione consentita a un utente autenticato.</span><span class="sxs-lookup"><span data-stu-id="9e859-191">The request runs on the example.com server with the user's authentication context, and can do anything that an authenticated user is allowed to do.</span></span>

<span data-ttu-id="9e859-192">Anche se questo esempio richiede all'utente di fare clic sul pulsante del modulo, la pagina dannosa potrebbe altrettanto facilmente eseguire uno script che invia una richiesta AJAX all'applicazione SignalR.</span><span class="sxs-lookup"><span data-stu-id="9e859-192">Although this example requires the user to click the form button, the malicious page could just as easily run a script that sends an AJAX request to your SignalR application.</span></span> <span data-ttu-id="9e859-193">Inoltre, l'utilizzo di SSL non impedisce un attacco CSRF, perché il sito dannoso può inviare una richiesta "https://".</span><span class="sxs-lookup"><span data-stu-id="9e859-193">Moreover, using SSL does not prevent a CSRF attack, because the malicious site can send an "https://" request.</span></span>

<span data-ttu-id="9e859-194">In genere, gli attacchi CSRF sono possibili contro siti web che utilizzano i cookie per l'autenticazione, perché i browser inviano tutti i cookie rilevanti al sito web di destinazione.</span><span class="sxs-lookup"><span data-stu-id="9e859-194">Typically, CSRF attacks are possible against web sites that use cookies for authentication, because browsers send all relevant cookies to the destination web site.</span></span> <span data-ttu-id="9e859-195">Tuttavia, gli attacchi CSRF non si limitano allo sfruttamento dei cookie.</span><span class="sxs-lookup"><span data-stu-id="9e859-195">However, CSRF attacks are not limited to exploiting cookies.</span></span> <span data-ttu-id="9e859-196">Ad esempio, anche l'autenticazione di base e digest sono vulnerabili.</span><span class="sxs-lookup"><span data-stu-id="9e859-196">For example, Basic and Digest authentication are also vulnerable.</span></span> <span data-ttu-id="9e859-197">Dopo che un utente accede con l'autenticazione di base o digest, il browser invia automaticamente le credenziali fino al termine della sessione.</span><span class="sxs-lookup"><span data-stu-id="9e859-197">After a user logs in with Basic or Digest authentication, the browser automatically sends the credentials until the session ends.</span></span>

### <a name="csrf-mitigations-taken-by-signalr"></a><span data-ttu-id="9e859-198">Mitigazioni CSRF prese da SignalR</span><span class="sxs-lookup"><span data-stu-id="9e859-198">CSRF mitigations taken by SignalR</span></span>

<span data-ttu-id="9e859-199">SignalR esegue la procedura seguente per impedire a un sito dannoso di creare richieste valide per l'applicazione.</span><span class="sxs-lookup"><span data-stu-id="9e859-199">SignalR takes the following steps to prevent a malicious site from creating valid requests to your application.</span></span> <span data-ttu-id="9e859-200">SignalR esegue questi passaggi per impostazione predefinita, non è necessario eseguire alcuna azione nel codice.</span><span class="sxs-lookup"><span data-stu-id="9e859-200">SignalR takes these steps by default, you do not need to take any action in your code.</span></span>

- <span data-ttu-id="9e859-201">**Disabilitare le richieste tra dominiDisable cross domain requests** SignalR disabilita le richieste tra domini per impedire agli utenti di chiamare un endpoint SignalR da un dominio esterno.</span><span class="sxs-lookup"><span data-stu-id="9e859-201">**Disable cross domain requests** SignalR disables cross domain requests to prevent users from calling a SignalR endpoint from an external domain.</span></span> <span data-ttu-id="9e859-202">SignalR considera qualsiasi richiesta da un dominio esterno non valida e blocca la richiesta.</span><span class="sxs-lookup"><span data-stu-id="9e859-202">SignalR considers any request from an external domain to be invalid and blocks the request.</span></span> <span data-ttu-id="9e859-203">Si consiglia di mantenere questo comportamento predefinito; in caso contrario, un sito dannoso potrebbe indurre gli utenti a inviare comandi al sito.</span><span class="sxs-lookup"><span data-stu-id="9e859-203">We recommend that you keep this default behavior; otherwise, a malicious site could trick users into sending commands to your site.</span></span> <span data-ttu-id="9e859-204">Se è necessario utilizzare richieste tra domini, vedere [Come stabilire una connessione tra](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) domini .</span><span class="sxs-lookup"><span data-stu-id="9e859-204">If you need to use cross domain requests, see     [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) .</span></span>
- <span data-ttu-id="9e859-205">Passare il token di connessione nella stringa di **query, non nel cookiePass connection token in query string, not cookie** SignalR passa il token di connessione come valore della stringa di query, anziché come cookie.</span><span class="sxs-lookup"><span data-stu-id="9e859-205">**Pass connection token in query string, not cookie** SignalR passes the connection token as a query string value, instead of as a cookie.</span></span> <span data-ttu-id="9e859-206">L'archiviazione del token di connessione in un cookie non è sicura perché il browser può inavvertitamente inoltrare il token di connessione quando viene rilevato codice dannoso.</span><span class="sxs-lookup"><span data-stu-id="9e859-206">Storing the connection token in a cookie is unsafe because the browser can inadvertently forward the connection token when malicious code is encountered.</span></span> <span data-ttu-id="9e859-207">Inoltre, il passaggio del token di connessione nella stringa di query impedisce che il token di connessione persista oltre la connessione corrente.</span><span class="sxs-lookup"><span data-stu-id="9e859-207">Also, passing the connection token in the query string prevents the connection token from persisting beyond the current connection.</span></span> <span data-ttu-id="9e859-208">Pertanto, un utente malintenzionato non può effettuare una richiesta con le credenziali di autenticazione di un altro utente.</span><span class="sxs-lookup"><span data-stu-id="9e859-208">Therefore, a malicious user cannot make a request under another user's authentication credentials.</span></span>
- <span data-ttu-id="9e859-209">**Verificare il token di connessioneVerify connection token** Come descritto nella sezione Token di [connessione,](#connectiontoken) il server sa quale ID di connessione è associato a ogni utente autenticato.</span><span class="sxs-lookup"><span data-stu-id="9e859-209">**Verify connection token** As described in the     [Connection token](#connectiontoken) section, the server knows which connection id is associated with each authenticated user.</span></span> <span data-ttu-id="9e859-210">Il server non elabora alcuna richiesta da un ID di connessione che non corrisponde al nome utente.</span><span class="sxs-lookup"><span data-stu-id="9e859-210">The server does not process any request from a connection id that does not match the user name.</span></span> <span data-ttu-id="9e859-211">È improbabile che un utente malintenzionato possa indovinare una richiesta valida perché l'utente malintenzionato dovrebbe conoscere il nome utente e l'ID di connessione generato casualmente corrente. Tale ID di connessione diventa non valido non appena la connessione viene terminata.</span><span class="sxs-lookup"><span data-stu-id="9e859-211">It is unlikely a malicious user could guess a valid request because the malicious user would have to know the user name and the current randomly-generated connection id. That connection id becomes invalid as soon as the connection is ended.</span></span> <span data-ttu-id="9e859-212">Gli utenti anonimi non dovrebbero avere accesso a informazioni riservate.</span><span class="sxs-lookup"><span data-stu-id="9e859-212">Anonymous users should not have access to any sensitive information.</span></span>

<a id="recommendations"></a>

## <a name="signalr-security-recommendations"></a><span data-ttu-id="9e859-213">Raccomandazioni di sicurezza SignalR</span><span class="sxs-lookup"><span data-stu-id="9e859-213">SignalR Security Recommendations</span></span>

<a id="ssl"></a>

### <a name="secure-socket-layers-ssl-protocol"></a><span data-ttu-id="9e859-214">Protocollo SSL (Secure Socket Layers)</span><span class="sxs-lookup"><span data-stu-id="9e859-214">Secure Socket Layers (SSL) protocol</span></span>

<span data-ttu-id="9e859-215">Il protocollo SSL utilizza la crittografia per proteggere il trasporto dei dati tra un client e un server.</span><span class="sxs-lookup"><span data-stu-id="9e859-215">The SSL protocol uses encryption to secure the transport of data between a client and server.</span></span> <span data-ttu-id="9e859-216">Se l'applicazione SignalR trasmette informazioni riservate tra il client e il server, utilizzare SSL per il trasporto.</span><span class="sxs-lookup"><span data-stu-id="9e859-216">If your SignalR application transmits sensitive information between the client and server, use SSL for the transport.</span></span> <span data-ttu-id="9e859-217">Per ulteriori informazioni sulla configurazione di SSL, vedere [Come configurare SSL in IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span><span class="sxs-lookup"><span data-stu-id="9e859-217">For more information about setting up SSL, see [How to set up SSL on IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span></span>

<a id="groupsecurity"></a>

### <a name="do-not-use-groups-as-a-security-mechanism"></a><span data-ttu-id="9e859-218">Non utilizzare i gruppi come meccanismo di sicurezza</span><span class="sxs-lookup"><span data-stu-id="9e859-218">Do not use groups as a security mechanism</span></span>

<span data-ttu-id="9e859-219">I gruppi sono un modo pratico per raccogliere gli utenti correlati, ma non sono un meccanismo sicuro per limitare l'accesso alle informazioni riservate.</span><span class="sxs-lookup"><span data-stu-id="9e859-219">Groups are a convenient way of collecting related users, but they are not a secure mechanism for limiting access to sensitive information.</span></span> <span data-ttu-id="9e859-220">Ciò è particolarmente vero quando gli utenti possono riconnettersi automaticamente i gruppi durante una riconnessione.</span><span class="sxs-lookup"><span data-stu-id="9e859-220">This is especially true when users can automatically rejoin groups during a reconnect.</span></span> <span data-ttu-id="9e859-221">Considerare invece l'aggiunta di utenti con privilegi a un ruolo e la limitazione dell'accesso a un metodo hub solo ai membri di tale ruolo.</span><span class="sxs-lookup"><span data-stu-id="9e859-221">Instead, consider adding privileged users to a role and limiting access to a hub method to only members of that role.</span></span> <span data-ttu-id="9e859-222">Per un esempio di limitazione dell'accesso in base a un ruolo, vedere [Autenticazione e autorizzazione per hub SignalR](hub-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="9e859-222">For an example of restricting access based on a role, see [Authentication and Authorization for SignalR Hubs](hub-authorization.md).</span></span> <span data-ttu-id="9e859-223">Per un esempio di controllo dell'accesso degli utenti ai gruppi durante la riconnessione, vedere [Utilizzo dei gruppi](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="9e859-223">For an example of checking user access to groups when reconnecting, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="input"></a>

### <a name="safely-handling-input-from-clients"></a><span data-ttu-id="9e859-224">Gestione sicura dell'input dai client</span><span class="sxs-lookup"><span data-stu-id="9e859-224">Safely handling input from clients</span></span>

<span data-ttu-id="9e859-225">Per garantire che un utente malintenzionato non invii script ad altri utenti, è necessario codificare tutti gli input dai client destinati alla trasmissione ad altri client.</span><span class="sxs-lookup"><span data-stu-id="9e859-225">To ensure that a malicious user does not send script to other users, you must encode all input from clients that is intended for broadcast to other clients.</span></span> <span data-ttu-id="9e859-226">È consigliabile codificare i messaggi sui client riceventi anziché sul server, perché l'applicazione SignalR può avere molti tipi diversi di client.</span><span class="sxs-lookup"><span data-stu-id="9e859-226">You should encode messages on the receiving clients rather than the server, because your SignalR application may have many different types of clients.</span></span> <span data-ttu-id="9e859-227">Pertanto, la codifica HTML funziona per un client web, ma non per altri tipi di client.</span><span class="sxs-lookup"><span data-stu-id="9e859-227">Therefore, HTML-encoding works for a web client, but not for other types of clients.</span></span> <span data-ttu-id="9e859-228">Ad esempio, un metodo del client Web per visualizzare un messaggio `html()` di chat gestirà in modo sicuro il nome utente e il messaggio chiamando la funzione.</span><span class="sxs-lookup"><span data-stu-id="9e859-228">For example, a web client method to display a chat message would safely handle the user name and message by calling the `html()` function.</span></span>

[!code-html[Main](introduction-to-security/samples/sample2.html?highlight=3-4)]

<a id="reconcile"></a>

### <a name="reconciling-a-change-in-user-status-with-an-active-connection"></a><span data-ttu-id="9e859-229">Riconciliare una modifica dello stato utente con una connessione attiva</span><span class="sxs-lookup"><span data-stu-id="9e859-229">Reconciling a change in user status with an active connection</span></span>

<span data-ttu-id="9e859-230">Se lo stato di autenticazione di un utente cambia mentre esiste una connessione attiva, l'utente riceverà un errore che indica: "L'identità dell'utente non può cambiare durante una connessione SignalR attiva".</span><span class="sxs-lookup"><span data-stu-id="9e859-230">If a user's authentication status changes while an active connection exists, the user will receive an error that states, "The user identity cannot change during an active SignalR connection."</span></span> <span data-ttu-id="9e859-231">In tal caso, l'applicazione deve riconnettersi al server per assicurarsi che l'ID di connessione e il nome utente siano coordinati.</span><span class="sxs-lookup"><span data-stu-id="9e859-231">In that case, your application should re-connect to the server to make sure the connection id and username are coordinated.</span></span> <span data-ttu-id="9e859-232">Ad esempio, se l'applicazione consente all'utente di disconnettersi mentre esiste una connessione attiva, il nome utente per la connessione non corrisponderà più al nome che viene passato per la richiesta successiva.</span><span class="sxs-lookup"><span data-stu-id="9e859-232">For example, if your application allows the user to log out while an active connection exists, the username for the connection will no longer match the name that is passed in for the next request.</span></span> <span data-ttu-id="9e859-233">È consigliabile interrompere la connessione prima che l'utente si disconta e quindi riavviarla.</span><span class="sxs-lookup"><span data-stu-id="9e859-233">You will want to stop the connection before the user logs out, and then restart it.</span></span>

<span data-ttu-id="9e859-234">Tuttavia, è importante notare che la maggior parte delle applicazioni non sarà necessario arrestare e avviare manualmente la connessione.</span><span class="sxs-lookup"><span data-stu-id="9e859-234">However, it is important to note that most applications will not need to manually stop and start the connection.</span></span> <span data-ttu-id="9e859-235">Se l'applicazione reindirizza gli utenti a una pagina separata dopo la disconnessione, ad esempio il comportamento predefinito in un'applicazione Web Form o MVC, o aggiorna la pagina corrente dopo la disconnessione, la connessione attiva viene disconnessa automaticamente e non richiede alcuna azione aggiuntiva.</span><span class="sxs-lookup"><span data-stu-id="9e859-235">If your application redirects users to a separate page after logging out, such as the default behavior in a Web Forms application or MVC application, or refreshes the current page after logging out, the active connection is automatically disconnected and does not require any additional action.</span></span>

<span data-ttu-id="9e859-236">Nell'esempio seguente viene illustrato come arrestare e avviare una connessione quando lo stato dell'utente è cambiato.</span><span class="sxs-lookup"><span data-stu-id="9e859-236">The following example shows how to stop and start a connection when the user status has changed.</span></span>

[!code-html[Main](introduction-to-security/samples/sample3.html)]

<span data-ttu-id="9e859-237">In alternativa, lo stato di autenticazione dell'utente può cambiare se il sito utilizza la scadenza variabile con l'autenticazione basata su form e non vi è alcuna attività per mantenere valido il cookie di autenticazione.</span><span class="sxs-lookup"><span data-stu-id="9e859-237">Or, the user's authentication status may change if your site uses sliding expiration with Forms Authentication, and there is no activity to keep the authentication cookie valid.</span></span> <span data-ttu-id="9e859-238">In tal caso, l'utente verrà disconnesso e il nome utente non corrisponderà più al nome utente nel token di connessione.</span><span class="sxs-lookup"><span data-stu-id="9e859-238">In that case, the user will be logged out and the user name will no longer match the user name in the connection token.</span></span> <span data-ttu-id="9e859-239">È possibile risolvere questo problema aggiungendo uno script che richiede periodicamente una risorsa sul server web per mantenere valido il cookie di autenticazione.</span><span class="sxs-lookup"><span data-stu-id="9e859-239">You can fix this problem by adding some script that periodically requests a resource on the web server to keep the authentication cookie valid.</span></span> <span data-ttu-id="9e859-240">Nell'esempio seguente viene illustrato come richiedere una risorsa ogni 30 minuti.</span><span class="sxs-lookup"><span data-stu-id="9e859-240">The following example shows how to request a resource every 30 minutes.</span></span>

[!code-javascript[Main](introduction-to-security/samples/sample4.js)]

<a id="autogen"></a>

### <a name="automatically-generated-javascript-proxy-files"></a><span data-ttu-id="9e859-241">File proxy JavaScript generati automaticamente</span><span class="sxs-lookup"><span data-stu-id="9e859-241">Automatically generated JavaScript proxy files</span></span>

<span data-ttu-id="9e859-242">Se non si desidera includere tutti gli hub e i metodi nel file proxy JavaScript per ogni utente, è possibile disabilitare la generazione automatica del file.</span><span class="sxs-lookup"><span data-stu-id="9e859-242">If you do not want to include all of the hubs and methods in the JavaScript proxy file for each user, you can disable the automatic generation of the file.</span></span> <span data-ttu-id="9e859-243">È possibile scegliere questa opzione se si dispone di più hub e metodi, ma non si desidera che tutti gli utenti siano a conoscenza di tutti i metodi.</span><span class="sxs-lookup"><span data-stu-id="9e859-243">You might choose this option if you have multiple hubs and methods, but do not want every user to be aware of all of the methods.</span></span> <span data-ttu-id="9e859-244">Per disabilitare la generazione automatica, **impostare EnableJavaScriptProxies** su **false**.</span><span class="sxs-lookup"><span data-stu-id="9e859-244">You disable automatic generation by setting **EnableJavaScriptProxies** to **false**.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample5.cs)]

<span data-ttu-id="9e859-245">Per ulteriori informazioni sui file proxy JavaScript, consultate [Il proxy generato e le operazioni eseguite automaticamente.](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy)</span><span class="sxs-lookup"><span data-stu-id="9e859-245">For more information about the JavaScript proxy files, see [The generated proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span></span> <a id="exceptions"></a>

### <a name="exceptions"></a><span data-ttu-id="9e859-246">Eccezioni</span><span class="sxs-lookup"><span data-stu-id="9e859-246">Exceptions</span></span>

<span data-ttu-id="9e859-247">È consigliabile evitare di passare oggetti eccezione ai client perché gli oggetti possono esporre informazioni riservate ai client.</span><span class="sxs-lookup"><span data-stu-id="9e859-247">You should avoid passing exception objects to clients because the objects may expose sensitive information to the clients.</span></span> <span data-ttu-id="9e859-248">Chiamare invece un metodo sul client che visualizza il messaggio di errore pertinente.</span><span class="sxs-lookup"><span data-stu-id="9e859-248">Instead, call a method on the client that displays the relevant error message.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample6.cs)]
